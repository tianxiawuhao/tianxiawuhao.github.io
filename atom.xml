<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>http://localhost:4001</id>
    <title>tianxiawuhao</title>
    <updated>2021-04-22T09:26:44.226Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="http://localhost:4001"/>
    <link rel="self" href="https://tianxiawuhao.github.io/atom.xml"/>
    <subtitle>风起于青萍之末,浪成于微澜之间.友交于微末之时,妄终于菩提之巅</subtitle>
    <logo>https://tianxiawuhao.github.io/images/avatar.png</logo>
    <icon>https://tianxiawuhao.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, tianxiawuhao</rights>
    <entry>
        <title type="html"><![CDATA[附录 Flink常见面试问题汇总]]></title>
        <id>https://tianxiawuhao.github.io/post/EEiV7EREL/</id>
        <link href="https://tianxiawuhao.github.io/post/EEiV7EREL/">
        </link>
        <updated>2021-04-17T05:16:56.000Z</updated>
        <content type="html"><![CDATA[<h2 id="面试题一应用架构">面试题一：应用架构</h2>
<p>问题： 公司怎么提交的实时任务， 有多少 Job Manager、 Task Manager？<br>
解答：</p>
<ol>
<li>我们使用 yarn session 模式提交任务； 另一种方式是每次提交都会创建一个新的 Flink 集群， 为每一个 job 提供资源， 任务之间互相独立， 互不影响，<br>
方便管理。 任务执行完成之后创建的集群也会消失。 线上命令脚本如下：bin/yarn-session.sh -n 7 -s 8 -jm 3072 -tm 32768 -qu root.<em>.</em> -nm <em>-</em> -d其中申请 7 个 taskManager， 每个 8 核， 每个 taskmanager 有 32768M 内存。</li>
<li>集群默认只有一个 Job Manager。 但为了防止单点故障， 我们配置了高可用。<br>
对于 standlone 模式， 我们公司一般配置一个主 Job Manager， 两个备用 Job<br>
Manager， 然后结合 ZooKeeper 的使用， 来达到高可用； 对于 yarn 模式， yarn 在<br>
Job Mananger 故障会自动进行重启， 所以只需要一个， 我们配置的最大重启次数是<br>
10 次。</li>
</ol>
<h2 id="面试题二压测和监控">面试题二：压测和监控</h2>
<p>问题： 怎么做压力测试和监控？<br>
解答： 我们一般碰到的压力来自以下几个方面：</p>
<ol>
<li>产生数据流的速度如果过快， 而下游的算子消费不过来的话， 会产生背压。<br>
背压的监控可以使用 Flink Web UI(localhost:8081) 来可视化监控 Metrics， 一旦报警<br>
就能知道。 一般情况下背压问题的产生可能是由于 sink 这个 操作符没有优化好，<br>
做一下优化就可以了。 比如如果是写入 ElasticSearch， 那么可以改成批量写入，<br>
可以调大 ElasticSearch 队列的大小等等策略。</li>
<li>设置 watermark 的最大延迟时间这个参数， 如果设置的过大， 可能会造成<br>
内存的压力。 可以设置最大延迟时间小一些， 然后把迟到元素发送到侧输出流中去。<br>
晚一点更新结果。 或者使用类似于 RocksDB 这样的状态后端， RocksDB 会开辟<br>
堆外存储空间， 但 IO 速度会变慢， 需要权衡。</li>
<li>还有就是滑动窗口的长度如果过长， 而滑动距离很短的话， Flink 的性能<br>
会下降的很厉害。 我们主要通过时间分片的方法， 将每个元素只存入一个“ 重叠窗<br>
口” ， 这样就可以减少窗口处理中状态的写入。 参见链接：<br>
https://www.infoq.cn/article/sIhs_qY6HCpMQNblTI9M</li>
<li>状态后端使用 RocksDB， 还没有碰到被撑爆的问题。</li>
</ol>
<h2 id="面试题三为什么用-flink">面试题三：为什么用 Flink</h2>
<p>问题： 为什么使用 Flink 替代 Spark？<br>
解答： 主要考虑的是 flink 的低延迟、 高吞吐量和对流式数据应用场景更好的支<br>
持； 另外， flink 可以很好地处理乱序数据， 而且可以保证 exactly-once 的状态一致<br>
性。 详见文档第一章， 有 Flink 和 Spark 的详细对比。</p>
<h2 id="面试题四checkpoint-的存储">面试题四：checkpoint 的存储</h2>
<p>问题： Flink 的 checkpoint 存在哪里？<br>
解答： 可以是内存， 文件系统， 或者 RocksDB。 详见文档 第九章 状态编程和容错机制。</p>
<h2 id="面试题五exactly-once-的保证">面试题五：exactly-once 的保证</h2>
<p>问题： 如果下级存储不支持事务， Flink 怎么保证 exactly-once？<br>
解答： 端到端的 exactly-once 对 sink 要求比较高， 具体实现主要有幂等写入和<br>
事务性写入两种方式。 幂等写入的场景依赖于业务逻辑， 更常见的是用事务性写入。<br>
而事务性写入又有预写日志（ WAL） 和两阶段提交（ 2PC） 两种方式。<br>
如果外部系统不支持事务， 那么可以用预写日志的方式， 把结果数据先当成状<br>
态保存， 然后在收到 checkpoint 完成的通知时， 一次性写入 sink 系统。<br>
参见文档第九章 状态编程和容错机制</p>
<h2 id="面试题六状态机制">面试题六：状态机制</h2>
<p>问题： 说一下 Flink 状态机制？<br>
解答： Flink 内置的很多算子， 包括源 source， 数据存储 sink 都是有状态的。 在<br>
Flink 中， 状态始终与特定算子相关联。 Flink 会以 checkpoint 的形式对各个任务的<br>
状态进行快照， 用于保证故障恢复时的状态一致性。 Flink 通过状态后端来管理状态<br>
和 checkpoint 的存储， 状态后端可以有不同的配置选择。 详见文档第九章。</p>
<h2 id="面试题七海量-key-去重">面试题七：海量 key 去重</h2>
<p>问题： 怎么去重？ 考虑一个实时场景： 双十一场景， 滑动窗口长度为 1 小时，<br>
滑动距离为 10 秒钟， 亿级用户， 怎样计算 UV？<br>
解答： 使用类似于 scala 的 set 数据结构或者 redis 的 set 显然是不行的，<br>
因为可能有上亿个 Key， 内存放不下。 所以可以考虑使用布隆过滤器（ Bloom Filter）<br>
来去重。</p>
<h2 id="面试题八checkpoint-与-spark-比较">面试题八：checkpoint 与 spark 比较</h2>
<p>问题： Flink 的 checkpoint 机制对比 spark 有什么不同和优势？<br>
解答： spark streaming 的 checkpoint 仅仅是针对 driver 的故障恢复做了数据<br>
和元数据的 checkpoint。 而 flink 的 checkpoint 机制 要复杂了很多， 它采用的是<br>
轻量级的分布式快照， 实现了每个算子的快照， 及流动中的数据的快照。 参见文档<br>
第九章 状态编程和容错机制及文章链接： https://cloud.tencent.com/developer/article/1189624</p>
<h2 id="面试题九watermark-机制">面试题九：watermark 机制</h2>
<p>问题： 请详细解释一下 Flink 的 Watermark 机制。<br>
解答： Watermark 本质是 Flink 中衡量 EventTime 进展的一个机制， 主要用来处<br>
理乱序数据。 详见文档第七章 时间语义与 Wartermark。</p>
<p>10．面试题十：exactly-once 如何实现<br>
问题： Flink 中 exactly-once 语义是如何实现的， 状态是如何存储的？<br>
解答： Flink 依靠 checkpoint 机制来实现 exactly-once 语义， 如果要实现端到端<br>
的 exactly-once， 还需要外部 source 和 sink 满足一定的条件。 状态的存储通过状态<br>
后端来管理， Flink 中可以配置不同的状态后端。 详见文档 第九章 状态编程和容错机制。</p>
<h2 id="面试题十一cep">面试题十一：CEP</h2>
<p>问题： Flink CEP 编程中当状态没有到达的时候会将数据保存在哪里？<br>
解答： 在流式处理中， CEP 当然是要支持 EventTime 的， 那么相对应的也要<br>
支持数据的迟到现象， 也就是 watermark 的处理逻辑。 CEP 对未匹配成功的事件序<br>
列的处理， 和迟到数据是类似的。 在 Flink CEP 的处理逻辑中， 状态没有满足的和<br>
迟到的数据， 都会存储在一个 Map 数据结构中， 也就是说， 如果我们限定判断事件<br>
序列的时长为 5 分钟， 那么内存中就会存储 5 分钟的数据， 这在我看来， 也是对内<br>
存的极大损伤之一。</p>
<h2 id="面试题十二三种时间语义">面试题十二：三种时间语义</h2>
<p>问题： Flink 三种时间语义是什么， 分别说出应用场景？<br>
解答：</p>
<ol>
<li>Event Time： 这是实际应用最常见的时间语义， 具体见文档第七章。</li>
<li>Processing Time： 没有事件时间的情况下， 或者对实时性要求超高的情况下。</li>
<li>Ingestion Time： 存在多个 Source Operator 的情况下， 每个 Source Operator<br>
可以使用自己本地系统时钟指派 Ingestion Time。 后续基于时间相关的各种操作，<br>
都会使用数据记录中的 Ingestion Time。</li>
</ol>
<h2 id="面试题十三数据高峰的处理">面试题十三：数据高峰的处理</h2>
<p>问题： Flink 程序在面对数据高峰期时如何处理？<br>
解答： 使用大容量的 Kafka 把数据先放到消息队列里面作为数据源， 再使用<br>
Flink 进行消费， 不过这样会影响到一点实时性</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[第十章 Table API 与 SQL]]></title>
        <id>https://tianxiawuhao.github.io/post/tloS_nSAM/</id>
        <link href="https://tianxiawuhao.github.io/post/tloS_nSAM/">
        </link>
        <updated>2021-04-16T02:43:29.000Z</updated>
        <content type="html"><![CDATA[<p style="text-indent:2em">Table API 是流处理和批处理通用的关系型 API， Table API 可以基于流输入或者批输入来运行而不需要进行任何修改。 Table API 是 SQL 语言的超集并专门为 Apache Flink 设计的， Table API 是 Scala 和 Java 语言集成式的 API。 与常规 SQL 语言中将查询指定为字符串不同， Table API 查询是以 Java 或 Scala 中的语言嵌入样式来定义
的， 具有 IDE 支持如:自动完成和语法检测。</p>
<h2 id="需要引入的-pom-依赖">需要引入的 pom 依赖</h2>
<p>如果你想在 IDE 本地运行你的程序，你需要添加下面的模块，具体用哪个取决于你使用哪个 Planner：</p>
<pre><code class="language-java">&lt;!-- Either... (for the old planner that was available before Flink 1.9) --&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.apache.flink&lt;/groupId&gt;
    &lt;artifactId&gt;flink-table-planner_2.12&lt;/artifactId&gt;
    &lt;version&gt;1.10.1&lt;/version&gt;
&lt;/dependency&gt;
&lt;!-- or.. (for the new Blink planner) --&gt;
&lt;dependency&gt;
  &lt;groupId&gt;org.apache.flink&lt;/groupId&gt;
  &lt;artifactId&gt;flink-table-planner-blink_2.12&lt;/artifactId&gt;
  &lt;version&gt;1.10.1&lt;/version&gt;
  &lt;scope&gt;provided&lt;/scope&gt;
&lt;/dependency&gt;
</code></pre>
<p>此外，根据目标编程语言，您需要添加Java或Scala API。</p>
<pre><code class="language-java">&lt;!-- Either... --&gt;
&lt;dependency&gt;
  &lt;groupId&gt;org.apache.flink&lt;/groupId&gt;
  &lt;artifactId&gt;flink-table-api-java-bridge_2.11&lt;/artifactId&gt;
  &lt;version&gt;1.8.0&lt;/version&gt;
&lt;/dependency&gt;
&lt;!-- or... --&gt;
&lt;dependency&gt;
  &lt;groupId&gt;org.apache.flink&lt;/groupId&gt;
  &lt;artifactId&gt;flink-table-api-scala-bridge_2.11&lt;/artifactId&gt;
  &lt;version&gt;1.8.0&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<p>在内部，表生态系统的一部分是在Scala中实现的。 因此，请确保为批处理和流应用程序添加以下依赖项：</p>
<pre><code class="language-java">&lt;dependency&gt;
  &lt;groupId&gt;org.apache.flink&lt;/groupId&gt;
  &lt;artifactId&gt;flink-streaming-scala_2.11&lt;/artifactId&gt;
  &lt;version&gt;1.8.0&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<p>如果要实现与Kafka或一组用户定义函数交互的自定义格式，以下依赖关系就足够了，可用于SQL客户端的JAR文件：</p>
<pre><code class="language-java">&lt;dependency&gt;
  &lt;groupId&gt;org.apache.flink&lt;/groupId&gt;
  &lt;artifactId&gt;flink-table-common&lt;/artifactId&gt;
  &lt;version&gt;1.8.0&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<p>目前，该模块包括以下扩展点：</p>
<ul>
<li>SerializationSchemaFactory</li>
<li>DeserializationSchemaFactory</li>
<li>ScalarFunction</li>
<li>TableFunction</li>
<li>AggregateFunction</li>
</ul>
<h2 id="两种计划器planner的主要区别">两种计划器（Planner）的主要区别</h2>
<ol>
<li>Blink 将批处理作业视作流处理的一种特例。严格来说，<code>Table</code> 和 <code>DataSet</code> 之间不支持相互转换，并且批处理作业也不会转换成 <code>DataSet</code> 程序而是转换成 <code>DataStream</code> 程序，流处理作业也一样。</li>
<li>Blink 计划器不支持 <code>BatchTableSource</code>，而是使用有界的 <code>StreamTableSource</code> 来替代。</li>
<li>旧计划器和 Blink 计划器中 <code>FilterableTableSource</code> 的实现是不兼容的。旧计划器会将 <code>PlannerExpression</code> 下推至 <code>FilterableTableSource</code>，而 Blink 计划器则是将 <code>Expression</code> 下推。</li>
<li>基于字符串的键值配置选项仅在 Blink 计划器中使用。（详情参见 <a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/config.html">配置</a> ）</li>
<li><code>PlannerConfig</code> 在两种计划器中的实现（<code>CalciteConfig</code>）是不同的。</li>
<li>Blink 计划器会将多sink（multiple-sinks）优化成一张有向无环图（DAG），<code>TableEnvironment</code> 和 <code>StreamTableEnvironment</code> 都支持该特性。旧计划器总是将每个sink都优化成一个新的有向无环图，且所有图相互独立。</li>
<li>旧计划器目前不支持 catalog 统计数据，而 Blink 支持。</li>
</ol>
<h2 id="简单了解-tableapi">简单了解 TableAPI</h2>
<p>所有用于批处理和流处理的 Table API 和 SQL 程序都遵循相同的模式。下面的代码示例展示了 Table API 和 SQL 程序的通用结构。</p>
<pre><code class="language-java">public static void main(String[] args) throws Exception {
    // 对于批处理程序来说使用 ExecutionEnvironment 来替换 StreamExecutionEnvironment
    StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();

    // 创建一个TableEnvironment
    // 对于批处理程序来说使用 BatchTableEnvironment 替换 StreamTableEnvironment
    StreamTableEnvironment tableEnv = TableEnvironment.getTableEnvironment(env);

    // 注册一个 Table
    tableEnv.registerTable(&quot;table1&quot;, ...)            // 或者
    tableEnv.registerTableSource(&quot;table2&quot;, ...);     // 或者
    tableEnv.registerExternalCatalog(&quot;extCat&quot;, ...);

    // 从Table API的查询中创建一个Table
    Table tapiResult = tableEnv.scan(&quot;table1&quot;).select(...);
    // 从SQL查询中创建一个Table
    Table sqlResult  = tableEnv.sql(&quot;SELECT ... FROM table2 ... &quot;);

    // 将Table API 种的结果 Table 发射到TableSink中 , SQL查询也是一样的
    tapiResult.writeToSink(...);

    // 执行
    env.execute();
}
</code></pre>
<h2 id="创建-tableenvironment">创建 TableEnvironment</h2>
<p><code>TableEnvironment</code> 是 Table API 和 SQL 的核心概念。它负责:</p>
<ul>
<li>在内部的 catalog 中注册 <code>Table</code></li>
<li>注册外部的 catalog</li>
<li>加载可插拔模块</li>
<li>执行 SQL 查询</li>
<li>注册自定义函数 （scalar、table 或 aggregation）</li>
<li>将 <code>DataStream</code> 或 <code>DataSet</code> 转换成 <code>Table</code></li>
<li>持有对 <code>ExecutionEnvironment</code> 或 <code>StreamExecutionEnvironment</code> 的引用</li>
</ul>
<p><code>Table</code> 总是与特定的 <code>TableEnvironment</code> 绑定。不能在同一条查询中使用不同 TableEnvironment 中的表，例如，对它们进行 join 或 union 操作。</p>
<p><code>TableEnvironment</code> 可以通过静态方法 <code>BatchTableEnvironment.create()</code> 或者 <code>StreamTableEnvironment.create()</code> 在 <code>StreamExecutionEnvironment</code> 或者 <code>ExecutionEnvironment</code> 中创建，<code>TableConfig</code> 是可选项。<code>TableConfig</code>可用于配置<code>TableEnvironment</code>或定制的查询优化和转换过程(参见 <a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/common.html#query-optimization">查询优化</a>)。</p>
<p>请确保选择与你的编程语言匹配的特定的计划器<code>BatchTableEnvironment</code>/<code>StreamTableEnvironment</code>。</p>
<p>如果两种计划器的 jar 包都在 classpath 中（默认行为），你应该明确地设置要在当前程序中使用的计划器。</p>
<pre><code class="language-java">// **********************
// FLINK STREAMING QUERY
// **********************
import org.apache.flink.streaming.api.environment.StreamExecutionEnvironment;
import org.apache.flink.table.api.EnvironmentSettings;
import org.apache.flink.table.api.bridge.java.StreamTableEnvironment;

EnvironmentSettings fsSettings = EnvironmentSettings.newInstance().useOldPlanner().inStreamingMode().build();
StreamExecutionEnvironment fsEnv = StreamExecutionEnvironment.getExecutionEnvironment();
StreamTableEnvironment fsTableEnv = StreamTableEnvironment.create(fsEnv, fsSettings);
// or TableEnvironment fsTableEnv = TableEnvironment.create(fsSettings);

// ******************
// FLINK BATCH QUERY
// ******************
import org.apache.flink.api.java.ExecutionEnvironment;
import org.apache.flink.table.api.bridge.java.BatchTableEnvironment;

ExecutionEnvironment fbEnv = ExecutionEnvironment.getExecutionEnvironment();
BatchTableEnvironment fbTableEnv = BatchTableEnvironment.create(fbEnv);

// **********************
// BLINK STREAMING QUERY
// **********************
import org.apache.flink.streaming.api.environment.StreamExecutionEnvironment;
import org.apache.flink.table.api.EnvironmentSettings;
import org.apache.flink.table.api.bridge.java.StreamTableEnvironment;

StreamExecutionEnvironment bsEnv = StreamExecutionEnvironment.getExecutionEnvironment();
EnvironmentSettings bsSettings = EnvironmentSettings.newInstance().useBlinkPlanner().inStreamingMode().build();
StreamTableEnvironment bsTableEnv = StreamTableEnvironment.create(bsEnv, bsSettings);
// or TableEnvironment bsTableEnv = TableEnvironment.create(bsSettings);

// ******************
// BLINK BATCH QUERY
// ******************
import org.apache.flink.table.api.EnvironmentSettings;
import org.apache.flink.table.api.TableEnvironment;

EnvironmentSettings bbSettings = EnvironmentSettings.newInstance().useBlinkPlanner().inBatchMode().build();
TableEnvironment bbTableEnv = TableEnvironment.create(bbSettings);
</code></pre>
<h2 id="在-catalog-中创建表">在 Catalog 中创建表</h2>
<p><code>TableEnvironment</code> 维护着一个由标识符（identifier）创建的表 catalog 的映射。标识符由三个部分组成：catalog 名称、数据库名称以及对象名称。如果 catalog 或者数据库没有指明，就会使用当前默认值（参见<a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/common.html#expanding-table-identifiers">表标识符扩展</a>章节中的例子）。</p>
<p><code>Table</code> 可以是虚拟的（视图 <code>VIEWS</code>）也可以是常规的（表 <code>TABLES</code>）。视图 <code>VIEWS</code>可以从已经存在的<code>Table</code>中创建，一般是 Table API 或者 SQL 的查询结果。 表<code>TABLES</code>描述的是外部数据，例如文件、数据库表或者消息队列。</p>
<h3 id="临时表temporary-table和永久表permanent-table">临时表（Temporary Table）和永久表（Permanent Table）</h3>
<p>表可以是临时的，并与单个 Flink 会话（session）的生命周期相关，也可以是永久的，并且在多个 Flink 会话和群集（cluster）中可见。</p>
<p>永久表需要 <a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/catalogs.html">catalog</a>（例如 Hive Metastore）以维护表的元数据。一旦永久表被创建，它将对任何连接到 catalog 的 Flink 会话可见且持续存在，直至被明确删除。</p>
<p>另一方面，临时表通常保存于内存中并且仅在创建它们的 Flink 会话持续期间存在。这些表对于其它会话是不可见的。它们不与任何 catalog 或者数据库绑定但可以在一个命名空间（namespace）中创建。即使它们对应的数据库被删除，临时表也不会被删除。</p>
<h4 id="屏蔽shadowing">屏蔽（Shadowing）</h4>
<p>可以使用与已存在的永久表相同的标识符去注册临时表。临时表会屏蔽永久表，并且只要临时表存在，永久表就无法访问。所有使用该标识符的查询都将作用于临时表。</p>
<p>这可能对实验（experimentation）有用。它允许先对一个临时表进行完全相同的查询，例如只有一个子集的数据，或者数据是不确定的。一旦验证了查询的正确性，就可以对实际的生产表进行查询。</p>
<h3 id="创建表">创建表</h3>
<h4 id="虚拟表">虚拟表</h4>
<p>在 SQL 的术语中，Table API 的对象对应于<code>视图</code>（虚拟表）。它封装了一个逻辑查询计划。它可以通过以下方法在 catalog 中创建：</p>
<pre><code class="language-java">// get a TableEnvironment
TableEnvironment tableEnv = ...; // see &quot;Create a TableEnvironment&quot; section

// table is the result of a simple projection query 
Table projTable = tableEnv.from(&quot;X&quot;).select(...);

// register the Table projTable as table &quot;projectedTable&quot;
tableEnv.createTemporaryView(&quot;projectedTable&quot;, projTable);
</code></pre>
<p><strong>注意：</strong> 从传统数据库系统的角度来看，<code>Table</code> 对象与 <code>VIEW</code> 视图非常像。也就是，定义了 <code>Table</code> 的查询是没有被优化的， 而且会被内嵌到另一个引用了这个注册了的 <code>Table</code>的查询中。如果多个查询都引用了同一个注册了的<code>Table</code>，那么它会被内嵌每个查询中并被执行多次， 也就是说注册了的<code>Table</code>的结果<strong>不会</strong>被共享（注：Blink 计划器的<code>TableEnvironment</code>会优化成只执行一次）。</p>
<h4 id="connector-tables">Connector Tables</h4>
<p>另外一个方式去创建 <code>TABLE</code> 是通过 <a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/connect.html">connector</a> 声明。Connector 描述了存储表数据的外部系统。存储系统例如 Apache Kafka 或者常规的文件系统都可以通过这种方式来声明。</p>
<pre><code class="language-java">tableEnvironment
  .connect(...)
  .withFormat(...)
  .withSchema(...)
  .inAppendMode()
  .createTemporaryTable(&quot;MyTable&quot;)
</code></pre>
<h3 id="扩展表标识符">扩展表标识符</h3>
<p>表总是通过三元标识符注册，包括 catalog 名、数据库名和表名。</p>
<p>用户可以指定一个 catalog 和数据库作为 “当前catalog” 和”当前数据库”。有了这些，那么刚刚提到的三元标识符的前两个部分就可以被省略了。如果前两部分的标识符没有指定， 那么会使用当前的 catalog 和当前数据库。用户也可以通过 Table API 或 SQL 切换当前的 catalog 和当前的数据库。</p>
<p>标识符遵循 SQL 标准，因此使用时需要用反引号（```）进行转义。</p>
<pre><code class="language-java">TableEnvironment tEnv = ...;
tEnv.useCatalog(&quot;custom_catalog&quot;);
tEnv.useDatabase(&quot;custom_database&quot;);

Table table = ...;

// register the view named 'exampleView' in the catalog named 'custom_catalog'
// in the database named 'custom_database' 
tableEnv.createTemporaryView(&quot;exampleView&quot;, table);

// register the view named 'exampleView' in the catalog named 'custom_catalog'
// in the database named 'other_database' 
tableEnv.createTemporaryView(&quot;other_database.exampleView&quot;, table);

// register the view named 'example.View' in the catalog named 'custom_catalog'
// in the database named 'custom_database' 
tableEnv.createTemporaryView(&quot;`example.View`&quot;, table);

// register the view named 'exampleView' in the catalog named 'other_catalog'
// in the database named 'other_database' 
tableEnv.createTemporaryView(&quot;other_catalog.other_database.exampleView&quot;, table);
</code></pre>
<h2 id="查询表">查询表</h2>
<h3 id="table-api">Table API</h3>
<p>Table API 是关于 Scala 和 Java 的集成语言式查询 API。与 SQL 相反，Table API 的查询不是由字符串指定，而是在宿主语言中逐步构建。</p>
<p>Table API 是基于 <code>Table</code> 类的，该类表示一个表（流或批处理），并提供使用关系操作的方法。这些方法返回一个新的 Table 对象，该对象表示对输入 Table 进行关系操作的结果。 一些关系操作由多个方法调用组成，例如 <code>table.groupBy(...).select()</code>，其中 <code>groupBy(...)</code> 指定 <code>table</code> 的分组，而 <code>select(...)</code> 在 <code>table</code> 分组上的投影。</p>
<p>文档 <a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/tableApi.html">Table API</a> 说明了所有流处理和批处理表支持的 Table API 算子。</p>
<p>以下示例展示了一个简单的 Table API 聚合查询：</p>
<pre><code class="language-java">// get a TableEnvironment
TableEnvironment tableEnv = ...; // see &quot;Create a TableEnvironment&quot; section

// register Orders table

// scan registered Orders table
Table orders = tableEnv.from(&quot;Orders&quot;);
// compute revenue for all customers from France
Table revenue = orders
  .filter($(&quot;cCountry&quot;).isEqual(&quot;FRANCE&quot;))
  .groupBy($(&quot;cID&quot;), $(&quot;cName&quot;)
  .select($(&quot;cID&quot;), $(&quot;cName&quot;), $(&quot;revenue&quot;).sum().as(&quot;revSum&quot;));

// emit or convert Table
// execute query
</code></pre>
<h3 id="sql">SQL</h3>
<p>Flink SQL 是基于实现了SQL标准的 <a href="https://calcite.apache.org/">Apache Calcite</a> 的。SQL 查询由常规字符串指定。</p>
<p>文档 <a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/sql/">SQL</a> 描述了Flink对流处理和批处理表的SQL支持。</p>
<p>下面的示例演示了如何指定查询并将结果作为 <code>Table</code> 对象返回。</p>
<pre><code class="language-java">// get a TableEnvironment
TableEnvironment tableEnv = ...; // see &quot;Create a TableEnvironment&quot; section

// register Orders table

// compute revenue for all customers from France
Table revenue = tableEnv.sqlQuery(
    &quot;SELECT cID, cName, SUM(revenue) AS revSum &quot; +
    &quot;FROM Orders &quot; +
    &quot;WHERE cCountry = 'FRANCE' &quot; +
    &quot;GROUP BY cID, cName&quot;
  );

// emit or convert Table
// execute query
</code></pre>
<p>如下的示例展示了如何指定一个更新查询，将查询的结果插入到已注册的表中。</p>
<pre><code class="language-java">// get a TableEnvironment
TableEnvironment tableEnv = ...; // see &quot;Create a TableEnvironment&quot; section

// register &quot;Orders&quot; table
// register &quot;RevenueFrance&quot; output table

// compute revenue for all customers from France and emit to &quot;RevenueFrance&quot;
tableEnv.executeSql(
    &quot;INSERT INTO RevenueFrance &quot; +
    &quot;SELECT cID, cName, SUM(revenue) AS revSum &quot; +
    &quot;FROM Orders &quot; +
    &quot;WHERE cCountry = 'FRANCE' &quot; +
    &quot;GROUP BY cID, cName&quot;
  );
</code></pre>
<h3 id="混用-table-api-和-sql">混用 Table API 和 SQL</h3>
<p>Table API 和 SQL 查询的混用非常简单因为它们都返回 <code>Table</code> 对象：</p>
<ul>
<li>可以在 SQL 查询返回的 <code>Table</code> 对象上定义 Table API 查询。</li>
<li>在 <code>TableEnvironment</code> 中注册的<a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/common.html#register-a-table">结果表</a>可以在 SQL 查询的 <code>FROM</code> 子句中引用，通过这种方法就可以在 Table API 查询的结果上定义 SQL 查询。</li>
</ul>
<h2 id="输出表">输出表</h2>
<p><code>Table</code> 通过写入 <code>TableSink</code> 输出。<code>TableSink</code> 是一个通用接口，用于支持多种文件格式（如 CSV、Apache Parquet、Apache Avro）、存储系统（如 JDBC、Apache HBase、Apache Cassandra、Elasticsearch）或消息队列系统（如 Apache Kafka、RabbitMQ）。</p>
<p>批处理 <code>Table</code> 只能写入 <code>BatchTableSink</code>，而流处理 <code>Table</code> 需要指定写入 <code>AppendStreamTableSink</code>，<code>RetractStreamTableSink</code> 或者 <code>UpsertStreamTableSink</code>。</p>
<p>请参考文档 <a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/sourceSinks.html">Table Sources &amp; Sinks</a> 以获取更多关于可用 Sink 的信息以及如何自定义 <code>TableSink</code>。</p>
<p>方法 <code>Table.executeInsert(String tableName)</code> 将 <code>Table</code> 发送至已注册的 <code>TableSink</code>。该方法通过名称在 catalog 中查找 <code>TableSink</code> 并确认<code>Table</code> schema 和 <code>TableSink</code> schema 一致。</p>
<p>下面的示例演示如何输出 <code>Table</code>：</p>
<pre><code class="language-java">// get a TableEnvironment
TableEnvironment tableEnv = ...; // see &quot;Create a TableEnvironment&quot; section

// create an output Table
final Schema schema = new Schema()
    .field(&quot;a&quot;, DataTypes.INT())
    .field(&quot;b&quot;, DataTypes.STRING())
    .field(&quot;c&quot;, DataTypes.BIGINT());

tableEnv.connect(new FileSystem().path(&quot;/path/to/file&quot;))
    .withFormat(new Csv().fieldDelimiter('|').deriveSchema())
    .withSchema(schema)
    .createTemporaryTable(&quot;CsvSinkTable&quot;);

// compute a result Table using Table API operators and/or SQL queries
Table result = ...
// emit the result Table to the registered TableSink
result.executeInsert(&quot;CsvSinkTable&quot;);
</code></pre>
<h2 id="翻译与执行查询">翻译与执行查询</h2>
<p>两种计划器翻译和执行查询的方式是不同的。</p>
<ul>
<li><a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/common.html#tab_Blink_planner_9"><strong>Blink planner</strong></a></li>
<li><a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/common.html#tab_Old_planner_9"><strong>Old planner</strong></a></li>
</ul>
<p>不论输入数据源是流式的还是批式的，Table API 和 SQL 查询都会被转换成 <a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/datastream_api.html">DataStream</a> 程序。查询在内部表示为逻辑查询计划，并被翻译成两个阶段：</p>
<ol>
<li>优化逻辑执行计划</li>
<li>翻译成 DataStream 程序</li>
</ol>
<p>Table API 或者 SQL 查询在下列情况下会被翻译：</p>
<ul>
<li>当 <code>TableEnvironment.executeSql()</code> 被调用时。该方法是用来执行一个 SQL 语句，一旦该方法被调用， SQL 语句立即被翻译。</li>
<li>当 <code>Table.executeInsert()</code> 被调用时。该方法是用来将一个表的内容插入到目标表中，一旦该方法被调用， TABLE API 程序立即被翻译。</li>
<li>当 <code>Table.execute()</code> 被调用时。该方法是用来将一个表的内容收集到本地，一旦该方法被调用， TABLE API 程序立即被翻译。</li>
<li>当 <code>StatementSet.execute()</code> 被调用时。<code>Table</code> （通过 <code>StatementSet.addInsert()</code> 输出给某个 <code>Sink</code>）和 INSERT 语句 （通过调用 <code>StatementSet.addInsertSql()</code>）会先被缓存到 <code>StatementSet</code> 中，<code>StatementSet.execute()</code> 方法被调用时，所有的 sink 会被优化成一张有向无环图。</li>
<li>当 <code>Table</code> 被转换成 <code>DataStream</code> 时（参阅<a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/common.html#integration-with-datastream-and-dataset-api">与 DataStream 和 DataSet API 结合</a>）。转换完成后，它就成为一个普通的 DataStream 程序，并会在调用 <code>StreamExecutionEnvironment.execute()</code> 时被执行。</li>
</ul>
<p><strong>注意</strong> <strong>从 1.11 版本开始，<code>sqlUpdate</code> 方法 和 <code>insertInto</code> 方法被废弃，从这两个方法构建的 Table 程序必须通过 <code>StreamTableEnvironment.execute()</code> 方法执行，而不能通过 <code>StreamExecutionEnvironment.execute()</code> 方法来执行。</strong></p>
<h2 id="与-datastream-和-dataset-api-结合">与 DataStream 和 DataSet API 结合</h2>
<p>在流处理方面两种计划器都可以与 <code>DataStream</code> API 结合。只有旧计划器可以与 <code>DataSet API</code> 结合。在批处理方面，Blink 计划器不能同两种计划器中的任何一个结合。</p>
<p><strong>注意：</strong> 下文讨论的 <code>DataSet</code> API 只与旧计划起有关。</p>
<p>Table API 和 SQL 可以被很容易地集成并嵌入到 <a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/datastream_api.html">DataStream</a> 和 <a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/batch/">DataSet</a> 程序中。例如，可以查询外部表（例如从 RDBMS），进行一些预处理，例如过滤，投影，聚合或与元数据 join，然后使用 DataStream 或 DataSet API（以及在这些 API 之上构建的任何库，例如 CEP 或 Gelly）。相反，也可以将 Table API 或 SQL 查询应用于 DataStream 或 DataSet 程序的结果。</p>
<p>这种交互可以通过 <code>DataStream</code> 或 <code>DataSet</code> 与 <code>Table</code> 的相互转化实现。本节我们会介绍这些转化是如何实现的。</p>
<h3 id="scala-隐式转换">Scala 隐式转换</h3>
<p>Scala Table API 含有对 <code>DataSet</code>、<code>DataStream</code> 和 <code>Table</code> 类的隐式转换。 通过为 Scala DataStream API 导入 <code>org.apache.flink.table.api.bridge.scala._</code> 包以及 <code>org.apache.flink.api.scala._</code> 包，可以启用这些转换。</p>
<h3 id="通过-dataset-或-datastream-创建视图">通过 DataSet 或 DataStream 创建<code>视图</code></h3>
<p>在 <code>TableEnvironment</code> 中可以将 <code>DataStream</code> 或 <code>DataSet</code> 注册成视图。结果视图的 schema 取决于注册的 <code>DataStream</code> 或 <code>DataSet</code> 的数据类型。请参阅文档 <a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/common.html#mapping-of-data-types-to-table-schema">数据类型到 table schema 的映射</a>获取详细信息。</p>
<p><strong>注意：</strong> 通过 <code>DataStream</code> 或 <code>DataSet</code> 创建的视图只能注册成临时视图。</p>
<pre><code class="language-java">// get StreamTableEnvironment
// registration of a DataSet in a BatchTableEnvironment is equivalent
StreamTableEnvironment tableEnv = ...; // see &quot;Create a TableEnvironment&quot; section

DataStream&lt;Tuple2&lt;Long, String&gt;&gt; stream = ...

// register the DataStream as View &quot;myTable&quot; with fields &quot;f0&quot;, &quot;f1&quot;
tableEnv.createTemporaryView(&quot;myTable&quot;, stream);

// register the DataStream as View &quot;myTable2&quot; with fields &quot;myLong&quot;, &quot;myString&quot;
tableEnv.createTemporaryView(&quot;myTable2&quot;, stream, $(&quot;myLong&quot;), $(&quot;myString&quot;));
</code></pre>
<h3 id="将-datastream-或-dataset-转换成表">将 DataStream 或 DataSet 转换成表</h3>
<p>与在 <code>TableEnvironment</code> 注册 <code>DataStream</code> 或 <code>DataSet</code> 不同，DataStream 和 DataSet 还可以直接转换成 <code>Table</code>。如果你想在 Table API 的查询中使用表，这将非常便捷。</p>
<pre><code class="language-java">// get StreamTableEnvironment
// registration of a DataSet in a BatchTableEnvironment is equivalent
StreamTableEnvironment tableEnv = ...; // see &quot;Create a TableEnvironment&quot; section

DataStream&lt;Tuple2&lt;Long, String&gt;&gt; stream = ...

// Convert the DataStream into a Table with default fields &quot;f0&quot;, &quot;f1&quot;
Table table1 = tableEnv.fromDataStream(stream);

// Convert the DataStream into a Table with fields &quot;myLong&quot;, &quot;myString&quot;
Table table2 = tableEnv.fromDataStream(stream, $(&quot;myLong&quot;), $(&quot;myString&quot;));
</code></pre>
<h3 id="将表转换成-datastream-或-dataset">将表转换成 DataStream 或 DataSet</h3>
<p><code>Table</code> 可以被转换成 <code>DataStream</code> 或 <code>DataSet</code>。通过这种方式，定制的 DataSet 或 DataStream 程序就可以在 Table API 或者 SQL 的查询结果上运行了。</p>
<p>将 <code>Table</code> 转换为 <code>DataStream</code> 或者 <code>DataSet</code> 时，你需要指定生成的 <code>DataStream</code> 或者 <code>DataSet</code> 的数据类型，即，<code>Table</code> 的每行数据要转换成的数据类型。通常最方便的选择是转换成 <code>Row</code> 。以下列表概述了不同选项的功能：</p>
<ul>
<li><strong>Row</strong>: 字段按位置映射，字段数量任意，支持 <code>null</code> 值，无类型安全（type-safe）检查。</li>
<li><strong>POJO</strong>: 字段按名称映射（POJO 必须按<code>Table</code> 中字段名称命名），字段数量任意，支持 <code>null</code> 值，无类型安全检查。</li>
<li><strong>Case Class</strong>: 字段按位置映射，不支持 <code>null</code> 值，有类型安全检查。</li>
<li><strong>Tuple</strong>: 字段按位置映射，字段数量少于 22（Scala）或者 25（Java），不支持 <code>null</code> 值，无类型安全检查。</li>
<li><strong>Atomic Type</strong>: <code>Table</code> 必须有一个字段，不支持 <code>null</code> 值，有类型安全检查。</li>
</ul>
<h4 id="将表转换成-datastream">将表转换成 DataStream</h4>
<p>流式查询（streaming query）的结果表会动态更新，即，当新纪录到达查询的输入流时，查询结果会改变。因此，像这样将动态查询结果转换成 DataStream 需要对表的更新方式进行编码。</p>
<p>将 <code>Table</code> 转换为 <code>DataStream</code> 有两种模式：</p>
<ol>
<li><strong>Append Mode</strong>: 仅当动态 <code>Table</code> 仅通过<code>INSERT</code>更改进行修改时，才可以使用此模式，即，它仅是追加操作，并且之前输出的结果永远不会更新。</li>
<li><strong>Retract Mode</strong>: 任何情形都可以使用此模式。它使用 boolean 值对 <code>INSERT</code> 和 <code>DELETE</code> 操作的数据进行标记。</li>
</ol>
<pre><code class="language-java">// get StreamTableEnvironment. 
StreamTableEnvironment tableEnv = ...; // see &quot;Create a TableEnvironment&quot; section

// Table with two fields (String name, Integer age)
Table table = ...

// convert the Table into an append DataStream of Row by specifying the class
DataStream&lt;Row&gt; dsRow = tableEnv.toAppendStream(table, Row.class);

// convert the Table into an append DataStream of Tuple2&lt;String, Integer&gt; 
//   via a TypeInformation
TupleTypeInfo&lt;Tuple2&lt;String, Integer&gt;&gt; tupleType = new TupleTypeInfo&lt;&gt;(
  Types.STRING(),
  Types.INT());
DataStream&lt;Tuple2&lt;String, Integer&gt;&gt; dsTuple = 
  tableEnv.toAppendStream(table, tupleType);

// convert the Table into a retract DataStream of Row.
//   A retract stream of type X is a DataStream&lt;Tuple2&lt;Boolean, X&gt;&gt;. 
//   The boolean field indicates the type of the change. 
//   True is INSERT, false is DELETE.
DataStream&lt;Tuple2&lt;Boolean, Row&gt;&gt; retractStream = 
  tableEnv.toRetractStream(table, Row.class);
</code></pre>
<p><strong>注意：</strong> 文档<a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/streaming/dynamic_tables.html">动态表</a>给出了有关动态表及其属性的详细讨论。</p>
<p><strong>注意</strong> <strong>一旦 Table 被转化为 DataStream，必须使用 StreamExecutionEnvironment 的 execute 方法执行该 DataStream 作业。</strong></p>
<h4 id="将表转换成-dataset">将表转换成 DataSet</h4>
<p>将 <code>Table</code> 转换成 <code>DataSet</code> 的过程如下：</p>
<pre><code class="language-java">// get BatchTableEnvironment
BatchTableEnvironment tableEnv = BatchTableEnvironment.create(env);

// Table with two fields (String name, Integer age)
Table table = ...

// convert the Table into a DataSet of Row by specifying a class
DataSet&lt;Row&gt; dsRow = tableEnv.toDataSet(table, Row.class);

// convert the Table into a DataSet of Tuple2&lt;String, Integer&gt; via a TypeInformation
TupleTypeInfo&lt;Tuple2&lt;String, Integer&gt;&gt; tupleType = new TupleTypeInfo&lt;&gt;(
  Types.STRING(),
  Types.INT());
DataSet&lt;Tuple2&lt;String, Integer&gt;&gt; dsTuple = 
  tableEnv.toDataSet(table, tupleType);
</code></pre>
<p><strong>注意</strong> <strong>一旦 Table 被转化为 DataSet，必须使用 ExecutionEnvironment 的 execute 方法执行该 DataSet 作业。</strong></p>
<h3 id="数据类型到-table-schema-的映射">数据类型到 Table Schema 的映射</h3>
<p>Flink 的 DataStream 和 DataSet APIs 支持多样的数据类型。例如 Tuple（Scala 内置以及Flink Java tuple）、POJO 类型、Scala case class 类型以及 Flink 的 Row 类型等允许嵌套且有多个可在表的表达式中访问的字段的复合数据类型。其他类型被视为原子类型。下面，我们讨论 Table API 如何将这些数据类型类型转换为内部 row 表示形式，并提供将 <code>DataStream</code> 转换成 <code>Table</code> 的样例。</p>
<p>数据类型到 table schema 的映射有两种方式：<strong>基于字段位置</strong>或<strong>基于字段名称</strong>。</p>
<p><strong>基于位置映射</strong></p>
<p>基于位置的映射可在保持字段顺序的同时为字段提供更有意义的名称。这种映射方式可用于<em>具有特定的字段顺序</em>的复合数据类型以及原子类型。如 tuple、row 以及 case class 这些复合数据类型都有这样的字段顺序。然而，POJO 类型的字段则必须通过名称映射（参见下一章）。可以将字段投影出来，但不能使用<code>as</code>重命名。</p>
<p>定义基于位置的映射时，输入数据类型中一定不能存在指定的名称，否则 API 会假定应该基于字段名称进行映射。如果未指定任何字段名称，则使用默认的字段名称和复合数据类型的字段顺序，或者使用 <code>f0</code> 表示原子类型。</p>
<pre><code class="language-java">// get a StreamTableEnvironment, works for BatchTableEnvironment equivalently
StreamTableEnvironment tableEnv = ...; // see &quot;Create a TableEnvironment&quot; section;

DataStream&lt;Tuple2&lt;Long, Integer&gt;&gt; stream = ...

// convert DataStream into Table with default field names &quot;f0&quot; and &quot;f1&quot;
Table table = tableEnv.fromDataStream(stream);

// convert DataStream into Table with field &quot;myLong&quot; only
Table table = tableEnv.fromDataStream(stream, $(&quot;myLong&quot;));

// convert DataStream into Table with field names &quot;myLong&quot; and &quot;myInt&quot;
Table table = tableEnv.fromDataStream(stream, $(&quot;myLong&quot;), $(&quot;myInt&quot;));
</code></pre>
<p><strong>基于名称的映射</strong></p>
<p>基于名称的映射适用于任何数据类型包括 POJO 类型。这是定义 table schema 映射最灵活的方式。映射中的所有字段均按名称引用，并且可以通过 <code>as</code> 重命名。字段可以被重新排序和映射。</p>
<p>若果没有指定任何字段名称，则使用默认的字段名称和复合数据类型的字段顺序，或者使用 <code>f0</code> 表示原子类型。</p>
<pre><code class="language-java">// get a StreamTableEnvironment, works for BatchTableEnvironment equivalently
StreamTableEnvironment tableEnv = ...; // see &quot;Create a TableEnvironment&quot; section

DataStream&lt;Tuple2&lt;Long, Integer&gt;&gt; stream = ...

// convert DataStream into Table with default field names &quot;f0&quot; and &quot;f1&quot;
Table table = tableEnv.fromDataStream(stream);

// convert DataStream into Table with field &quot;f1&quot; only
Table table = tableEnv.fromDataStream(stream, $(&quot;f1&quot;));

// convert DataStream into Table with swapped fields
Table table = tableEnv.fromDataStream(stream, $(&quot;f1&quot;), $(&quot;f0&quot;));

// convert DataStream into Table with swapped fields and field names &quot;myInt&quot; and &quot;myLong&quot;
Table table = tableEnv.fromDataStream(stream, $(&quot;f1&quot;).as(&quot;myInt&quot;), $(&quot;f0&quot;).as(&quot;myLong&quot;));
</code></pre>
<h4 id="原子类型">原子类型</h4>
<p>Flink 将基础数据类型（<code>Integer</code>、<code>Double</code>、<code>String</code>）或者通用数据类型（不可再拆分的数据类型）视为原子类型。原子类型的 <code>DataStream</code> 或者 <code>DataSet</code> 会被转换成只有一条属性的 <code>Table</code>。属性的数据类型可以由原子类型推断出，还可以重新命名属性。</p>
<pre><code class="language-java">// get a StreamTableEnvironment, works for BatchTableEnvironment equivalently
StreamTableEnvironment tableEnv = ...; // see &quot;Create a TableEnvironment&quot; section

DataStream&lt;Long&gt; stream = ...

// convert DataStream into Table with default field name &quot;f0&quot;
Table table = tableEnv.fromDataStream(stream);

// convert DataStream into Table with field name &quot;myLong&quot;
Table table = tableEnv.fromDataStream(stream, $(&quot;myLong&quot;));
</code></pre>
<h4 id="tuple类型scala-和-java和-case-class类型仅-scala">Tuple类型（Scala 和 Java）和 Case Class类型（仅 Scala）</h4>
<p>Flink 支持 Scala 的内置 tuple 类型并给 Java 提供自己的 tuple 类型。两种 tuple 的 DataStream 和 DataSet 都能被转换成表。可以通过提供所有字段名称来重命名字段（基于位置映射）。如果没有指明任何字段名称，则会使用默认的字段名称。如果引用了原始字段名称（对于 Flink tuple 为<code>f0</code>、<code>f1</code> … …，对于 Scala tuple 为<code>_1</code>、<code>_2</code> … …），则 API 会假定映射是基于名称的而不是基于位置的。基于名称的映射可以通过 <code>as</code> 对字段和投影进行重新排序。</p>
<pre><code class="language-java">// get a StreamTableEnvironment, works for BatchTableEnvironment equivalently
StreamTableEnvironment tableEnv = ...; // see &quot;Create a TableEnvironment&quot; section

DataStream&lt;Tuple2&lt;Long, String&gt;&gt; stream = ...

// convert DataStream into Table with default field names &quot;f0&quot;, &quot;f1&quot;
Table table = tableEnv.fromDataStream(stream);

// convert DataStream into Table with renamed field names &quot;myLong&quot;, &quot;myString&quot; (position-based)
Table table = tableEnv.fromDataStream(stream, $(&quot;myLong&quot;), $(&quot;myString&quot;));

// convert DataStream into Table with reordered fields &quot;f1&quot;, &quot;f0&quot; (name-based)
Table table = tableEnv.fromDataStream(stream, $(&quot;f1&quot;), $(&quot;f0&quot;));

// convert DataStream into Table with projected field &quot;f1&quot; (name-based)
Table table = tableEnv.fromDataStream(stream, $(&quot;f1&quot;));

// convert DataStream into Table with reordered and aliased fields &quot;myString&quot;, &quot;myLong&quot; (name-based)
Table table = tableEnv.fromDataStream(stream, $(&quot;f1&quot;).as(&quot;myString&quot;), $(&quot;f0&quot;).as(&quot;myLong&quot;));
</code></pre>
<h4 id="pojo-类型-java-和-scala">POJO 类型 （Java 和 Scala）</h4>
<p>Flink 支持 POJO 类型作为复合类型。确定 POJO 类型的规则记录在<a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/types_serialization.html#pojos">这里</a>.</p>
<p>在不指定字段名称的情况下将 POJO 类型的 <code>DataStream</code> 或 <code>DataSet</code> 转换成 <code>Table</code> 时，将使用原始 POJO 类型字段的名称。名称映射需要原始名称，并且不能按位置进行。字段可以使用别名（带有 <code>as</code> 关键字）来重命名，重新排序和投影。</p>
<pre><code class="language-java">// get a StreamTableEnvironment, works for BatchTableEnvironment equivalently
StreamTableEnvironment tableEnv = ...; // see &quot;Create a TableEnvironment&quot; section

// Person is a POJO with fields &quot;name&quot; and &quot;age&quot;
DataStream&lt;Person&gt; stream = ...

// convert DataStream into Table with default field names &quot;age&quot;, &quot;name&quot; (fields are ordered by name!)
Table table = tableEnv.fromDataStream(stream);

// convert DataStream into Table with renamed fields &quot;myAge&quot;, &quot;myName&quot; (name-based)
Table table = tableEnv.fromDataStream(stream, $(&quot;age&quot;).as(&quot;myAge&quot;), $(&quot;name&quot;).as(&quot;myName&quot;));

// convert DataStream into Table with projected field &quot;name&quot; (name-based)
Table table = tableEnv.fromDataStream(stream, $(&quot;name&quot;));

// convert DataStream into Table with projected and renamed field &quot;myName&quot; (name-based)
Table table = tableEnv.fromDataStream(stream, $(&quot;name&quot;).as(&quot;myName&quot;));
</code></pre>
<h4 id="row类型">Row类型</h4>
<p><code>Row</code> 类型支持任意数量的字段以及具有 <code>null</code> 值的字段。字段名称可以通过 <code>RowTypeInfo</code> 指定，也可以在将 <code>Row</code> 的 <code>DataStream</code> 或 <code>DataSet</code> 转换为 <code>Table</code> 时指定。Row 类型的字段映射支持基于名称和基于位置两种方式。字段可以通过提供所有字段的名称的方式重命名（基于位置映射）或者分别选择进行投影/排序/重命名（基于名称映射）。</p>
<pre><code class="language-java">// get a StreamTableEnvironment, works for BatchTableEnvironment equivalently
StreamTableEnvironment tableEnv = ...; // see &quot;Create a TableEnvironment&quot; section

// DataStream of Row with two fields &quot;name&quot; and &quot;age&quot; specified in `RowTypeInfo`
DataStream&lt;Row&gt; stream = ...

// convert DataStream into Table with default field names &quot;name&quot;, &quot;age&quot;
Table table = tableEnv.fromDataStream(stream);

// convert DataStream into Table with renamed field names &quot;myName&quot;, &quot;myAge&quot; (position-based)
Table table = tableEnv.fromDataStream(stream, $(&quot;myName&quot;), $(&quot;myAge&quot;));

// convert DataStream into Table with renamed fields &quot;myName&quot;, &quot;myAge&quot; (name-based)
Table table = tableEnv.fromDataStream(stream, $(&quot;name&quot;).as(&quot;myName&quot;), $(&quot;age&quot;).as(&quot;myAge&quot;));

// convert DataStream into Table with projected field &quot;name&quot; (name-based)
Table table = tableEnv.fromDataStream(stream, $(&quot;name&quot;));

// convert DataStream into Table with projected and renamed field &quot;myName&quot; (name-based)
Table table = tableEnv.fromDataStream(stream, $(&quot;name&quot;).as(&quot;myName&quot;));
</code></pre>
<h2 id="查询优化">查询优化</h2>
<ul>
<li><a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/common.html#tab_Blink_planner_20"><strong>Blink planner</strong></a></li>
<li><a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/common.html#tab_Old_planner_20"><strong>Old planner</strong></a></li>
</ul>
<p>Apache Flink 使用并扩展了 Apache Calcite 来执行复杂的查询优化。 这包括一系列基于规则和成本的优化，例如：</p>
<ul>
<li>基于 Apache Calcite 的子查询解相关</li>
<li>投影剪裁</li>
<li>分区剪裁</li>
<li>过滤器下推</li>
<li>子计划消除重复数据以避免重复计算</li>
<li>特殊子查询重写，包括两部分：
<ul>
<li>将 IN 和 EXISTS 转换为 left semi-joins</li>
<li>将 NOT IN 和 NOT EXISTS 转换为 left anti-join</li>
</ul>
</li>
<li>可选 join 重新排序
<ul>
<li>通过 <code>table.optimizer.join-reorder-enabled</code> 启用</li>
</ul>
</li>
</ul>
<p><strong>注意：</strong> 当前仅在子查询重写的结合条件下支持 IN / EXISTS / NOT IN / NOT EXISTS。</p>
<p>优化器不仅基于计划，而且还基于可从数据源获得的丰富统计信息以及每个算子（例如 io，cpu，网络和内存）的细粒度成本来做出明智的决策。</p>
<p>高级用户可以通过 <code>CalciteConfig</code> 对象提供自定义优化，可以通过调用 <code>TableEnvironment＃getConfig＃setPlannerConfig</code> 将其提供给 TableEnvironment。</p>
<h2 id="解释表">解释表</h2>
<p>Table API 提供了一种机制来解释计算 <code>Table</code> 的逻辑和优化查询计划。 这是通过 <code>Table.explain()</code> 方法或者 <code>StatementSet.explain()</code> 方法来完成的。<code>Table.explain()</code> 返回一个 Table 的计划。<code>StatementSet.explain()</code> 返回多 sink 计划的结果。它返回一个描述三种计划的字符串：</p>
<ol>
<li>关系查询的抽象语法树（the Abstract Syntax Tree），即未优化的逻辑查询计划，</li>
<li>优化的逻辑查询计划，以及</li>
<li>物理执行计划。</li>
</ol>
<p>可以用 <code>TableEnvironment.explainSql()</code> 方法和 <code>TableEnvironment.executeSql()</code> 方法支持执行一个 <code>EXPLAIN</code> 语句获取逻辑和优化查询计划，请参阅 <a href="https://ci.apache.org/projects/flink/flink-docs-release-1.12/zh/dev/table/sql/explain.html">EXPLAIN</a> 页面.</p>
<p>以下代码展示了一个示例以及对给定 <code>Table</code> 使用 <code>Table.explain()</code> 方法的相应输出：</p>
<pre><code class="language-java">StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
StreamTableEnvironment tEnv = StreamTableEnvironment.create(env);

DataStream&lt;Tuple2&lt;Integer, String&gt;&gt; stream1 = env.fromElements(new Tuple2&lt;&gt;(1, &quot;hello&quot;));
DataStream&lt;Tuple2&lt;Integer, String&gt;&gt; stream2 = env.fromElements(new Tuple2&lt;&gt;(1, &quot;hello&quot;));

// explain Table API
Table table1 = tEnv.fromDataStream(stream1, $(&quot;count&quot;), $(&quot;word&quot;));
Table table2 = tEnv.fromDataStream(stream2, $(&quot;count&quot;), $(&quot;word&quot;));
Table table = table1
  .where($(&quot;word&quot;).like(&quot;F%&quot;))
  .unionAll(table2);
System.out.println(table.explain());
</code></pre>
<p>上述例子的结果是：</p>
<pre><code class="language-java">== Abstract Syntax Tree ==
LogicalUnion(all=[true])
  LogicalFilter(condition=[LIKE($1, _UTF-16LE'F%')])
    FlinkLogicalDataStreamScan(id=[1], fields=[count, word])
  FlinkLogicalDataStreamScan(id=[2], fields=[count, word])

== Optimized Logical Plan ==
DataStreamUnion(all=[true], union all=[count, word])
  DataStreamCalc(select=[count, word], where=[LIKE(word, _UTF-16LE'F%')])
    DataStreamScan(id=[1], fields=[count, word])
  DataStreamScan(id=[2], fields=[count, word])

== Physical Execution Plan ==
Stage 1 : Data Source
	content : collect elements with CollectionInputFormat

Stage 2 : Data Source
	content : collect elements with CollectionInputFormat

	Stage 3 : Operator
		content : from: (count, word)
		ship_strategy : REBALANCE

		Stage 4 : Operator
			content : where: (LIKE(word, _UTF-16LE'F%')), select: (count, word)
			ship_strategy : FORWARD

			Stage 5 : Operator
				content : from: (count, word)
				ship_strategy : REBALANCE
</code></pre>
<p>以下代码展示了一个示例以及使用 <code>StatementSet.explain()</code> 的多 sink 计划的相应输出：</p>
<pre><code class="language-java">EnvironmentSettings settings = EnvironmentSettings.newInstance().useBlinkPlanner().inStreamingMode().build();
TableEnvironment tEnv = TableEnvironment.create(settings);

final Schema schema = new Schema()
    .field(&quot;count&quot;, DataTypes.INT())
    .field(&quot;word&quot;, DataTypes.STRING());

tEnv.connect(new FileSystem().path(&quot;/source/path1&quot;))
    .withFormat(new Csv().deriveSchema())
    .withSchema(schema)
    .createTemporaryTable(&quot;MySource1&quot;);
tEnv.connect(new FileSystem().path(&quot;/source/path2&quot;))
    .withFormat(new Csv().deriveSchema())
    .withSchema(schema)
    .createTemporaryTable(&quot;MySource2&quot;);
tEnv.connect(new FileSystem().path(&quot;/sink/path1&quot;))
    .withFormat(new Csv().deriveSchema())
    .withSchema(schema)
    .createTemporaryTable(&quot;MySink1&quot;);
tEnv.connect(new FileSystem().path(&quot;/sink/path2&quot;))
    .withFormat(new Csv().deriveSchema())
    .withSchema(schema)
    .createTemporaryTable(&quot;MySink2&quot;);

StatementSet stmtSet = tEnv.createStatementSet();

Table table1 = tEnv.from(&quot;MySource1&quot;).where($(&quot;word&quot;).like(&quot;F%&quot;));
stmtSet.addInsert(&quot;MySink1&quot;, table1);

Table table2 = table1.unionAll(tEnv.from(&quot;MySource2&quot;));
stmtSet.addInsert(&quot;MySink2&quot;, table2);

String explanation = stmtSet.explain();
System.out.println(explanation);
</code></pre>
<p>多 sink 计划的结果是：</p>
<pre><code class="language-java">== Abstract Syntax Tree ==
LogicalLegacySink(name=[MySink1], fields=[count, word])
+- LogicalFilter(condition=[LIKE($1, _UTF-16LE'F%')])
   +- LogicalTableScan(table=[[default_catalog, default_database, MySource1, source: [CsvTableSource(read fields: count, word)]]])

LogicalLegacySink(name=[MySink2], fields=[count, word])
+- LogicalUnion(all=[true])
   :- LogicalFilter(condition=[LIKE($1, _UTF-16LE'F%')])
   :  +- LogicalTableScan(table=[[default_catalog, default_database, MySource1, source: [CsvTableSource(read fields: count, word)]]])
   +- LogicalTableScan(table=[[default_catalog, default_database, MySource2, source: [CsvTableSource(read fields: count, word)]]])

== Optimized Logical Plan ==
Calc(select=[count, word], where=[LIKE(word, _UTF-16LE'F%')], reuse_id=[1])
+- TableSourceScan(table=[[default_catalog, default_database, MySource1, source: [CsvTableSource(read fields: count, word)]]], fields=[count, word])

LegacySink(name=[MySink1], fields=[count, word])
+- Reused(reference_id=[1])

LegacySink(name=[MySink2], fields=[count, word])
+- Union(all=[true], union=[count, word])
   :- Reused(reference_id=[1])
   +- TableSourceScan(table=[[default_catalog, default_database, MySource2, source: [CsvTableSource(read fields: count, word)]]], fields=[count, word])

== Physical Execution Plan ==
Stage 1 : Data Source
	content : collect elements with CollectionInputFormat

	Stage 2 : Operator
		content : CsvTableSource(read fields: count, word)
		ship_strategy : REBALANCE

		Stage 3 : Operator
			content : SourceConversion(table:Buffer(default_catalog, default_database, MySource1, source: [CsvTableSource(read fields: count, word)]), fields:(count, word))
			ship_strategy : FORWARD

			Stage 4 : Operator
				content : Calc(where: (word LIKE _UTF-16LE'F%'), select: (count, word))
				ship_strategy : FORWARD

				Stage 5 : Operator
					content : SinkConversionToRow
					ship_strategy : FORWARD

					Stage 6 : Operator
						content : Map
						ship_strategy : FORWARD

Stage 8 : Data Source
	content : collect elements with CollectionInputFormat

	Stage 9 : Operator
		content : CsvTableSource(read fields: count, word)
		ship_strategy : REBALANCE

		Stage 10 : Operator
			content : SourceConversion(table:Buffer(default_catalog, default_database, MySource2, source: [CsvTableSource(read fields: count, word)]), fields:(count, word))
			ship_strategy : FORWARD

			Stage 12 : Operator
				content : SinkConversionToRow
				ship_strategy : FORWARD

				Stage 13 : Operator
					content : Map
					ship_strategy : FORWARD

					Stage 7 : Data Sink
						content : Sink: CsvTableSink(count, word)
						ship_strategy : FORWARD

						Stage 14 : Data Sink
							content : Sink: CsvTableSink(count, word)
							ship_strategy : FORWARD
</code></pre>
<h3 id="动态表">动态表</h3>
<p>如果流中的数据类型是 case class 可以直接根据 case class 的结构生成 table</p>
<pre><code class="language-java">tableEnv.fromDataStream(dataStream)
</code></pre>
<p>或者根据字段顺序单独命名</p>
<pre><code class="language-java">tableEnv.fromDataStream(dataStream,’id,’timestamp .......)
</code></pre>
<p>最后的动态表可以转换为流进行输出</p>
<pre><code class="language-java">table.toAppendStream[(String,String)]
</code></pre>
<h3 id="字段">字段</h3>
<p>用一个单引放到字段前面来标识字段名, 如 ‘name , ‘id ,’amount 等</p>
<h2 id="tableapi-的窗口聚合操作">TableAPI 的窗口聚合操作</h2>
<h3 id="通过一个例子了解-tableapi">通过一个例子了解 TableAPI</h3>
<pre><code class="language-java">package myflink.sql;
 
import org.apache.flink.api.java.DataSet;
import org.apache.flink.api.java.ExecutionEnvironment;
import org.apache.flink.table.api.Table;
import org.apache.flink.table.api.java.BatchTableEnvironment;
 
 
public class WordCountSQL {
 
    public static void main(String[] args) throws Exception {
 
        ExecutionEnvironment env = ExecutionEnvironment.getExecutionEnvironment();
        BatchTableEnvironment tEnv = BatchTableEnvironment.getTableEnvironment(env);
 
        DataSet&lt;WC&gt; input = env.fromElements(
                WC.of(&quot;hello&quot;, 1),
                WC.of(&quot;hqs&quot;, 1),
                WC.of(&quot;world&quot;, 1),
                WC.of(&quot;hello&quot;, 1)
        );
        //注册数据集
        tEnv.registerDataSet(&quot;WordCount&quot;, input, &quot;word, frequency&quot;);
 
        //执行SQL，并结果集做为一个新表
        Table table = tEnv.sqlQuery(&quot;SELECT word, SUM(frequency) as frequency FROM WordCount GROUP BY word&quot;);
 
        DataSet&lt;WC&gt; result = tEnv.toDataSet(table, WC.class);
 
        result.print();
 
    }
 
    public static class WC {
        public String word; //hello
        public long frequency;
 
        //创建构造方法，让flink进行实例化
        public WC() {}
 
        public static WC of(String word, long frequency) {
            WC wc = new WC();
            wc.word = word;
            wc.frequency = frequency;
            return wc;
        }
 
        @Override
        public String toString() {
            return &quot;WC &quot; + word + &quot; &quot; + frequency;
        }
    }
 
}
</code></pre>
<h3 id="关于-group-by">关于 group by</h3>
<ol>
<li>如果了使用 groupby， table 转换为流的时候只能用 toRetractDstream</li>
</ol>
<pre><code class="language-java">DataStream&lt;Tuple2&lt;Boolean, Tuple2&lt;Integer, Integer&gt;&gt;&gt; joinStream = bsTableEnv.toRetractStream(queryTable, Types.TUPLE(Types.INT, Types.INT));
</code></pre>
<ol start="2">
<li>
<p>toRetractDstream 得到的第一个 boolean 型字段标识 true 就是最新的数据<br>
(Insert)， false 表示过期老数据(Delete)</p>
</li>
<li>
<p>如果使用的 api 包括时间窗口， 那么窗口的字段必须出现在 groupBy 中。</p>
</li>
</ol>
<pre><code class="language-java">Table resultTable= dataTable
   .window( Tumble over 10.seconds on 'ts as 'tw )
   .groupBy('id, 'tw)
   .select('id, 'id.count)
</code></pre>
<h3 id="关于时间窗口">关于时间窗口</h3>
<ol>
<li>用到时间窗口， 必须提前声明时间字段， 如果是 processTime 直接在创建动<br>
态表时进行追加就可以。</li>
</ol>
<pre><code class="language-java">Table dataTable= tableEnv.fromDataStream(dataStream, 'id,'temperature, 'ps.proctime)
</code></pre>
<ol>
<li>如果是 EventTime 要在创建动态表时声明</li>
</ol>
<pre><code class="language-java">Table dataTable= tableEnv.fromDataStream(dataStream, 'id,'temperature, 'ts.rowtime)
</code></pre>
<ol>
<li>滚动窗口可以使用 Tumble over 10000.millis on 来表示</li>
</ol>
<pre><code class="language-java">Table resultTable = dataTable
    .window( Tumble over 10.seconds on 'ts as 'tw )
    .groupBy('id, 'tw)
    .select('id, 'id.count)
</code></pre>
<h2 id="sql-如何编写">SQL 如何编写</h2>
<p>从一个txt文件中读取数据，txt文件中包含id, 人字, 书名，价格信息。然后将数据注册成一个表，然后将这个表的结果进行统计，按人名统计出来这个人买书所花费的钱，将结果sink到一个文件中。上代码。</p>
<pre><code class="language-java">package myflink.sql;
 
import org.apache.flink.api.common.functions.MapFunction;
import org.apache.flink.api.common.typeinfo.TypeInformation;
import org.apache.flink.api.java.DataSet;
import org.apache.flink.api.java.ExecutionEnvironment;
import org.apache.flink.api.java.operators.DataSource;
import org.apache.flink.api.java.tuple.Tuple2;
import org.apache.flink.table.api.Table;
import org.apache.flink.table.api.Types;
import org.apache.flink.table.api.java.BatchTableEnvironment;
import org.apache.flink.table.sinks.CsvTableSink;
import org.apache.flink.table.sinks.TableSink;
 
public class SQLFromFile {
 
    public static void main(String[] args) throws Exception {
        ExecutionEnvironment env = ExecutionEnvironment.getExecutionEnvironment();
 
        BatchTableEnvironment tableEnv = BatchTableEnvironment.getTableEnvironment(env);
 
        env.setParallelism(1);
        //读取文件
        DataSource&lt;String&gt; input = env.readTextFile(&quot;test.txt&quot;);
        //将读取到的文件进行输出
        input.print();
        //转换为DataSet
        DataSet&lt;Orders&gt; inputDataSet = input.map(new MapFunction&lt;String, Orders&gt;() {
            @Override
            public Orders map(String s) throws Exception {
                String[] splits = s.split(&quot; &quot;);
                return Orders.of(Integer.valueOf(splits[0]), String.valueOf(splits[1]), String.valueOf(splits[2]), Double.valueOf(splits[3]));
            }
        });
        //转换为table
        Table order = tableEnv.fromDataSet(inputDataSet);
        //注册Orders表名
        tableEnv.registerTable(&quot;Orders&quot;, order);
        Table nameResult = tableEnv.scan(&quot;Orders&quot;).select(&quot;name&quot;);
        //输出一下表
        nameResult.printSchema();
 
        //执行一下查询
        Table sqlQueryResult = tableEnv.sqlQuery(&quot;select name, sum(price) as total from Orders group by name order by total desc&quot;);
        //查询结果转换为DataSet
        DataSet&lt;Result&gt; result = tableEnv.toDataSet(sqlQueryResult, Result.class);
        result.print();
 
        //以tuple的方式进行输出
        result.map(new MapFunction&lt;Result, Tuple2&lt;String, Double&gt;&gt;() {
            @Override
            public Tuple2&lt;String, Double&gt; map(Result result) throws Exception {
                String name = result.name;
                Double total = result.total;
                return Tuple2.of(name, total);
            }
        }).print();
 
        TableSink sink  = new CsvTableSink(&quot;SQLText.txt&quot;, &quot; | &quot;);
 
        //设置字段名
        String[] filedNames = {&quot;name&quot;, &quot;total&quot;};
        //设置字段类型
        TypeInformation[] filedTypes = {Types.STRING(), Types.DOUBLE()};
 
        tableEnv.registerTableSink(&quot;SQLTEXT&quot;, filedNames, filedTypes, sink);
 
        sqlQueryResult.insertInto(&quot;SQLTEXT&quot;);
 
        env.execute();
 
    }
 
    public static class Orders {
        public Integer id;
        public String name;
        public String book;
        public Double price;
 
        public Orders() {
            super();
        }
 
        public static Orders of(Integer id, String name, String book, Double price) {
            Orders orders = new Orders();
            orders.id = id;
            orders.name = name;
            orders.book = book;
            orders.price = price;
            return orders;
        }
    }
 
    public static class Result {
        public String name;
        public Double total;
 
        public Result() {
            super();
        }
 
        public static Result of(String name, Double total) {
            Result result = new Result();
            result.name = name;
            result.total = total;
            return result;
        }
    }
 
}
</code></pre>
<h2 id="操作">操作</h2>
<p>Table API支持下面的操作，请注意并不是所有的操作都同时支持批程序和流程序，不支持的会被响应的标记出来。</p>
<h3 id="scan-projection-and-filter">Scan, Projection, and Filter</h3>
<table>
<thead>
<tr>
<th>Operators</th>
<th style="text-align:left">Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>From</strong> <code>Batch</code> <code>Streaming</code></td>
<td style="text-align:left">与SQL查询中的FROM子句类似。 执行已注册表的扫描。<br/> <code>Table orders = tableEnv.from(&quot;Orders&quot;);</code></td>
</tr>
<tr>
<td><strong>Select</strong> <code>Batch</code> <code>Streaming</code></td>
<td style="text-align:left">与SQL SELECT语句类似。 执行选择操作。<br/> <code>Table orders= tableEnv.from(&quot;Orders&quot;)</code><br/> <code>Table result = orders.select($(&quot;a&quot;), $(&quot;c&quot;).as(&quot;d&quot;))</code> <br/>可以使用星号（**<em>）作为通配符，选择表格中的所有列。<br/> <code>Table orders = tableEnv.from(&quot;Orders&quot;)</code><br/> ``Table result = orders.select($(&quot;</em>&quot;))`</td>
</tr>
<tr>
<td><strong>As</strong> <code>Batch</code> <code>Streaming</code></td>
<td style="text-align:left">重命名字段。 <code>Table orders = tableEnv.from(&quot;Orders&quot;).as('x, 'y, 'z, 't)</code></td>
</tr>
<tr>
<td><strong>Where / Filter</strong> <code>Batch</code> <code>Streaming</code></td>
<td style="text-align:left">与SQL WHERE子句类似。 过滤掉未通过过滤谓词的行。<br/> <code>Table orders = tableEnv.from(&quot;Orders&quot;)</code> <br/><code>Table result = orders.filter($(&quot;a&quot;).mod(2).isEqual(0))</code><br /> or<br /><code>Table orders= tableEnv.from(&quot;Orders&quot;)</code> <br/><code>Table result = orders.where($(&quot;b&quot;).isEqual(&quot;red&quot;))</code></td>
</tr>
</tbody>
</table>
<h3 id="column-operations">Column Operations</h3>
<table>
<thead>
<tr>
<th>Operators</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>AddColumns</strong> <code>Batch</code> <code>Streaming</code></td>
<td>执行字段添加操作。如果添加的字段已经存在，它将引发异常。<br/> <code>Table orders = tableEnv.from(&quot;Orders&quot;);</code> <br/><code>Table result = orders.addColumns(concat($(&quot;c&quot;), &quot;sunny&quot;))</code></td>
</tr>
<tr>
<td><strong>AddOrReplaceColumns</strong> <code>Batch</code> <code>Streaming</code></td>
<td>执行字段添加操作。如果添加列名称与现有列名称相同，则现有字段将被替换。此外，如果添加的字段具有重复的字段名称，则使用最后一个。<br/> <code>Table orders = tableEnv.from(&quot;Orders&quot;);</code> <br/><code>Table result = orders.addOrReplaceColumns(concat($(&quot;c&quot;), &quot;sunny&quot;).as(&quot;desc&quot;))</code></td>
</tr>
<tr>
<td><strong>DropColumns</strong> <code>Batch</code> <code>Streaming</code></td>
<td>执行字段删除操作。字段表达式应该是字段引用表达式，并且只能删除现有字段。<br/> <code>Table orders = tableEnv.from(&quot;Orders&quot;);</code> <br/><code>Table result = orders.dropColumns($(&quot;b&quot;), $(&quot;c&quot;))</code></td>
</tr>
<tr>
<td><strong>RenameColumns</strong> <code>Batch</code> <code>Streaming</code></td>
<td>执行字段重命名操作。字段表达式应该是别名表达式，并且只有现有字段可以重命名。<br/> <code>Table orders = tableEnv.from(&quot;Orders&quot;);</code> <br/><code>Table result = orders.renameColumns($(&quot;b&quot;).as(&quot;b2&quot;), $(&quot;c&quot;).as(&quot;c2&quot;))</code></td>
</tr>
</tbody>
</table>
<h3 id="aggregations">Aggregations</h3>
<table>
<thead>
<tr>
<th>Operators</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>GroupBy聚合</strong> <code>Batch</code> <code>Streaming</code> <code>Result Updating</code></td>
<td>类似于SQL GROUP BY子句。使用以下正在运行的聚合运算符将分组键上的行分组，以逐行聚合行。<br/> <code>Table orders: Table = tableEnv.scan(&quot;Orders&quot;)</code><br/> <code>Table result = orders.groupBy($(&quot;a&quot;)).select($(&quot;a&quot;), $(&quot;b&quot;).sum().as(&quot;d&quot;))</code> <br/>注意：对于流式查询，计算查询结果所需的状态可能会无限增长，具体取决于聚合类型和不同分组键的数量。 请提供具有有效保留间隔的查询配置，以防止状态过大。 请参阅<a href="https://blog.csdn.net/lp284558195/article/details/104609739">查询配置</a></td>
</tr>
<tr>
<td><strong>GroupBy窗口聚合</strong> <code>Batch</code> <code>Streaming</code></td>
<td>在组窗口可能的一个或多个分组key上对表进行分组和聚集。<br/> <code>val orders: Table = tableEnv.scan(&quot;Orders&quot;)</code> <br/><code>Table result = orders</code><br />    <code>.window(Tumble.over(lit(5).minutes()).on($(&quot;rowtime&quot;)).as(&quot;w&quot;)) // define window</code><br />    <code>.groupBy($(&quot;a&quot;), $(&quot;w&quot;)) // group by key and window</code><br />  <code>// access window properties and aggregate</code><br />   <code>.select(</code><br />       <code>$(&quot;a&quot;),</code><br />       <code>$(&quot;w&quot;).start(),</code><br />       <code>$(&quot;w&quot;).end(),</code><br />       <code>$(&quot;w&quot;).rowtime(),</code><br />       <code>$(&quot;b&quot;).sum().as(&quot;d&quot;)</code><br />  <code>);</code></td>
</tr>
<tr>
<td><strong>Over 窗口聚合</strong> <code>Batch</code> <code>Streaming</code></td>
<td>类似于SQL OVER子句。基于前一行和后一行的窗口（范围），为每一行计算窗口聚合。<br/> <code>Table orders = tableEnv.from(&quot;Orders&quot;);</code><br /> <code>Table result = orders // define window</code><br />   <code>.window(</code><br />       <code>Over</code><br />         <code>.partitionBy($(&quot;a&quot;))</code><br />         <code>.orderBy($(&quot;rowtime&quot;))</code><br />         <code>.preceding(UNBOUNDED_RANGE)</code><br />         <code>.following(CURRENT_RANGE)</code><br />         <code>.as(&quot;w&quot;)) // sliding aggregate</code><br />    <code>.select(</code><br />         <code>$(&quot;a&quot;),</code><br />         <code>$(&quot;b&quot;).avg().over($(&quot;w&quot;)),</code><br />         <code>$(&quot;b&quot;).max().over($(&quot;w&quot;)),</code><br />         <code>$(&quot;b&quot;).min().over($(&quot;w&quot;))</code><br />   <code>);</code><br />**Note:**必须在同一窗口（即相同的分区，排序和范围）上定义所有聚合。当前，仅支持PRECEDING（无边界和有界）到CURRENT ROW范围的窗口。目前尚不支持带有FOLLOWING的范围。必须在单个<a href="http://www.lllpan.top/article/55">时间属性</a>上指定ORDER BY。</td>
</tr>
<tr>
<td><strong>Distinct聚合</strong> <code>Batch</code> <code>Streaming</code> <code>Result Updating</code></td>
<td>类似于SQL DISTINCT AGGREGATION子句，例如COUNT（DISTINCT a）。不同的聚合声明聚合函数（内置或用户定义的）仅应用于不同的输入值。Distinct聚合可以用于GroupBy聚合，GroupBy窗口聚合和Over窗口聚合。<br/><code>Table orders = tableEnv.from(&quot;Orders&quot;);</code><br /> <code>// Distinct aggregation on group by</code><br /> <code>Table groupByDistinctResult = orders</code><br />   <code>.groupBy($(&quot;a&quot;))</code><br />   <code>.select($(&quot;a&quot;), $(&quot;b&quot;).sum().distinct().as(&quot;d&quot;));</code><br /><code>// Distinct aggregation on time window group by</code><br /> <code>Table groupByWindowDistinctResult = orders</code><br />   <code>.window(Tumble</code><br />          <code>.over(lit(5).minutes())</code><br />          <code>.on($(&quot;rowtime&quot;))</code><br />          <code>.as(&quot;w&quot;)</code><br />   <code>)</code><br />    <code>.groupBy($(&quot;a&quot;), $(&quot;w&quot;))</code><br />    <code>.select($(&quot;a&quot;), $(&quot;b&quot;).sum().distinct().as(&quot;d&quot;));</code><br /><code>// Distinct aggregation on over window</code><br /> <code>Table result = orders</code><br />     <code>.window(Over</code><br />         <code>.partitionBy($(&quot;a&quot;))</code><br />         <code>.orderBy($(&quot;rowtime&quot;))</code><br />         <code>.preceding(UNBOUNDED_RANGE)</code><br />         <code>.as(&quot;w&quot;))</code><br />    <code>.select(</code><br />         <code>$(&quot;a&quot;), $(&quot;b&quot;).avg().distinct().over($(&quot;w&quot;)),</code><br />         <code>$(&quot;b&quot;).max().over($(&quot;w&quot;)),</code><br />         <code>$(&quot;b&quot;).min().over($(&quot;w&quot;))</code><br />    <code>);</code><br />用户定义的聚合函数也可以与DISTINCT修饰符一起使用。要仅针对不同值计算聚合结果，只需向聚合函数添加distinct修饰符即可。 <br /><code>Table orders = tEnv.from(&quot;Orders&quot;);</code><br /> <code>// Use distinct aggregation for user-defined aggregate functions</code><br /><code>tEnv.registerFunction(&quot;myUdagg&quot;, new MyUdagg());</code><br /> <code>orders.groupBy(&quot;users&quot;)</code><br />     <code>.select(</code><br />        <code>$(&quot;users&quot;),</code><br />        <code>call(&quot;myUdagg&quot;, $(&quot;points&quot;)).distinct().as(&quot;myDistinctResult&quot;)</code><br />  <code>);</code><br /> **Note:**对于流式查询，计算查询结果所需的状态可能会无限增长，具体取决于聚合类型和不同分组键的数量。 请提供具有有效保留间隔的查询配置，以防止状态过大。 请参阅<a href="https://blog.csdn.net/lp284558195/article/details/104609739">查询配置</a></td>
</tr>
<tr>
<td><strong>Distinct</strong> <code>Batch</code> <code>Streaming</code> <code>Result Updating</code></td>
<td>类似于SQL DISTINCT子句。返回具有不同值组合的记录。<br /> <code>Table orders= tableEnv.from(&quot;Orders&quot;)</code> <br /><code>Table result = orders.distinct()</code><br /> **Note:**对于流查询，根据查询字段的数量，计算查询结果所需的状态可能会无限增长。请提供具有有效保留间隔的查询配置，以防止出现过多的状态。如果启用了状态清除功能，那么distinct必须发出消息，以防止下游运算符过早地退出状态，这会导致distinct包含结果更新。有关详细信息，请参阅<a href="https://blog.csdn.net/lp284558195/article/details/104609739">查询配置</a></td>
</tr>
</tbody>
</table>
<h3 id="joins">Joins</h3>
<table>
<thead>
<tr>
<th>Operators</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Inner Join</strong> <code>Batch</code> <code>Streaming</code></td>
<td>类似于SQL JOIN子句。连接两个表。两个表必须具有不同的字段名称，并且至少一个相等的联接谓词必须通过联接运算符或使用where或filter运算符进行定义。<br /><code>Table left = tableEnv.fromDataSet(ds1, &quot;a, b, c&quot;);</code><br /><code>Table right = tableEnv.fromDataSet(ds2, &quot;d, e, f&quot;);</code><br /><code>Table result = left.join(right)</code><br />    <code>.where($(&quot;a&quot;).isEqual($(&quot;d&quot;)))</code><br />    <code>.select($(&quot;a&quot;), $(&quot;b&quot;), $(&quot;e&quot;));</code><br />Note:** 对于流查询，根据不同输入行的数量，计算查询结果所需的状态可能会无限增长。请提供具有有效保留间隔的查询配置，以防止出现过多的状态。有关详细信息，请参阅<a href="https://blog.csdn.net/lp284558195/article/details/104609739">查询配置</a></td>
</tr>
<tr>
<td><strong>Outer Join</strong> <code>Batch</code> <code>Streaming</code> <code>Result Updating</code></td>
<td>类似于SQL LEFT / RIGHT / FULL OUTER JOIN子句。连接两个表。两个表必须具有不同的字段名称，并且必须至少定义一个相等联接谓词。 <br /><code>Table left = tableEnv.fromDataSet(ds1, &quot;a, b, c&quot;);</code><br /><code>Table right = tableEnv.fromDataSet(ds2, &quot;d, e, f&quot;);</code><br /><code>Table leftOuterResult = left.leftOuterJoin(right, $(&quot;a&quot;).isEqual($(&quot;d&quot;)))</code><br />                           <code>.select($(&quot;a&quot;), $(&quot;b&quot;), $(&quot;e&quot;));</code><br /><code>Table rightOuterResult = left.rightOuterJoin(right, $(&quot;a&quot;).isEqual($(&quot;d&quot;)))</code><br />                         <code>.select($(&quot;a&quot;), $(&quot;b&quot;), $(&quot;e&quot;));</code><br /><code>Table fullOuterResult = left.fullOuterJoin(right, $(&quot;a&quot;).isEqual($(&quot;d&quot;)))</code><br />                           <code>.select($(&quot;a&quot;), $(&quot;b&quot;), $(&quot;e&quot;));</code><br /><strong>Note:</strong> 对于流查询，根据不同输入行的数量，计算查询结果所需的状态可能会无限增长。请提供具有有效保留间隔的查询配置，以防止出现过多的状态。有关详细信息，请参阅<a href="https://blog.csdn.net/lp284558195/article/details/104609739">查询配置</a></td>
</tr>
<tr>
<td><strong>Inner/Outer Interval Join</strong><code>Batch</code> <code>Streaming</code></td>
<td><strong>Note:</strong> 时间窗口连接是可以以流方式处理的常规子集连接。 时间窗口连接需要至少一个等连接和一个限制双方时间的连接条件。可以通过两个适当的范围谓词（&lt;，&lt;=，&gt; =，&gt;）或比较两个输入表的相同类型的时间属性（即处理时间或事件时间）的单个相等谓词来定义这种条件。 例如，以下是有效的窗口连接条件：<br /> <code>'ltime === 'rtime</code><br /> <code>'ltime &gt;= 'rtime &amp;&amp; 'ltime &lt; 'rtime + 10.minutes</code>  <br /><code>Table left = tableEnv.fromDataSet(ds1, $(&quot;a&quot;), $(&quot;b&quot;), $(&quot;c&quot;), $(&quot;ltime&quot;).rowtime());</code><br /><code>Table right = tableEnv.fromDataSet(ds2, $(&quot;d&quot;), $(&quot;e&quot;), $(&quot;f&quot;), $(&quot;rtime&quot;).rowtime()));</code><br /><code>Table result = left.join(right)</code><br />   <code>.where(</code><br />    <code>and(</code><br />         <code>$(&quot;a&quot;).isEqual($(&quot;d&quot;)),</code><br />         <code>$(&quot;ltime&quot;).isGreaterOrEqual($(&quot;rtime&quot;).minus(lit(5).minutes())),</code><br />         <code>$(&quot;ltime&quot;).isLess($(&quot;rtime&quot;).plus(lit(10).minutes()))</code><br />    <code>))</code><br /> <code>.select($(&quot;a&quot;), $(&quot;b&quot;), $(&quot;e&quot;), $(&quot;ltime&quot;));</code></td>
</tr>
<tr>
<td><strong>Inner Join with Table Function (UDTF)</strong> <code>Batch</code> <code>Streaming</code></td>
<td>使用表函数的结果与表连接。左(外)表的每一行都与表函数的相应调用产生的所有行连接在一起。如果其表函数调用返回空结果，则删除左(外)表的一行。<br /> <code>// register User-Defined Table Function</code><br /><code>TableFunction&lt;String&gt; split = new MySplitUDTF();</code><br /><code>tableEnv.registerFunction(&quot;split&quot;, split);</code><br /> <code>// join</code><br /><code>Table orders = tableEnv.from(&quot;Orders&quot;);</code><br /><code>Table result = orders</code><br />     <code>.joinLateral(call(&quot;split&quot;, $(&quot;c&quot;)).as(&quot;s&quot;, &quot;t&quot;, &quot;v&quot;))</code><br />     <code>.select($(&quot;a&quot;), $(&quot;b&quot;), $(&quot;s&quot;), $(&quot;t&quot;), $(&quot;v&quot;));</code></td>
</tr>
<tr>
<td><strong>Left Outer Join with Table Function (UDTF)</strong> <code>Batch</code> <code>Streaming</code></td>
<td>使用表函数的结果连接表。 左(外)表的每一行与表函数的相应调用产生的所有行连接。 如果表函数调用返回空结果，则保留相应的外部行，并使用空值填充结果。 **Note:**目前，表函数的左外连接只能为空或为true。<br /><code>// register User-Defined Table Function</code><br /><code>TableFunction&lt;String&gt; split = new MySplitUDTF();</code><br /><code>tableEnv.registerFunction(&quot;split&quot;, split);</code><br /><code>// join</code><br /><code>Table orders = tableEnv.from(&quot;Orders&quot;);</code><br /><code>Table result = orders</code><br />     <code>.leftOuterJoinLateral(call(&quot;split&quot;, $(&quot;c&quot;)).as(&quot;s&quot;, &quot;t&quot;, &quot;v&quot;))</code><br />     <code>.select($(&quot;a&quot;), $(&quot;b&quot;), $(&quot;s&quot;), $(&quot;t&quot;), $(&quot;v&quot;));</code></td>
</tr>
<tr>
<td><strong>Join with Temporal Table</strong> <code>Streaming</code></td>
<td><a href="http://www.lllpan.top/article/54">时态表</a>是跟踪其随时间变化的表。 时态表功能提供对特定时间点时态表状态的访问。使用时态表函数联接表的语法与使用表函数进行内部联接的语法相同。 **Note:**当前仅支持使用临时表的内部联接。 <br /><code>Table ratesHistory = tableEnv.from(&quot;RatesHistory&quot;);</code><br /><code>// register temporal table function with a time attribute and primary key</code><br /><code>TemporalTableFunction rates = ratesHistory.createTemporalTableFunction(</code><br />   <code>&quot;r_proctime&quot;,</code><br />   <code>&quot;r_currency&quot;);</code><br /><code>tableEnv.registerFunction(&quot;rates&quot;, rates);</code><br /><code>// join with &quot;Orders&quot; based on the time attribute and key</code><br /><code>Table orders = tableEnv.from(&quot;Orders&quot;);</code><br /><code>Table result = orders</code><br />    <code>.joinLateral(call(&quot;rates&quot;, $(&quot;o_proctime&quot;)), $(&quot;o_currency&quot;).isEqual($(&quot;r_currency&quot;)))</code></td>
</tr>
</tbody>
</table>
<h3 id="集合算子">集合算子</h3>
<table>
<thead>
<tr>
<th>Operators</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Union</strong> <code>Batch</code></td>
<td>类似于SQL UNION子句。合并两个已删除重复记录的表，两个表必须具有相同的字段类型。<br /> <code>Table left = ds1.toTable(tableEnv, 'a, 'b, 'c)</code><br /> <code>Table right = ds2.toTable(tableEnv, 'a, 'b, 'c)</code><br /> <code>Table result = left.union(right)</code></td>
</tr>
<tr>
<td><strong>UnionAll</strong> <code>Batch</code> <code>Streaming</code></td>
<td>类似于SQL UNION ALL子句。合并两个表，两个表必须具有相同的字段类型。<br /> <code>Table left = ds1.toTable(tableEnv, 'a, 'b, 'c)</code><br /> <code>Table right = ds2.toTable(tableEnv, 'a, 'b, 'c)</code><br /> <code>Table result = left.unionAll(right)</code></td>
</tr>
<tr>
<td><strong>Intersect</strong> <code>Batch</code></td>
<td>类似于SQL INTERSECT子句。相交返回两个表中都存在的记录。如果一个记录在一个或两个表中多次出现，则仅返回一次，即结果表中没有重复的记录。两个表必须具有相同的字段类型。<br /> <code>Table left = ds1.toTable(tableEnv, 'a, 'b, 'c)</code><br /> <code>Table right = ds2.toTable(tableEnv, 'a, 'b, 'c)</code><br /><code>Table result = left.intersect(right)</code></td>
</tr>
<tr>
<td><strong>IntersectAll</strong> <code>Batch</code></td>
<td>类似于SQL INTERSECT ALL子句。IntersectAll返回两个表中都存在的记录。如果一个记录在两个表中都存在一次以上，则返回的次数与两个表中存在的次数相同，即，结果表可能具有重复的记录。两个表必须具有相同的字段类型。 <br /> <code>Table left = ds1.toTable(tableEnv, 'a, 'b, 'c)</code><br /> <code>Table right = ds2.toTable(tableEnv, 'a, 'b, 'c)</code><br /> <code>Table result = left.intersectAll(right)</code></td>
</tr>
<tr>
<td><strong>Minus</strong> <code>Batch</code></td>
<td>类似于SQL EXCEPT子句。Minus返回左表中右表中不存在的记录。左表中的重复记录只返回一次，即删除重复项。 两个表必须具有相同的字段类型。 <br /> <code>Table left = ds1.toTable(tableEnv, 'a, 'b, 'c)</code><br /> <code>Table right = ds2.toTable(tableEnv, 'a, 'b, 'c)</code><br /> <code>Table result = left.minus(right)</code></td>
</tr>
<tr>
<td><strong>MinusAll</strong> <code>Batch</code></td>
<td>类似于SQL EXCEPT ALL子句。 MinusAll返回右表中不存在的记录。 在左表中出现n次并在右表中出现m次的记录返回（n-m）次，即，删除右表中出现的重复数。 两个表必须具有相同的字段类型。 <br /> <code>Table left = ds1.toTable(tableEnv, 'a, 'b, 'c)</code><br /> <code>Table right = ds2.toTable(tableEnv, 'a, 'b, 'c)</code><br /> <code>Table result = left.minusAll(right)</code></td>
</tr>
<tr>
<td><strong>In</strong> <code>Batch</code> <code>Streaming</code></td>
<td>与SQL IN子句类似。 如果表达式存在于给定的表子查询中，则返回true。 子查询表必须包含一列。 此列必须与表达式具有相同的数据类型。<br /> <code>Table left = ds1.toTable(tableEnv, &quot;a, b, c&quot;);</code><br /> <code>Table right = ds2.toTable(tableEnv, &quot;a&quot;);</code><br /> <code>Table result = left.select($(&quot;a&quot;), $(&quot;b&quot;), $(&quot;c&quot;)).where($(&quot;a&quot;).in(right));</code><br /><strong>Note:</strong> 对于流查询，该操作将被重写为join and group操作。根据不同的输入行的数量，计算查询结果所需的状态可能会无限增长。请提供具有有效保留间隔的查询配置，以防止出现过多的状态。有关详细信息，请参阅<a href="https://blog.csdn.net/lp284558195/article/details/104609739">查询配置</a></td>
</tr>
</tbody>
</table>
<h3 id="orderby-offset-fetch">OrderBy, Offset &amp; Fetch</h3>
<table>
<thead>
<tr>
<th>Operators</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Order By</strong> <code>Batch</code></td>
<td>类似于SQL ORDER BY子句。返回在所有并行分区上全局排序的记录。<br /><code>Table in = tableEnv.fromDataSet(ds, &quot;a, b, c&quot;);</code><br /> <code>Table result = in.orderBy($(&quot;a&quot;).asc()&quot;);</code></td>
</tr>
<tr>
<td><strong>Offset &amp; Fetch</strong> <code>Batch</code></td>
<td>类似于SQL OFFSET和FETCH子句。偏移和提取限制了从排序结果返回的记录数。偏移和提取在技术上是Order By运算符的一部分，因此必须在其之前。<br /><code>Table in = tableEnv.fromDataSet(ds, &quot;a, b, c&quot;);</code><br />  <code>// returns the first 5 records from the sorted result</code><br /><code>Table result1 = in.orderBy($(&quot;a&quot;).asc()).fetch(5);</code><br /> <code>// skips the first 3 records and returns all following records from the sorted result</code><br /><code>Table result2 = in.orderBy($(&quot;a&quot;).asc()).offset(3);</code><br /><code>// skips the first 10 records and returns the next 5 records from the sorted result</code><br /><code>Table result3 = in.orderBy($(&quot;a&quot;).asc()).offset(10).fetch(5);</code></td>
</tr>
</tbody>
</table>
<h3 id="insert">Insert</h3>
<table>
<thead>
<tr>
<th>Operators</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Insert Into</strong> <code>Batch</code> <code>Streaming</code></td>
<td>与SQL查询中的INSERT INTO子句相似。在已插入的输出表中执行插入。 输出表必须在TableEnvironment中注册。此外，已注册表的模式必须与查询的模式匹配。 <br /><code>Table orders = tableEnv.from(&quot;Orders&quot;);</code><br /><code>orders.executeInsert(&quot;OutOrders&quot;);</code></td>
</tr>
</tbody>
</table>
<h3 id="group-windows">Group Windows</h3>
<p>组窗口根据时间或行计数（row-count ）间隔将行组聚合为有限组，并按组聚合函数。 对于批处理表，窗口是按时间间隔对记录进行分组的便捷快捷方式。</p>
<p>Windows是使用window（w：GroupWindow）子句定义的，并且需要使用as子句指定的别名。为了按窗口对表进行分组，必须像常规分组属性一样在groupBy（…）子句中引用窗口别名。</p>
<p>以下示例显示如何在表上定义窗口聚合。</p>
<pre><code class="language-java">Table table = input
  .window([GroupWindow w].as(&quot;w&quot;))  // define window with alias w
  .groupBy($(&quot;w&quot;))  // group the table by window w
  .select($(&quot;b&quot;).sum());  // aggregate
</code></pre>
<p>在流式环境中，如果窗口聚合除了窗口之外还在一个或多个属性上进行分组，则它们只能并行计算。即groupBy（…）子句引用了窗口别名和至少一个其他属性。仅引用窗口别名的groupBy（…）子句（例如上例中的子句）只能由单个非并行任务求值。以下示例显示如何使用其他分组属性定义窗口聚合。</p>
<pre><code class="language-java">Table table = input
  .window([GroupWindow w].as(&quot;w&quot;))  // define window with alias w
  .groupBy($(&quot;w&quot;), $(&quot;a&quot;))  // group the table by attribute a and window w
  .select($(&quot;a&quot;), $(&quot;b&quot;).sum());  // aggregate
</code></pre>
<p>可以在select语句中将窗口属性（例如时间窗口的开始，结束或行时间戳）添加为窗口别名的属性，分别为w.start，w.end和w.rowtime。窗口开始和行时间时间戳是包含窗口的上下边界。相反，窗口结束时间戳是唯一的窗口上边界。例如，从下午2点开始的30分钟滚动窗口将以14：00：00.000作为开始时间戳，以14：29：59.999作为行时间时间戳，以14：30：00.000作为结束时间戳。</p>
<pre><code class="language-java">Table table = input
  .window([GroupWindow w].as(&quot;w&quot;))  // define window with alias w
  .groupBy($(&quot;w&quot;), $(&quot;a&quot;))  // group the table by attribute a and window w
  .select($(&quot;a&quot;), $(&quot;w&quot;).start(), $(&quot;w&quot;).end(), $(&quot;w&quot;).rowtime(), $(&quot;b&quot;).count()); // aggregate and add window start, end, and rowtime timestamps
</code></pre>
<p>Window参数定义如何将行映射到窗口。窗口不是用户可以实现的接口。相反，Table API提供了一组具有特定语义的预定义Window类，这些类被转换为基础的DataStream或DataSet操作。支持的窗口定义在下面列出。</p>
<h4 id="tumble-滚动窗口">Tumble (滚动窗口)</h4>
<p>滚动窗口将行分配给固定长度的不重叠的连续窗口。</p>
<p>例如，5分钟的翻滚窗口以5分钟为间隔对行进行分组。 可以在事件时间，处理时间或行数上定义翻滚窗口。使用Tumble类定义翻滚窗口如下：</p>
<p>滚动窗口是使用Tumble类定义的，如下所示：</p>
<table>
<thead>
<tr>
<th>Method</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>over</code></td>
<td>定义窗口的长度，可以是时间间隔也可以是行数间隔。</td>
</tr>
<tr>
<td><code>on</code></td>
<td>时间属性为组（时间间隔）或排序（行计数）。 对于批处理查询，这可能是任何Long或Timestamp属性。 对于流式查询，这必须是声明的事件时间或处理时间属性。</td>
</tr>
<tr>
<td><code>as</code></td>
<td>为窗口指定别名。 别名用于引用以下groupBy（）子句中的窗口，并可选择在select（）子句中选择窗口属性，如window start，end或rowtime timestamp。</td>
</tr>
</tbody>
</table>
<pre><code class="language-java">// Tumbling Event-time Window
.window(Tumble.over(lit(10).minutes()).on($(&quot;rowtime&quot;)).as(&quot;w&quot;));

// Tumbling Processing-time Window (assuming a processing-time attribute &quot;proctime&quot;)
.window(Tumble.over(lit(10).minutes()).on($(&quot;proctime&quot;)).as(&quot;w&quot;));

// Tumbling Row-count Window (assuming a processing-time attribute &quot;proctime&quot;)
.window(Tumble.over(rowInterval(10)).on($(&quot;proctime&quot;)).as(&quot;w&quot;));
</code></pre>
<h4 id="slide-滑动窗口">Slide (滑动窗口)</h4>
<p>滑动窗口的大小固定，并以指定的滑动间隔滑动。如果滑动间隔小于窗口大小，则滑动窗口重叠。因此，可以将行分配给多个窗口。</p>
<p>例如，15分钟大小和5分钟滑动间隔的滑动窗口将每行分配给3个不同的15分钟大小的窗口，这些窗口以5分钟的间隔进行调用。 可以在事件时间，处理时间或行数上定义滑动窗口。</p>
<p>滑动窗口是通过使用Slide类定义的，如下所示：</p>
<table>
<thead>
<tr>
<th>Method</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>over</code></td>
<td>定义窗口的长度，可以是时间或行计数间隔。</td>
</tr>
<tr>
<td><code>every</code></td>
<td>定义滑动间隔，可以是时间间隔也可以是行数。 滑动间隔必须与大小间隔的类型相同。</td>
</tr>
<tr>
<td><code>on</code></td>
<td>时间属性为组（时间间隔）或排序（行计数）。 对于批处理查询，这可能是任何Long或Timestamp属性。 对于流式查询，这必须是<a href="https://ci.apache.org/projects/flink/flink-docs-release-1.10/dev/table/streaming/time_attributes.html">声明的事件时间或处理时间属性</a>。</td>
</tr>
<tr>
<td><code>as</code></td>
<td>为窗口指定别名。 别名用于引用以下groupBy（）子句中的窗口，并可选择在select（）子句中选择窗口属性，如window start，end或rowtime timestamp。</td>
</tr>
</tbody>
</table>
<pre><code class="language-java">// Sliding Event-time Window
.window(Slide.over(lit(10).minutes())
            .every(lit(5).minutes())
            .on($(&quot;rowtime&quot;))
            .as(&quot;w&quot;));

// Sliding Processing-time window (assuming a processing-time attribute &quot;proctime&quot;)
.window(Slide.over(lit(10).minutes())
            .every(lit(5).minutes())
            .on($(&quot;proctime&quot;))
            .as(&quot;w&quot;));

// Sliding Row-count window (assuming a processing-time attribute &quot;proctime&quot;)
.window(Slide.over(rowInterval(10)).every(rowInterval(5)).on($(&quot;proctime&quot;)).as(&quot;w&quot;));
</code></pre>
<h4 id="session-会话窗口">Session (会话窗口)</h4>
<p>会话窗口没有固定的大小，但是其边界由不活动的时间间隔定义，即如果在定义的间隔时间内没有事件出现，则会话窗口关闭。</p>
<p>例如，间隔30分钟的会话窗口会在30分钟不活动后观察到一行（否则该行将被添加到现有窗口）后开始，如果30分钟内未添加任何行，则关闭该窗口。会话窗口可以在事件时间或处理时间工作。</p>
<p>会话窗口是通过使用Session类定义的，如下所示：</p>
<table>
<thead>
<tr>
<th>Method</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>withGap</code></td>
<td>将两个窗口之间的间隔定义为时间间隔。</td>
</tr>
<tr>
<td><code>on</code></td>
<td>时间属性为组（时间间隔）或排序（行计数）。 对于批处理查询，这可能是任何Long或Timestamp属性。 对于流式查询，这必须是声明的事件时间或处理时间属性。</td>
</tr>
<tr>
<td><code>as</code></td>
<td>为窗口指定别名。 别名用于引用以下groupBy（）子句中的窗口，并可选择在select（）子句中选择窗口属性，如window start，end或rowtime timestamp。</td>
</tr>
</tbody>
</table>
<pre><code class="language-java">// Session Event-time Window
.window(Session.withGap(lit(10).minutes()).on($(&quot;rowtime&quot;)).as(&quot;w&quot;));

// Session Processing-time Window (assuming a processing-time attribute &quot;proctime&quot;)
.window(Session.withGap(lit(10).minutes()).on($(&quot;proctime&quot;)).as(&quot;w&quot;));
</code></pre>
<h3 id="over-windows">Over Windows</h3>
<p>窗口聚合是标准SQL（OVER子句）已知的，并在查询的SELECT子句中定义。与在GROUP BY子句中指定的组窗口不同，在窗口上方不会折叠行。取而代之的是在窗口聚合中，为每个输入行在其相邻行的范围内计算聚合。</p>
<p>使用window（w：OverWindow <em>）子句（在Python API中使用over_window（</em> OverWindow））定义窗口，并在select() 方法中通过别名引用。以下示例显示如何在表上定义窗口聚合。</p>
<pre><code class="language-java">Table table = input
  .window([OverWindow w].as(&quot;w&quot;))           // define over window with alias w
  .select($(&quot;a&quot;), $(&quot;b&quot;).sum().over($(&quot;w&quot;)), $(&quot;c&quot;).min().over($(&quot;w&quot;))); // aggregate over the over window w
</code></pre>
<p>OverWindow定义了计算聚合的行范围。OverWindow不是用户可以实现的接口。相反，Table API提供了Over类来配置over窗口的属性。可以在事件时间或处理时间以及指定为时间间隔或行计数的范围上定义窗口上方。受支持的over窗口定义作为Over（和其他类）上的方法公开，并在下面列出：</p>
<table>
<thead>
<tr>
<th>Method</th>
<th>Required</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>partitionBy</code></td>
<td>Optional</td>
<td>在一个或多个属性上定义输入的分区。每个分区都经过单独排序，并且聚合函数分别应用于每个分区。 <strong>Note:</strong> 在流环境中，如果窗口包含partition by子句，则只能并行计算整个窗口聚合。没有partitionBy（…），流将由单个非并行任务处理。</td>
</tr>
<tr>
<td><code>orderBy</code></td>
<td>Required</td>
<td>定义每个分区内的行顺序，从而定义将聚合函数应用于行的顺序。 <strong>Note:</strong> 对于流查询，它必须是声明的事件时间或处理时间时间属性。当前，仅支持单个sort属性。</td>
</tr>
<tr>
<td><code>preceding</code></td>
<td>Optional</td>
<td>定义窗口中包含的并在当前行之前的行的间隔。该间隔可以指定为时间间隔或行计数间隔。 用时间间隔的大小指定窗口上的边界，例如，时间间隔为10分钟，行计数间隔为10行。 使用常数指定窗口上的无边界，即对于时间间隔为UNBOUNDED_RANGE或对于行计数间隔为UNBOUNDED_ROW。Windows上的无边界从分区的第一行开始。 如果省略了前面的子句，则将UNBOUNDED_RANGE和CURRENT_RANGE用作窗口的默认前后。</td>
</tr>
<tr>
<td><code>following</code></td>
<td>Optional</td>
<td>定义窗口中包含并紧随当前行的行的窗口间隔。该间隔必须与前面的间隔（时间或行计数）以相同的单位指定。目前，不支持具有当前行之后的行的窗口。而是可以指定两个常量之一： 1. CURRENT_ROW将窗口的上限设置为当前行。 2. CURRENT_RANGE将窗口的上限设置为当前行的排序键，即，与当前行具有相同排序键的所有行都包含在窗口中。 如果省略以下子句，则将时间间隔窗口的上限定义为CURRENT_RANGE，将行计数间隔窗口的上限定义为CURRENT_ROW。</td>
</tr>
<tr>
<td><code>as</code></td>
<td>Required</td>
<td>为上方窗口分配别名。别名用于引用以下select（）子句中的over窗口。</td>
</tr>
</tbody>
</table>
<p>注意：当前，同一select（）调用中的所有聚合函数必须在相同的窗口范围内计算。</p>
<h4 id="unbounded-over-windows">Unbounded Over Windows</h4>
<pre><code class="language-java">// Unbounded Event-time over window (assuming an event-time attribute &quot;rowtime&quot;)
.window(Over.partitionBy($(&quot;a&quot;)).orderBy($(&quot;rowtime&quot;)).preceding(UNBOUNDED_RANGE).as(&quot;w&quot;));

// Unbounded Processing-time over window (assuming a processing-time attribute &quot;proctime&quot;)
.window(Over.partitionBy($(&quot;a&quot;)).orderBy(&quot;proctime&quot;).preceding(UNBOUNDED_RANGE).as(&quot;w&quot;));

// Unbounded Event-time Row-count over window (assuming an event-time attribute &quot;rowtime&quot;)
.window(Over.partitionBy($(&quot;a&quot;)).orderBy($(&quot;rowtime&quot;)).preceding(UNBOUNDED_ROW).as(&quot;w&quot;));
 
// Unbounded Processing-time Row-count over window (assuming a processing-time attribute &quot;proctime&quot;)
.window(Over.partitionBy($(&quot;a&quot;)).orderBy($(&quot;proctime&quot;)).preceding(UNBOUNDED_ROW).as(&quot;w&quot;));
</code></pre>
<h4 id="bounded-over-windows">Bounded Over Windows</h4>
<pre><code class="language-java">// Bounded Event-time over window (assuming an event-time attribute &quot;rowtime&quot;)
.window(Over.partitionBy($(&quot;a&quot;)).orderBy($(&quot;rowtime&quot;)).preceding(lit(1).minutes()).as(&quot;w&quot;))

// Bounded Processing-time over window (assuming a processing-time attribute &quot;proctime&quot;)
.window(Over.partitionBy($(&quot;a&quot;)).orderBy($(&quot;proctime&quot;)).preceding(lit(1).minutes()).as(&quot;w&quot;))

// Bounded Event-time Row-count over window (assuming an event-time attribute &quot;rowtime&quot;)
.window(Over.partitionBy($(&quot;a&quot;)).orderBy($(&quot;rowtime&quot;)).preceding(rowInterval(10)).as(&quot;w&quot;))
 
// Bounded Processing-time Row-count over window (assuming a processing-time attribute &quot;proctime&quot;)
.window(Over.partitionBy($(&quot;a&quot;)).orderBy($(&quot;proctime&quot;)).preceding(rowInterval(10)).as(&quot;w&quot;))
</code></pre>
<h3 id="基于行的操作">基于行的操作</h3>
<p>基于行的操作生成具有多列的输出。</p>
<ul>
<li>
<p><strong>Map</strong> <code>Batch</code> <code>Streaming</code></p>
<p>使用用户定义的标量函数或内置标量函数执行映射操作。如果输出类型是复合类型，则输出将被展平。</p>
<pre><code class="language-java">public class MyMapFunction extends ScalarFunction {
    public Row eval(String a) {
        return Row.of(a, &quot;pre-&quot; + a);
    }

    @Override
    public TypeInformation&lt;?&gt; getResultType(Class&lt;?&gt;[] signature) {
        return Types.ROW(Types.STRING(), Types.STRING());
    }
}

ScalarFunction func = new MyMapFunction();
tableEnv.registerFunction(&quot;func&quot;, func);

Table table = input
  .map(call(&quot;func&quot;, $(&quot;c&quot;)).as(&quot;a&quot;, &quot;b&quot;))
</code></pre>
</li>
<li>
<p><strong>FlatMap</strong> <code>Batch</code> <code>Streaming</code></p>
<p>使用表函数执行flatMap操作。</p>
<pre><code class="language-java">public class MyFlatMapFunction extends TableFunction&lt;Row&gt; {

    public void eval(String str) {
        if (str.contains(&quot;#&quot;)) {
            String[] array = str.split(&quot;#&quot;);
            for (int i = 0; i &lt; array.length; ++i) {
                collect(Row.of(array[i], array[i].length()));
            }
        }
    }

    @Override
    public TypeInformation&lt;Row&gt; getResultType() {
        return Types.ROW(Types.STRING(), Types.INT());
    }
}

TableFunction func = new MyFlatMapFunction();
tableEnv.registerFunction(&quot;func&quot;, func);

Table table = input
  .flatMap(call(&quot;func&quot;, $(&quot;c&quot;)).as(&quot;a&quot;, &quot;b&quot;))
</code></pre>
</li>
<li>
<p><strong>Aggregate</strong> <code>Batch</code> <code>Streaming</code> <code>Result Updating</code></p>
<p>使用聚合函数执行聚合操作。您必须使用select语句关闭“聚合”，并且select语句不支持聚合函数。如果输出类型是复合类型，则聚合的输出将被展平。</p>
<pre><code class="language-java">public class MyMinMaxAcc {
    public int min = 0;
    public int max = 0;
}

public class MyMinMax extends AggregateFunction&lt;Row, MyMinMaxAcc&gt; {

    public void accumulate(MyMinMaxAcc acc, int value) {
        if (value &lt; acc.min) {
            acc.min = value;
        }
        if (value &gt; acc.max) {
            acc.max = value;
        }
    }

    @Override
    public MyMinMaxAcc createAccumulator() {
        return new MyMinMaxAcc();
    }

    public void resetAccumulator(MyMinMaxAcc acc) {
        acc.min = 0;
        acc.max = 0;
    }

    @Override
    public Row getValue(MyMinMaxAcc acc) {
        return Row.of(acc.min, acc.max);
    }

    @Override
    public TypeInformation&lt;Row&gt; getResultType() {
        return new RowTypeInfo(Types.INT, Types.INT);
    }
}

AggregateFunction myAggFunc = new MyMinMax();
tableEnv.registerFunction(&quot;myAggFunc&quot;, myAggFunc);
Table table = input
  .groupBy($(&quot;key&quot;))
  .aggregate(call(&quot;myAggFunc&quot;, $(&quot;a&quot;)).as(&quot;x&quot;, &quot;y&quot;))
  .select($(&quot;key&quot;), $(&quot;x&quot;), $(&quot;y&quot;))
</code></pre>
</li>
<li>
<p><strong>Group Window Aggregate</strong> <code>Batch</code> <code>Streaming</code></p>
<p>在组窗口和可能的一个或多个分组键上对表进行分组和聚集。您必须使用select语句关闭“聚合”。并且select语句不支持“ *”或聚合函数。</p>
<pre><code class="language-java">AggregateFunction myAggFunc = new MyMinMax();
tableEnv.registerFunction(&quot;myAggFunc&quot;, myAggFunc);

Table table = input
    .window(Tumble.over(lit(5).minutes())
                  .on($(&quot;rowtime&quot;))
                  .as(&quot;w&quot;)) // define window
    .groupBy($(&quot;key&quot;), $(&quot;w&quot;)) // group by key and window
    .aggregate(call(&quot;myAggFunc&quot;, $(&quot;a&quot;)).as(&quot;x&quot;, &quot;y&quot;))
    .select($(&quot;key&quot;), $(&quot;x&quot;), $(&quot;y&quot;), $(&quot;w&quot;).start(), $(&quot;w&quot;).end()); // access window properties and aggregate results
</code></pre>
</li>
<li>
<p><strong>FlatAggregate</strong> <code>Batch</code> <code>Streaming</code></p>
<p>类似于GroupBy聚合。使用以下运行表聚合运算符将分组键上的行分组，以逐行聚合行。与AggregateFunction的区别在于TableAggregateFunction可以为一个组返回0个或更多记录。您必须使用select语句关闭“ flatAggregate”。并且select语句不支持聚合函数。除了使用emitValue来输出结果之外，还可以使用emitUpdateWithRetract方法。与emitValue不同，emitUpdateWithRetract用于发出已更新的值。此方法在撤消模式下增量输出数据，即，一旦有更新，我们就必须撤消旧记录，然后再发送新的更新记录。如果在表聚合函数中定义了这两种方法，则将优先使用emitUpdateWithRetract方法，因为这两种方法比emitValue更有效，因为它可以增量输出值。</p>
<pre><code class="language-java">/**
 * Accumulator for Top2.
 */
public class Top2Accum {
    public Integer first;
    public Integer second;
}

/**
 * The top2 user-defined table aggregate function.
 */
public class Top2 extends TableAggregateFunction&lt;Tuple2&lt;Integer, Integer&gt;, Top2Accum&gt; {

    @Override
    public Top2Accum createAccumulator() {
        Top2Accum acc = new Top2Accum();
        acc.first = Integer.MIN_VALUE;
        acc.second = Integer.MIN_VALUE;
        return acc;
    }


    public void accumulate(Top2Accum acc, Integer v) {
        if (v &gt; acc.first) {
            acc.second = acc.first;
            acc.first = v;
        } else if (v &gt; acc.second) {
            acc.second = v;
        }
    }

    public void merge(Top2Accum acc, java.lang.Iterable&lt;Top2Accum&gt; iterable) {
        for (Top2Accum otherAcc : iterable) {
            accumulate(acc, otherAcc.first);
            accumulate(acc, otherAcc.second);
        }
    }

    public void emitValue(Top2Accum acc, Collector&lt;Tuple2&lt;Integer, Integer&gt;&gt; out) {
        // emit the value and rank
        if (acc.first != Integer.MIN_VALUE) {
            out.collect(Tuple2.of(acc.first, 1));
        }
        if (acc.second != Integer.MIN_VALUE) {
            out.collect(Tuple2.of(acc.second, 2));
        }
    }
}

tEnv.registerFunction(&quot;top2&quot;, new Top2());
Table orders = tableEnv.from(&quot;Orders&quot;);
Table result = orders
    .groupBy($(&quot;key&quot;))
    .flatAggregate(call(&quot;top2&quot;, $(&quot;a&quot;)).as(&quot;v&quot;, &quot;rank&quot;))
    .select($(&quot;key&quot;), $(&quot;v&quot;), $(&quot;rank&quot;);
</code></pre>
<p>**Note:**对于流查询，根据聚合的类型和不同的分组键的数量，计算查询结果所需的状态可能会无限增长。请提供具有有效保留间隔的查询配置，以防止出现过多的状态。有关详细信息，请参见查询配置。</p>
</li>
<li>
<p><strong>Group Window FlatAggregate</strong> <code>Batch</code> <code>Streaming</code></p>
<p>在组窗口和可能的一个或多个分组键上对表进行分组和聚集。您必须使用select语句关闭“ flatAggregate”。并且select语句不支持聚合函数。</p>
<pre><code class="language-java">tableEnv.registerFunction(&quot;top2&quot;, new Top2());
Table orders = tableEnv.from(&quot;Orders&quot;);
Table result = orders
    .window(Tumble.over(lit(5).minutes())
                  .on($(&quot;rowtime&quot;))
                  .as(&quot;w&quot;)) // define window
    .groupBy($(&quot;a&quot;), $(&quot;w&quot;)) // group by key and window
    .flatAggregate(call(&quot;top2&quot;, $(&quot;b&quot;).as(&quot;v&quot;, &quot;rank&quot;))
    .select($(&quot;a&quot;), $(&quot;w&quot;).start(), $(&quot;w&quot;).end(), $(&quot;w&quot;).rowtime(), $(&quot;v&quot;), $(&quot;rank&quot;)); // access window properties and aggregate results
</code></pre>
</li>
</ul>
<h2 id="data-types">Data Types</h2>
<p>请参阅有关<a href="http://www.lllpan.top/article/52">数据类型</a>的专用页面。通用类型和（嵌套的）复合类型（例如POJO，元组，行，Scala案例类）也可以是一行的字段。可以使用值访问功能访问具有任意嵌套的复合类型的字段。泛型类型被视为黑盒，可以通过用户定义的函数传递或处理。</p>
<h2 id="表达式语法">表达式语法</h2>
<p>前面几节中的某些运算符期望一个或多个表达式。可以使用嵌入式Scala DSL或字符串指定表达式。请参考上面的示例以了解如何指定表达式。</p>
<p>这是用于表达式的EBNF语法：</p>
<pre><code class="language-sh">expressionList = expression , { &quot;,&quot; , expression } ;

expression = overConstant | alias ;

alias = logic | ( logic , &quot;as&quot; , fieldReference ) | ( logic , &quot;as&quot; , &quot;(&quot; , fieldReference , { &quot;,&quot; , fieldReference } , &quot;)&quot; ) ;

logic = comparison , [ ( &quot;&amp;&amp;&quot; | &quot;||&quot; ) , comparison ] ;

comparison = term , [ ( &quot;=&quot; | &quot;==&quot; | &quot;===&quot; | &quot;!=&quot; | &quot;!==&quot; | &quot;&gt;&quot; | &quot;&gt;=&quot; | &quot;&lt;&quot; | &quot;&lt;=&quot; ) , term ] ;

term = product , [ ( &quot;+&quot; | &quot;-&quot; ) , product ] ;

product = unary , [ ( &quot;*&quot; | &quot;/&quot; | &quot;%&quot;) , unary ] ;

unary = [ &quot;!&quot; | &quot;-&quot; | &quot;+&quot; ] , composite ;

composite = over | suffixed | nullLiteral | prefixed | atom ;

suffixed = interval | suffixAs | suffixCast | suffixIf | suffixDistinct | suffixFunctionCall | timeIndicator ;

prefixed = prefixAs | prefixCast | prefixIf | prefixDistinct | prefixFunctionCall ;

interval = timeInterval | rowInterval ;

timeInterval = composite , &quot;.&quot; , (&quot;year&quot; | &quot;years&quot; | &quot;quarter&quot; | &quot;quarters&quot; | &quot;month&quot; | &quot;months&quot; | &quot;week&quot; | &quot;weeks&quot; | &quot;day&quot; | &quot;days&quot; | &quot;hour&quot; | &quot;hours&quot; | &quot;minute&quot; | &quot;minutes&quot; | &quot;second&quot; | &quot;seconds&quot; | &quot;milli&quot; | &quot;millis&quot;) ;

rowInterval = composite , &quot;.&quot; , &quot;rows&quot; ;

suffixCast = composite , &quot;.cast(&quot; , dataType , &quot;)&quot; ;

prefixCast = &quot;cast(&quot; , expression , dataType , &quot;)&quot; ;

dataType = &quot;BYTE&quot; | &quot;SHORT&quot; | &quot;INT&quot; | &quot;LONG&quot; | &quot;FLOAT&quot; | &quot;DOUBLE&quot; | &quot;BOOLEAN&quot; | &quot;STRING&quot; | &quot;DECIMAL&quot; | &quot;SQL_DATE&quot; | &quot;SQL_TIME&quot; | &quot;SQL_TIMESTAMP&quot; | &quot;INTERVAL_MONTHS&quot; | &quot;INTERVAL_MILLIS&quot; | ( &quot;MAP&quot; , &quot;(&quot; , dataType , &quot;,&quot; , dataType , &quot;)&quot; ) | ( &quot;PRIMITIVE_ARRAY&quot; , &quot;(&quot; , dataType , &quot;)&quot; ) | ( &quot;OBJECT_ARRAY&quot; , &quot;(&quot; , dataType , &quot;)&quot; ) ;

suffixAs = composite , &quot;.as(&quot; , fieldReference , &quot;)&quot; ;

prefixAs = &quot;as(&quot; , expression, fieldReference , &quot;)&quot; ;

suffixIf = composite , &quot;.?(&quot; , expression , &quot;,&quot; , expression , &quot;)&quot; ;

prefixIf = &quot;?(&quot; , expression , &quot;,&quot; , expression , &quot;,&quot; , expression , &quot;)&quot; ;

suffixDistinct = composite , &quot;distinct.()&quot; ;

prefixDistinct = functionIdentifier , &quot;.distinct&quot; , [ &quot;(&quot; , [ expression , { &quot;,&quot; , expression } ] , &quot;)&quot; ] ;

suffixFunctionCall = composite , &quot;.&quot; , functionIdentifier , [ &quot;(&quot; , [ expression , { &quot;,&quot; , expression } ] , &quot;)&quot; ] ;

prefixFunctionCall = functionIdentifier , [ &quot;(&quot; , [ expression , { &quot;,&quot; , expression } ] , &quot;)&quot; ] ;

atom = ( &quot;(&quot; , expression , &quot;)&quot; ) | literal | fieldReference ;

fieldReference = &quot;*&quot; | identifier ;

nullLiteral = &quot;nullOf(&quot; , dataType , &quot;)&quot; ;

timeIntervalUnit = &quot;YEAR&quot; | &quot;YEAR_TO_MONTH&quot; | &quot;MONTH&quot; | &quot;QUARTER&quot; | &quot;WEEK&quot; | &quot;DAY&quot; | &quot;DAY_TO_HOUR&quot; | &quot;DAY_TO_MINUTE&quot; | &quot;DAY_TO_SECOND&quot; | &quot;HOUR&quot; | &quot;HOUR_TO_MINUTE&quot; | &quot;HOUR_TO_SECOND&quot; | &quot;MINUTE&quot; | &quot;MINUTE_TO_SECOND&quot; | &quot;SECOND&quot; ;

timePointUnit = &quot;YEAR&quot; | &quot;MONTH&quot; | &quot;DAY&quot; | &quot;HOUR&quot; | &quot;MINUTE&quot; | &quot;SECOND&quot; | &quot;QUARTER&quot; | &quot;WEEK&quot; | &quot;MILLISECOND&quot; | &quot;MICROSECOND&quot; ;

over = composite , &quot;over&quot; , fieldReference ;

overConstant = &quot;current_row&quot; | &quot;current_range&quot; | &quot;unbounded_row&quot; | &quot;unbounded_row&quot; ;

timeIndicator = fieldReference , &quot;.&quot; , ( &quot;proctime&quot; | &quot;rowtime&quot; ) ;
</code></pre>
<p><strong>文字</strong>：这里的文字是有效的Java文字。字符串文字可以使用单引号或双引号指定。复制引号以进行转义（例如“是我。”或“我”喜欢”狗。”）。</p>
<p><strong>空文字</strong>：空文字必须附加一个类型。使用nullOf（type）（例如nullOf（INT））创建空值。</p>
<p><strong>字段引用</strong>：fieldReference指定数据中的一列（如果使用*，则指定所有列），而functionIdentifier指定受支持的标量函数。列名和函数名遵循Java标识符语法。</p>
<p><strong>函数调用</strong>：指定为字符串的表达式也可以使用前缀表示法而不是后缀表示法来调用运算符和函数。</p>
<p><strong>小数</strong>：如果需要使用精确的数值或大的小数，则Table API还支持Java的BigDecimal类型。在Scala Table API中，小数可以由BigDecimal（“ 123456”）定义，而在Java中，可以通过附加“ p”来精确定义例如123456页</p>
<p><strong>时间表示</strong>：为了使用时间值，Table API支持Java SQL的日期，时间和时间戳类型。在Scala Table API中，可以使用java.sql.Date.valueOf（“ 2016-06-27”），java.sql.Time.valueOf（“ 10:10:42”）或java.sql定义文字。Timestamp.valueOf（“ 2016-06-27 10：10：42.123”）。Java和Scala表API还支持调用“ 2016-06-27” .toDate（），“ 10:10:42” .toTime（）和“ 2016-06-27 10：10：42.123” .toTimestamp（）用于将字符串转换为时间类型。注意：由于Java的时态SQL类型取决于时区，因此请确保Flink Client和所有TaskManager使用相同的时区。</p>
<p><strong>时间间隔</strong>：时间间隔可以表示为月数（Types.INTERVAL_MONTHS）或毫秒数（Types.INTERVAL_MILLIS）。可以添加或减去相同类型的间隔（例如1.小时+ 10分钟）。可以将毫秒间隔添加到时间点（例如“ 2016-08-10” .toDate + 5.days）。</p>
<p><strong>Scala表达式</strong>:：Scala表达式使用隐式转换。因此，请确保将通配符导入org.apache.flink.table.api.scala._添加到程序中。如果文字不被视为表达式，请使用.toExpr（如3.toExpr）强制转换文字。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[第九章 状态编程和容错机制]]></title>
        <id>https://tianxiawuhao.github.io/post/rxadb61B8/</id>
        <link href="https://tianxiawuhao.github.io/post/rxadb61B8/">
        </link>
        <updated>2021-04-15T08:54:53.000Z</updated>
        <content type="html"><![CDATA[<p style="text-indent:2em">流式计算分为无状态和有状态两种情况。 无状态的计算观察每个独立事件， 并根据最后一个事件输出结果。 例如， 流处理应用程序从传感器接收温度读数， 并在
温度超过 90 度时发出警告。 有状态的计算则会基于多个事件输出结果。 以下是一些
例子。</p>
<ul>
<li>所有类型的窗口。 例如， 计算过去一小时的平均温度， 就是有状态的计算。</li>
<li>所有用于复杂事件处理的状态机。 例如， 若在一分钟内收到两个相差 20 度<br>
以上的温度读数， 则发出警告， 这是有状态的计算。</li>
<li>流与流之间的所有关联操作， 以及流与静态表或动态表之间的关联操作，<br>
都是有状态的计算。</li>
</ul>
<p style="text-indent:2em">下图展示了无状态流处理和有状态流处理的主要区别。 无状态流处理分别接收每条数据记录(图中的黑条)， 然后根据最新输入的数据生成输出数据(白条)。 有状态
流处理会维护状态(根据每条输入记录进行更新)， 并基于最新输入的记录和当前的
状态值生成输出记录(灰条)。</p>
<p><img src="https://tianxiawuhao.github.io/post-images/1618478116508.png" alt="" loading="lazy"><br>
图 无状态和有状态的流处理</p>
<p style="text-indent:2em">上图中输入数据由黑条表示。 无状态流处理每次只转换一条输入记录， 并且仅根据最新的输入记录输出结果(白条)。 有状态 流处理维护所有已处理记录的状态
值， 并根据每条新输入的记录更新状态， 因此输出记录(灰条)反映的是综合考虑多
个事件之后的结果。</p>
<p style="text-indent:2em">尽管无状态的计算很重要， 但是流处理对有状态的计算更感兴趣。 事实上， 正确地实现有状态的计算比实现无状态的计算难得多。 旧的流处理系统并不支持有状
态的计算， 而新一代的流处理系统则将状态及其正确性视为重中之重。</p>
<h2 id="有状态的算子和应用程序">有状态的算子和应用程序</h2>
<p style="text-indent:2em">Flink 内置的很多算子， 数据源 source， 数据存储 sink 都是有状态的， 流中的数据都是 buffer records， 会保存一定的元素或者元数据。 例如: ProcessWindowFunction会缓存输入流的数据， ProcessFunction 会保存设置的定时器信息等等。
在 Flink 中， 状态始终与特定算子相关联。 总的来说， 有两种类型的状态：</p>
<ul>
<li>算子状态（ operator state）</li>
<li>键控状态（ keyed state）</li>
</ul>
<h3 id="算子状态operator-state">算子状态（operator state）</h3>
<p style="text-indent:2em">算子状态的作用范围限定为算子任务。 这意味着由同一并行任务所处理的所有数据都可以访问到相同的状态， 状态对于同一任务而言是共享的。 算子状态不能由
相同或不同算子的另一个任务访问。</p>
<p><img src="https://tianxiawuhao.github.io/post-images/1618478177511.png" alt="" loading="lazy"><br>
图 具有算子状态的任务<br>
Flink 为算子状态提供三种基本数据结构：</p>
<ol>
<li>列表状态（List state）<br>
将状态表示为一组数据的列表。</li>
<li>联合列表状态（Union list state）<br>
也将状态表示为数据的列表。 它与常规列表状态的区别在于， 在发生故障时， 或者从保<br>
存点（savepoint） 启动应用程序时如何恢复。</li>
<li>广播状态（Broadcast state）<br>
如果一个算子有多项任务， 而它的每项任务状态又都相同， 那么这种特殊情况最适合应<br>
用广播状态。</li>
</ol>
<h3 id="键控状态keyed-state">键控状态（keyed state）</h3>
<p style="text-indent:2em">键控状态是根据输入数据流中定义的键（key） 来维护和访问的。 Flink 为每个键值维护一个状态实例， 并将具有相同键的所有数据， 都分区到同一个算子任务中， 这个任务会维护和处理这个 key 对应的状态。 当任务处理一条数据时， 它会自动将状态的访问范围限定为当
前数据的 key。 因此， 具有相同 key 的所有数据都会访问相同的状态。 Keyed State 很类似于
一个分布式的 key-value map 数据结构， 只能用于 KeyedStream（ keyBy 算子处理之后） 。</p>
<p><img src="https://tianxiawuhao.github.io/post-images/1618478247650.png" alt="" loading="lazy"><br>
图 具有键控状态的任务<br>
Flink 的 Keyed State 支持以下数据类型：</p>
<ol>
<li>ValueState[T]保存单个的值， 值的类型为 T。<br>
1.1 get 操作: ValueState.value()<br>
1.2 set 操作: ValueState.update(value: T)</li>
<li>ListState[T]保存一个列表， 列表里的元素的数据类型为 T。 基本操作如下：<br>
2.1 ListState.add(value: T)<br>
2.2 ListState.addAll(values: java.util.List[T])<br>
2.3 ListState.get()返回 Iterable[T]<br>
2.4 ListState.update(values: java.util.List[T])</li>
<li>MapState[K, V]保存 Key-Value 对。<br>
3.1 MapState.get(key: K)<br>
3.2 MapState.put(key: K, value: V)<br>
3.3 MapState.contains(key: K)<br>
3.4 MapState.remove(key: K)</li>
<li>ReducingState[T]</li>
<li>AggregatingState[I, O]<br>
State.clear()是清空操作。</li>
</ol>
<pre><code class="language-java">val sensorData: DataStream[SensorReading] = ...
val keyedData: KeyedStream[SensorReading, String] = sensorData.keyBy(_.id)
val alerts: DataStream[(String, Double, Double)] = keyedData
    .flatMap(new TemperatureAlertFunction(1.7))
class TemperatureAlertFunction(val threshold: Double) extends
RichFlatMapFunction[SensorReading, (String, Double, Double)] {
    private var lastTempState: ValueState[Double] = _
    override def open(parameters: Configuration): Unit = {
        val lastTempDescriptor = new ValueStateDescriptor[Double](&quot;lastTemp&quot;,
        classOf[Double])
        lastTempState = getRuntimeContext.getState[Double](lastTempDescriptor)
    } 
    override def flatMap(reading: SensorReading,
    out: Collector[(String, Double, Double)]): Unit = {
        val lastTemp = lastTempState.value()
        val tempDiff = (reading.temperature - lastTemp).abs
        if (tempDiff &gt; threshold) {
            out.collect((reading.id, reading.temperature, tempDiff))
        }
        this.lastTempState.update(reading.temperature)
    }
} 
</code></pre>
<p style="text-indent:2em">通过 RuntimeContext 注册 StateDescriptor。 StateDescriptor 以状态 state 的名字和存储的数据类型为参数。</p>
<p style="text-indent:2em">在 open()方法中创建 state 变量。 注意复习之前的 RichFunction 相关知识。接下来我们使用了 FlatMap with keyed ValueState 的快捷方式 flatMapWithState
实现以上需求。</p>
<pre><code class="language-java">val alerts: DataStream[(String, Double, Double)] = keyedSensorData
.flatMapWithState[(String, Double, Double), Double] {
    case (in: SensorReading, None) =&gt;
         (List.empty, Some(in.temperature))
    case (r: SensorReading, lastTemp: Some[Double]) =&gt;
        val tempDiff = (r.temperature - lastTemp.get).abs
        if (tempDiff &gt; 1.7) {
            (List((r.id, r.temperature, tempDiff)), Some(r.temperature))
        } else {
            (List.empty, Some(r.temperature))
        }
}
</code></pre>
<h2 id="状态一致性">状态一致性</h2>
<p style="text-indent:2em">当在分布式系统中引入状态时， 自然也引入了一致性问题。 一致性实际上是"正确性级别"的另一种说法， 也就是说在成功处理故障并恢复之后得到的结果， 与没
有发生任何故障时得到的结果相比， 前者到底有多正确？ 举例来说， 假设要对最近
一小时登录的用户计数。 在系统经历故障之后， 计数结果是多少？ 如果有偏差， 是
有漏掉的计数还是重复计数？</p>
<h3 id="一致性级别">一致性级别</h3>
<p>在流处理中， 一致性可以分为 3 个级别：</p>
<ul>
<li>at-most-once: 这其实是没有正确性保障的委婉说法——故障发生之后， 计<br>
数结果可能丢失。 同样的还有 udp。</li>
<li>at-least-once: 这表示计数结果可能大于正确值， 但绝不会小于正确值。 也<br>
就是说， 计数程序在发生故障后可能多算， 但是绝不会少算。</li>
<li>exactly-once: 这指的是系统保证在发生故障后得到的计数结果与正确值一<br>
致。</li>
</ul>
<p style="text-indent:2em">曾经， at-least-once 非常流行。 第一代流处理器(如 Storm 和 Samza)刚问世时只保证 at-least-once， 原因有二。</p>
<ol>
<li>保证 exactly-once 的系统实现起来更复杂。 这在基础架构层(决定什么代表<br>
正确， 以及 exactly-once 的范围是什么)和实现层都很有挑战性。</li>
<li>流处理系统的早期用户愿意接受框架的局限性， 并在应用层想办法弥补(例<br>
如使应用程序具有幂等性， 或者用批量计算层再做一遍计算)。<br>
最先保证 exactly-once 的系统(Storm Trident 和 Spark Streaming)在性能和表现力<br>
这两个方面付出了很大的代价。 为了保证 exactly-once， 这些系统无法单独地对每条<br>
记录运用应用逻辑， 而是同时处理多条(一批)记录， 保证对每一批的处理要么全部<br>
成功， 要么全部失败。 这就导致在得到结果前， 必须等待一批记录处理结束。 因此，<br>
用户经常不得不使用两个流处理框架(一个用来保证 exactly-once， 另一个用来对每<br>
个元素做低延迟处理)， 结果使基础设施更加复杂。 曾经， 用户不得不在保证<br>
exactly-once 与获得低延迟和效率之间权衡利弊。 Flink 避免了这种权衡。<br>
Flink 的一个重大价值在于， 它既保证了 exactly-once， 也具有低延迟和高吞吐<br>
的处理能力。<br>
从根本上说， Flink 通过使自身满足所有需求来避免权衡， 它是业界的一次意义<br>
重大的技术飞跃。 尽管这在外行看来很神奇， 但是一旦了解， 就会恍然大悟。</li>
</ol>
<h3 id="端到端end-to-end-状态一致性">端到端（end-to-end） 状态一致性</h3>
<p style="text-indent:2em">目前我们看到的一致性保证都是由流处理器实现的， 也就是说都是在 Flink 流处理器内部保证的； 而在真实应用中， 流处理应用除了流处理器以外还包含了数据
源（ 例如 Kafka） 和输出到持久化系统。</p>
<p style="text-indent:2em">端到端的一致性保证， 意味着结果的正确性贯穿了整个流处理应用的始终； 每一个组件都保证了它自己的一致性， 整个端到端的一致性级别取决于所有组件中一
致性最弱的组件。 具体可以划分如下：</p>
<ul>
<li>内部保证 —— 依赖 checkpoint</li>
<li>source 端 —— 需要外部源可重设数据的读取位置</li>
<li>sink 端 —— 需要保证从故障恢复时， 数据不会重复写入外部系统<br>
而对于 sink 端， 又有两种具体的实现方式： 幂等（ Idempotent） 写入和事务性<br>
（ Transactional） 写入。</li>
<li>幂等写入<br>
所谓幂等操作， 是说一个操作， 可以重复执行很多次， 但只导致一次结果更改，<br>
也就是说， 后面再重复执行就不起作用了。</li>
<li>事务写入<br>
需要构建事务来写入外部系统， 构建的事务对应着 checkpoint， 等到 checkpoint<br>
真正完成的时候， 才把所有对应的结果写入 sink 系统中。</li>
</ul>
<p style="text-indent:2em">对于事务性写入， 具体又有两种实现方式： 预写日志（ WAL） 和两阶段提交（ 2PC） 。 DataStream API 提供了 GenericWriteAheadSink 模板类和
TwoPhaseCommitSinkFunction 接口， 可以方便地实现这两种方式的事务性写入。
不同 Source 和 Sink 的一致性保证可以用下表说明：</p>
<figure data-type="image" tabindex="1"><img src="https://tianxiawuhao.github.io/post-images/1618538597897.png" alt="" loading="lazy"></figure>
<h2 id="检查点checkpoint">检查点（checkpoint）</h2>
<p style="text-indent:2em">Flink 具体如何保证 exactly-once 呢? 它使用一种被称为"检查点"（ checkpoint）的特性， 在出现故障时将系统重置回正确状态。 下面通过简单的类比来解释检查点
的作用。</p>
<p style="text-indent:2em">假设你和两位朋友正在数项链上有多少颗珠子， 如下图所示。 你捏住珠子， 边数边拨， 每拨过一颗珠子就给总数加一。 你的朋友也这样数他们手中的珠子。 当你
分神忘记数到哪里时， 怎么办呢? 如果项链上有很多珠子， 你显然不想从头再数一
遍， 尤其是当三人的速度不一样却又试图合作的时候， 更是如此(比如想记录前一分
钟三人一共数了多少颗珠子， 回想一下一分钟滚动窗口)。</p>
<figure data-type="image" tabindex="2"><img src="https://tianxiawuhao.github.io/post-images/1618538624575.png" alt="" loading="lazy"></figure>
<p style="text-indent:2em">于是， 你想了一个更好的办法: 在项链上每隔一段就松松地系上一根有色皮筋，
将珠子分隔开; 当珠子被拨动的时候， 皮筋也可以被拨动; 然后， 你安排一个助手，
让他在你和朋友拨到皮筋时记录总数。 用这种方法， 当有人数错时， 就不必从头开
始数。 相反， 你向其他人发出错误警示， 然后你们都从上一根皮筋处开始重数， 助
手则会告诉每个人重数时的起始数值， 例如在粉色皮筋处的数值是多少。</p>
<p style="text-indent:2em">Flink 检查点的作用就类似于皮筋标记。 数珠子这个类比的关键点是: 对于指定的皮筋而言， 珠子的相对位置是确定的; 这让皮筋成为重新计数的参考点。 总状态
(珠子的总数)在每颗珠子被拨动之后更新一次， 助手则会保存与每根皮筋对应的检
查点状态， 如当遇到粉色皮筋时一共数了多少珠子， 当遇到橙色皮筋时又是多少。
当问题出现时， 这种方法使得重新计数变得简单。</p>
<h3 id="flink-的检查点算法">Flink 的检查点算法</h3>
<p style="text-indent:2em">Flink 检查点的核心作用是确保状态正确， 即使遇到程序中断， 也要正确。 记住这一基本点之后， 我们用一个例子来看检查点是如何运行的。 Flink 为用户提供了用
来定义状态的工具。 例如， 以下这个 Scala 程序按照输入记录的第一个字段(一个字
符串)进行分组并维护第二个字段的计数状态。</p>
<pre><code class="language-java">val stream: DataStream[(String, Int)] = ...
val counts: DataStream[(String, Int)] = stream
    .keyBy(record =&gt; record._1)
    .mapWithState( (in: (String, Int), state: Option[Int]) =&gt;
    state match {
        case Some(c) =&gt; ( (in._1, c + in._2), Some(c + in._2) )
        case None =&gt; ( (in._1, in._2), Some(in._2) )
    })
</code></pre>
<p style="text-indent:2em">该程序有两个算子: keyBy 算子用来将记录按照第一个元素(一个字符串)进行分组， 根据该 key 将数据进行重新分区， 然后将记录再发送给下一个算子: 有状态的
map 算子(mapWithState)。 map 算子在接收到每个元素后， 将输入记录的第二个字段
的数据加到现有总数中， 再将更新过的元素发射出去。 下图表示程序的初始状态: 输
入流中的 6 条记录被检查点分割线(checkpoint barrier)隔开， 所有的 map 算子状态均
为 0(计数还未开始)。 所有 key 为 a 的记录将被顶层的 map 算子处理， 所有 key 为 b
的记录将被中间层的 map 算子处理， 所有 key 为 c 的记录则将被底层的 map 算子处
理。</p>
<p><img src="https://tianxiawuhao.github.io/post-images/1618538699318.png" alt="" loading="lazy"><br>
图 按 key 累加计数程序初始状态</p>
<p style="text-indent:2em">上图是程序的初始状态。 注意， a、 b、 c 三组的初始计数状态都是 0， 即三个圆柱上的值。 ckpt 表示检查点分割线（ checkpoint barriers） 。 每条记录在处理顺序上严格地遵守在检查点之前或之后的规定， 例如["b",2]在检查点之前被处理， ["a",2]
则在检查点之后被处理。</p>
<p style="text-indent:2em">当该程序处理输入流中的 6 条记录时， 涉及的操作遍布 3 个并行实例(节点、 CPU内核等)。 那么， 检查点该如何保证 exactly-once 呢?</p>
<p style="text-indent:2em">检查点分割线和普通数据记录类似。 它们由算子处理， 但并不参与计算， 而是会触发与检查点相关的行为。 当读取输入流的数据源(在本例中与 keyBy 算子内联)
遇到检查点屏障时， 它将其在输入流中的位置保存到持久化存储中。 如果输入流来
自消息传输系统(Kafka)， 这个位置就是偏移量。 Flink 的存储机制是插件化的， 持久
化存储可以是分布式文件系统， 如 HDFS。 下图展示了这个过程。</p>
<p><img src="https://tianxiawuhao.github.io/post-images/1618538715815.png" alt="" loading="lazy"><br>
图 遇到 checkpoint barrier 时， 保存其在输入流中的位置</p>
<p style="text-indent:2em">当 Flink 数据源(在本例中与 keyBy 算子内联)遇到检查点分界线（ barrier） 时，它会将其在输入流中的位置保存到持久化存储中。 这让 Flink 可以根据该位置重启。检查点像普通数据记录一样在算子之间流动。 当 map 算子处理完前 3 条数据并
收到检查点分界线时， 它们会将状态以异步的方式写入持久化存储， 如下图所示。</p>
<p><img src="https://tianxiawuhao.github.io/post-images/1618538731125.png" alt="" loading="lazy"><br>
图 保存 map 算子状态， 也就是当前各个 key 的计数值</p>
<p style="text-indent:2em">位于检查点之前的所有记录(["b",2]、 ["b",3]和["c",1])被 map 算子处理之后的情况。 此时， 持久化存储已经备份了检查点分界线在输入流中的位置(备份操作发生在barrier 被输入算子处理的时候)。 map 算子接着开始处理检查点分界线， 并触发将状
态异步备份到稳定存储中这个动作。</p>
<p style="text-indent:2em">当 map 算子的状态备份和检查点分界线的位置备份被确认之后， 该检查点操作就可以被标记为完成， 如下图所示。 我们在无须停止或者阻断计算的条件下， 在一
个逻辑时间点(对应检查点屏障在输入流中的位置)为计算状态拍了快照。 通过确保
备份的状态和位置指向同一个逻辑时间点， 后文将解释如何基于备份恢复计算， 从
而保证 exactly-once。 值得注意的是， 当没有出现故障时， Flink 检查点的开销极小，
检查点操作的速度由持久化存储的可用带宽决定。 回顾数珠子的例子: 除了因为数
错而需要用到皮筋之外， 皮筋会被很快地拨过。</p>
<p><img src="https://tianxiawuhao.github.io/post-images/1618538743728.png" alt="" loading="lazy"><br>
图 检查点操作完成， 继续处理数据</p>
<p style="text-indent:2em">检查点操作完成， 状态和位置均已备份到稳定存储中。 输入流中的所有数据记录都已处理完成。 值得注意的是， 备份的状态值与实际的状态值是不同的。 备份反
映的是检查点的状态。</p>
<p style="text-indent:2em">如果检查点操作失败， Flink 可以丢弃该检查点并继续正常执行， 因为之后的某一个检查点可能会成功。 虽然恢复时间可能更长， 但是对于状态的保证依旧很有力。
只有在一系列连续的检查点操作失败之后， Flink 才会抛出错误， 因为这通常预示着
发生了严重且持久的错误。</p>
<p>现在来看看下图所示的情况: 检查点操作已经完成， 但故障紧随其后。<br>
<img src="https://tianxiawuhao.github.io/post-images/1618538750996.png" alt="" loading="lazy"><br>
图 故障紧跟检查点， 导致最底部的实例丢失</p>
<p style="text-indent:2em">在这种情况下， Flink 会重新拓扑(可能会获取新的执行资源)， 将输入流倒回到上一个检查点， 然后恢复状态值并从该处开始继续计算。 在本例中， ["a",2]、 ["a",2]和["c",2]这几条记录将被重播。</p>
<p style="text-indent:2em">下图展示了这一重新处理过程。 从上一个检查点开始重新计算， 可以保证在剩下的记录被处理之后， 得到的 map 算子的状态值与没有发生故障时的状态值一致。</p>
<p><img src="https://tianxiawuhao.github.io/post-images/1618538757680.png" alt="" loading="lazy"><br>
图 故障时的状态恢复</p>
<p style="text-indent:2em">Flink 将输入流倒回到上一个检查点屏障的位置， 同时恢复 map 算子的状态值。然后， Flink 从此处开始重新处理。 这样做保证了在记录被处理之后， map 算子的状
态值与没有发生故障时的一致。</p>
<p style="text-indent:2em">Flink 检查点算法的正式名称是异步分界线快照(asynchronous barrier snapshotting)。 该算法大致基于 Chandy-Lamport 分布式快照算法。
检查点是 Flink 最有价值的创新之一， 因为它使 Flink 可以保证 exactly-once，
并且不需要牺牲性能。</p>
<h3 id="flinkkafka-如何实现端到端的-exactly-once-语义">Flink+Kafka 如何实现端到端的 exactly-once 语义</h3>
<p style="text-indent:2em">我们知道， 端到端的状态一致性的实现， 需要每一个组件都实现， 对于 Flink +Kafka 的数据管道系统（ Kafka 进、 Kafka 出） 而言， 各组件怎样保证 exactly-once</p>
<p>语义呢？</p>
<ul>
<li>内部 —— 利用 checkpoint 机制， 把状态存盘， 发生故障的时候可以恢复，<br>
保证内部的状态一致性</li>
<li>source —— kafka consumer 作为 source， 可以将偏移量保存下来， 如果后<br>
续任务出现了故障， 恢复的时候可以由连接器重置偏移量， 重新消费数据，<br>
保证一致性</li>
<li>sink —— kafka producer 作为 sink， 采用两阶段提交 sink， 需要实现一个<br>
TwoPhaseCommitSinkFunction</li>
</ul>
<p style="text-indent:2em">内部的 checkpoint 机制我们已经有了了解， 那 source 和 sink 具体又是怎样运行的呢？ 接下来我们逐步做一个分析。</p>
<p style="text-indent:2em">我们知道 Flink 由 JobManager 协调各个 TaskManager 进行 checkpoint 存储，checkpoint 保存在 StateBackend 中， 默认 StateBackend 是内存级的， 也可以改为文件级的进行持久化保存。</p>
<p style="text-indent:2em">当 checkpoint 启动时， JobManager 会将检查点分界线（ barrier） 注入数据流；barrier 会在算子间传递下去。</p>
<p style="text-indent:2em">每个算子会对当前的状态做个快照， 保存到状态后端。 对于 source 任务而言，就会把当前的 offset 作为状态保存起来。 下次从 checkpoint 恢复时， source 任务可以重新提交偏移量， 从上次保存的位置开始重新消费数据。</p>
<p style="text-indent:2em">每个内部的 transform 任务遇到 barrier 时， 都会把状态存到 checkpoint 里。sink 任务首先把数据写入外部 kafka， 这些数据都属于预提交的事务（ 还不能
被消费） ； 当遇到 barrier 时， 把状态保存到状态后端， 并开启新的预提交事务。
当所有算子任务的快照完成， 也就是这次的 checkpoint 完成时， JobManager 会
向所有任务发通知， 确认这次 checkpoint 完成。</p>
<p style="text-indent:2em">当 sink 任务收到确认通知， 就会正式提交之前的事务， kafka 中未确认的数据就改为“ 已确认” ， 数据就真正可以被消费了。</p>
<p style="text-indent:2em">所以我们看到， 执行过程实际上是一个两段式提交， 每个算子执行完成， 会进行“ 预提交” ， 直到执行完 sink 操作， 会发起“ 确认提交” ， 如果执行失败， 预提
交会放弃掉。</p>
<p>具体的两阶段提交步骤总结如下：</p>
<ol>
<li>第一条数据来了之后， 开启一个 kafka 的事务（ transaction） ， 正常写入<br>
kafka 分区日志但标记为未提交， 这就是“ 预提交”</li>
<li>jobmanager 触发 checkpoint 操作， barrier 从 source 开始向下传递， 遇到<br>
barrier 的算子将状态存入状态后端， 并通知 jobmanager</li>
<li>sink 连接器收到 barrier， 保存当前状态， 存入 checkpoint， 通知<br>
jobmanager， 并开启下一阶段的事务， 用于提交下个检查点的数据</li>
<li>jobmanager 收到所有任务的通知， 发出确认信息， 表示 checkpoint 完成</li>
<li>sink 任务收到 jobmanager 的确认信息， 正式提交这段时间的数据</li>
<li>外部 kafka 关闭事务， 提交的数据可以正常消费了。</li>
</ol>
<p style="text-indent:2em">所以我们也可以看到， 如果宕机需要通过 StateBackend 进行恢复， 只能恢复所有确认提交的操作。</p>
<h2 id="选择一个状态后端state-backend">选择一个状态后端(state backend)</h2>
<ul>
<li>MemoryStateBackend<br>
内存级的状态后端， 会将键控状态作为内存中的对象进行管理， 将它们存储<br>
在 TaskManager 的 JVM 堆上； 而将 checkpoint 存储在 JobManager 的内存中。</li>
<li>FsStateBackend<br>
将 checkpoint 存到远程的持久化文件系统（ FileSystem） 上。 而对于本地状态，<br>
跟 MemoryStateBackend 一样， 也会存在 TaskManager 的 JVM 堆上。</li>
<li>RocksDBStateBackend<br>
将所有状态序列化后， 存入本地的 RocksDB 中存储。<br>
注意： RocksDB 的支持并不直接包含在 flink 中， 需要引入依赖：</li>
</ul>
<pre><code class="language-java">&lt;dependency&gt;
    &lt;groupId&gt;org.apache.flink&lt;/groupId&gt;
    &lt;artifactId&gt;flink-statebackend-rocksdb_2.12&lt;/artifactId&gt;
    &lt;version&gt;1.10.1&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<p>设置状态后端为 FsStateBackend：</p>
<pre><code class="language-java">val env = StreamExecutionEnvironment.getExecutionEnvironment
val checkpointPath: String = ???
val backend = new RocksDBStateBackend(checkpointPath)
env.setStateBackend(backend)
env.setStateBackend(new FsStateBackend(&quot;file:///tmp/checkpoints&quot;))
env.enableCheckpointing(1000)
// 配置重启策略
env.setRestartStrategy(RestartStrategies.fixedDelayRestart(60, Time.of(10,
TimeUnit.SECONDS)))
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[第八章 ProcessFunction API（底层 API）]]></title>
        <id>https://tianxiawuhao.github.io/post/cctea1w9G/</id>
        <link href="https://tianxiawuhao.github.io/post/cctea1w9G/">
        </link>
        <updated>2021-04-14T08:39:46.000Z</updated>
        <content type="html"><![CDATA[<p style="text-indent:2em">我们之前学习的转换算子是无法访问事件的时间戳信息和水位线信息的。 而这在一些应用场景下， 极为重要。 例如 MapFunction 这样的 map 转换算子就无法访问
时间戳或者当前事件的事件时间。</p>
<p style="text-indent:2em">基于此， DataStream API 提供了一系列的 Low-Level 转换算子。 可以访问时间戳、 watermark 以及注册定时事件。 还可以输出特定的一些事件， 例如超时事件等。Process Function 用来构建事件驱动的应用以及实现自定义的业务逻辑(使用之前的
window 函数和转换算子无法实现)。 例如， Flink SQL 就是使用 Process Function 实
现的。</p>
<p>Flink 提供了 8 个 Process Function：</p>
<ol>
<li>ProcessFunction</li>
<li>KeyedProcessFunction</li>
<li>CoProcessFunction</li>
<li>ProcessJoinFunction</li>
<li>BroadcastProcessFunction</li>
<li>KeyedBroadcastProcessFunction</li>
<li>ProcessWindowFunction</li>
<li>ProcessAllWindowFunction</li>
</ol>
<h2 id="keyedprocessfunction">KeyedProcessFunction</h2>
<p>这里我们重点介绍 KeyedProcessFunction。</p>
<p style="text-indent:2em">KeyedProcessFunction 用来操作 KeyedStream。 KeyedProcessFunction 会处理流的每一个元素， 输出为 0 个、 1 个或者多个元素。 所有的 Process Function 都继承自RichFunction 接口， 所以都有 open()、 close()和 getRuntimeContext()等方法。 而KeyedProcessFunction[KEY, IN, OUT]还额外提供了两个方法:</p>
<ul>
<li>processElement(v: IN, ctx: Context, out: Collector[OUT]), 流中的每一个元素<br>
都会调用这个方法， 调用结果将会放在 Collector 数据类型中输出。 Context<br>
可以访问元素的时间戳， 元素的 key， 以及 TimerService 时间服务。 Context<br>
还可以将结果输出到别的流(side outputs)。</li>
<li>onTimer(timestamp: Long, ctx: OnTimerContext, out: Collector[OUT])是一个回<br>
调函数。 当之前注册的定时器触发时调用。 参数 timestamp 为定时器所设定<br>
的触发的时间戳。 Collector 为输出结果的集合。 OnTimerContext 和<br>
processElement 的 Context 参数一样， 提供了上下文的一些信息， 例如定时器<br>
触发的时间信息(事件时间或者处理时间)。</li>
</ul>
<h2 id="timerservice-和-定时器timers">TimerService 和 定时器（Timers）</h2>
<p>Context 和 OnTimerContext 所持有的 TimerService 对象拥有以下方法:</p>
<ol>
<li>currentProcessingTime(): Long 返回当前处理时间</li>
<li>currentWatermark(): Long 返回当前 watermark 的时间戳</li>
<li>registerProcessingTimeTimer(timestamp: Long): Unit 会注册当前 key 的processing time 的定时器。 当 processing time 到达定时时间时， 触发 timer。</li>
<li>registerEventTimeTimer(timestamp: Long): Unit 会注册当前 key 的 event time<br>
定时器。 当水位线大于等于定时器注册的时间时， 触发定时器执行回调函数。</li>
<li>deleteProcessingTimeTimer(timestamp: Long): Unit 删除之前注册处理时间定<br>
时器。 如果没有这个时间戳的定时器， 则不执行。</li>
<li>deleteEventTimeTimer(timestamp: Long): Unit 删除之前注册的事件时间定时<br>
器， 如果没有此时间戳的定时器， 则不执行。<br>
当定时器 timer 触发时， 会执行回调函数 onTimer()。 注意定时器 timer 只能在<br>
keyed streams 上面使用。<br>
下面举个例子说明 KeyedProcessFunction 如何操作 KeyedStream。<br>
需求： 监控温度传感器的温度值， 如果温度值在一秒钟之内(processing time)连<br>
续上升， 则报警。</li>
</ol>
<pre><code class="language-java">val warnings = readings
    .keyBy(_.id)
    .process(new TempIncreaseAlertFunction)
</code></pre>
<p>看一下 TempIncreaseAlertFunction 如何实现, 程序中使用了 ValueState 这样一个<br>
状态变量。</p>
<pre><code class="language-java">class TempIncreaseAlertFunction extends KeyedProcessFunction[String, SensorReading, String] {
    // 保存上一个传感器温度值
    lazy val lastTemp: ValueState[Double] = getRuntimeContext.getState(
        new ValueStateDescriptor[Double](&quot;lastTemp&quot;, Types.of[Double])
    ) 
    //保存注册的定时器的时间戳
    lazy val currentTimer: ValueState[Long] = getRuntimeContext.getState(
        new ValueStateDescriptor[Long](&quot;timer&quot;, Types.of[Long])
    ) 
    override def processElement(r: SensorReading,
    ctx: KeyedProcessFunction[String, SensorReading, String]#Context,
    out: Collector[String]): Unit = {
        // 取出上一次的温度
        val prevTemp = lastTemp.value()
        // 将当前温度更新到上一次的温度这个变量中
        lastTemp.update(r.temperature)
        val curTimerTimestamp = currentTimer.value()
        if (prevTemp == 0.0 || r.temperature &lt; prevTemp) {
            // 温度下降或者是第一个温度值， 删除定时器
            ctx.timerService().deleteProcessingTimeTimer(curTimerTimestamp)
            // 清空状态变量
            currentTimer.clear()
        } else if (r.temperature &gt; prevTemp &amp;&amp; curTimerTimestamp == 0) {
            // 温度上升且我们并没有设置定时器
            val timerTs = ctx.timerService().currentProcessingTime() + 1000
            ctx.timerService().registerProcessingTimeTimer(timerTs)
            currentTimer.update(timerTs)
        }
    } 
    override def onTimer(ts: Long,
    ctx: KeyedProcessFunction[String, SensorReading, String]#OnTimerContext,
    out: Collector[String]): Unit = {
        out.collect(&quot;传感器 id 为: &quot; + ctx.getCurrentKey + &quot;的传感器温度值已经连续 1s 上升了。 &quot;)
        currentTimer.clear()
    }
}
</code></pre>
<h2 id="侧输出流sideoutput">侧输出流（SideOutput）</h2>
<p style="text-indent:2em">大部分的 DataStream API 的算子的输出是单一输出， 也就是某种数据类型的流。
除了 split 算子， 可以将一条流分成多条流， 这些流的数据类型也都相同。 process
function 的 side outputs 功能可以产生多条流， 并且这些流的数据类型可以不一样。
一个 side output 可以定义为 OutputTag[X]对象， X 是输出流的数据类型。 process
function 可以通过 Context 对象发射一个事件到一个或者多个 side outputs。
下面是一个示例程序：</p>
<pre><code class="language-java">val monitoredReadings: DataStream[SensorReading] = readings
    .process(new FreezingMonitor)
    monitoredReadings
    .getSideOutput(new OutputTag[String](&quot;freezing-alarms&quot;))
    .print()
readings.print()
</code></pre>
<p style="text-indent:2em">接下来我们实现 FreezingMonitor 函数， 用来监控传感器温度值， 将温度值低于32F 的温度输出到 side output。</p>
<pre><code class="language-java">class FreezingMonitor extends ProcessFunction[SensorReading, SensorReading] {
    // 定义一个侧输出标签
    lazy val freezingAlarmOutput: OutputTag[String] =
    new OutputTag[String](&quot;freezing-alarms&quot;)
    override def processElement(r: SensorReading,
    ctx: ProcessFunction[SensorReading, SensorReading]#Context,
    out: Collector[SensorReading]): Unit = {
        // 温度在 32F 以下时， 输出警告信息
        if (r.temperature &lt; 32.0) {
            output(freezingAlarmOutput, s&quot;Freezing Alarm for ${r.id}&quot;)
        } 
        //所有数据直接常规输出到主流
        out.collect(r)
    }
} 
</code></pre>
<h2 id="coprocessfunction">CoProcessFunction</h2>
<p style="text-indent:2em">对于两条输入流， DataStream API 提供了 CoProcessFunction 这样的 low-level操作。 CoProcessFunction 提供了操作每一个输入流的方法: processElement1()和processElement2()。</p>
<p style="text-indent:2em">类似于 ProcessFunction， 这两种方法都通过 Context 对象来调用。 这个 Context对象可以访问事件数据， 定时器时间戳， TimerService， 以及 side outputs。CoProcessFunction 也提供了 onTimer()回调函数</p>]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[第七章 时间语义与 Wartermark]]></title>
        <id>https://tianxiawuhao.github.io/post/GzZxLdaVh/</id>
        <link href="https://tianxiawuhao.github.io/post/GzZxLdaVh/">
        </link>
        <updated>2021-04-13T07:39:23.000Z</updated>
        <content type="html"><![CDATA[<h2 id="flink-中的时间语义">Flink 中的时间语义</h2>
<p>在 Flink 的流式处理中， 会涉及到时间的不同概念， 如下图所示：<br>
<img src="https://tianxiawuhao.github.io/post-images/1618472695574.png" alt="" loading="lazy"><br>
图 Flink 时间概念<br>
<code>Event Time</code>： 是事件创建的时间。 它通常由事件中的时间戳描述， 例如采集的<br>
日志数据中， 每一条日志都会记录自己的生成时间， Flink 通过时间戳分配器访问事<br>
件时间戳。<br>
<code>Ingestion Time</code>： 是数据进入 Flink 的时间。<br>
<code>Processing Time</code>： 是每一个执行基于时间操作的算子的本地系统时间， 与机器<br>
相关， 默认的时间属性就是 Processing Time。<br>
一个例子——电影《 星球大战》 ：<br>
<img src="https://tianxiawuhao.github.io/post-images/1618472731095.png" alt="" loading="lazy"><br>
例如， 一条日志进入 Flink 的时间为 2020-11-12 10:00:00.123， 到达 Window 的<br>
系统时间为 2020-11-12 10:00:01.234， 日志的内容如下：</p>
<pre><code class="language-sh">2020-11-02 18:37:15.624 INFO Fail over to rm2
</code></pre>
<p>对于业务来说， 要统计 1min 内的故障日志个数， 哪个时间是最有意义的？ ——<br>
eventTime， 因为我们要根据日志的生成时间进行统计。</p>
<h2 id="eventtime-的引入">EventTime 的引入</h2>
<p style="text-indent:2em">在 Flink 的流式处理中， 绝大部分的业务都会使用 eventTime， 一般只在eventTime 无法使用时， 才会被迫使用 ProcessingTime 或者 IngestionTime。
如果要使用 EventTime， 那么需要引入 EventTime 的时间属性， 引入方式如下所
示：</p>
<pre><code class="language-java">// 创建 execution environment
StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
        // 告诉系统按照 EventTime 处理
env.setStreamTimeCharacteristic(TimeCharacteristic.EventTime);
</code></pre>
<h2 id="watermark">Watermark</h2>
<h3 id="基本概念">基本概念</h3>
<p style="text-indent:2em">我们知道， 流处理从事件产生， 到流经 source， 再到 operator， 中间是有一个过程和时间的， 虽然大部分情况下， 流到 operator 的数据都是按照事件产生的时间顺序来的， 但是也不排除由于网络、 分布式等原因， 导致乱序的产生， 所谓乱序， 就
是指 Flink 接收到的事件的先后顺序不是严格按照事件的 Event Time 顺序排列的。</p>
<p><img src="https://tianxiawuhao.github.io/post-images/1618472852122.png" alt="" loading="lazy"><br>
图 数据的乱序</p>
<p style="text-indent:2em">那么此时出现一个问题， 一旦出现乱序， 如果只根据 eventTime 决定 window 的运行， 我们不能明确数据是否全部到位， 但又不能无限期的等下去， 此时必须要有
个机制来保证一个特定的时间后， 必须触发 window 去进行计算了， 这个特别的机
制， 就是 Watermark。</p>
<ol>
<li>Watermark 是一种衡量 Event Time 进展的机制。</li>
<li>Watermark 是用于处理乱序事件的， 而正确的处理乱序事件， 通常用<br>
Watermark 机制结合 window 来实现。</li>
<li>数据流中的 Watermark 用于表示 timestamp 小于 Watermark 的数据， 都已经<br>
到达了， 因此， window 的执行也是由 Watermark 触发的。</li>
<li>Watermark 可以理解成一个延迟触发机制， 我们可以设置 Watermark 的延时<br>
时长 t， 每次系统会校验已经到达的数据中最大的 maxEventTime， 然后认定 eventTime<br>
小于 maxEventTime - t 的所有数据都已经到达， 如果有窗口的停止时间等于<br>
maxEventTime – t， 那么这个窗口被触发执行。<br>
有序流的 Watermarker 如下图所示： （ Watermark 设置为 0）<br>
<img src="https://tianxiawuhao.github.io/post-images/1618472893459.png" alt="" loading="lazy"><br>
图 有序数据的 Watermark<br>
乱序流的 Watermarker 如下图所示： （ Watermark 设置为 2）<br>
<img src="https://tianxiawuhao.github.io/post-images/1618472899867.png" alt="" loading="lazy"><br>
图 无序数据的 Watermark</li>
</ol>
<p style="text-indent:2em">当 Flink 接收到数据时， 会按照一定的规则去生成 Watermark， 这条 Watermark
就等于当前所有到达数据中的 maxEventTime - 延迟时长， 也就是说， Watermark 是
基于数据携带的时间戳生成的， 一旦 Watermark 比当前未触发的窗口的停止时间要
晚， 那么就会触发相应窗口的执行。 由于 event time 是由数据携带的， 因此， 如果
运行过程中无法获取新的数据， 那么没有被触发的窗口将永远都不被触发。</p>
<p style="text-indent:2em">上图中， 我们设置的允许最大延迟到达时间为 2s， 所以时间戳为 7s 的事件对应
的 Watermark 是 5s， 时间戳为 12s 的事件的 Watermark 是 10s， 如果我们的窗口 1
是 1s~5s， 窗口 2 是 6s~10s， 那么时间戳为 7s 的事件到达时的 Watermarker 恰好触
发窗口 1， 时间戳为 12s 的事件到达时的 Watermark 恰好触发窗口 2。Watermark 就是触发前一窗口的“ 关窗时间” ， 一旦触发关门那么以当前时刻为准在窗口范围内的所有所有数据都会收入窗中。
只要没有达到水位那么不管现实中的时间推进了多久都不会触发关窗。</p>
<h3 id="watermark-的引入">Watermark 的引入</h3>
<p>watermark 的引入很简单， 对于乱序数据， 最常见的引用方式如下：</p>
<pre><code class="language-java">// 抽取出时间和生成 watermark
dataStream.assignTimestampsAndWatermarks(new AscendingTimestampExtractor&lt;UserBehavior&gt;() {
    @Override
    public long extractAscendingTimestamp(UserBehavior userBehavior) {
        // 原始数据单位秒，将其转成毫秒
        return userBehavior.timestamp * 1000;
    }
})
</code></pre>
<p style="text-indent:2em">Event Time 的使用一定要指定数据源中的时间戳。 否则程序无法知道事件的事
件时间是什么(数据源里的数据没有时间戳的话， 就只能使用 Processing Time 了)。
我们看到上面的例子中创建了一个看起来有点复杂的类， 这个类实现的其实就
是分配时间戳的接口。 Flink 暴露了 TimestampAssigner 接口供我们实现， 使我们可
以自定义如何从事件数据中抽取时间戳。</p>
<pre><code class="language-java">// 创建 execution environment
final StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
// 通过连接 socket 获取输入数据，这里连接到本地9000端口，如果9000端口已被占用，请换一个端口
DataStream&lt;String&gt; text = env.socketTextStream(&quot;localhost&quot;, 9000, &quot;\n&quot;);
text.assignTimestampsAndWatermarks(new MyAssigner())
</code></pre>
<p>MyAssigner 有两种类型</p>
<ol>
<li>AssignerWithPeriodicWatermarks</li>
<li>AssignerWithPunctuatedWatermarks<br>
以上两个接口都继承自 TimestampAssigner。</li>
</ol>
<h3 id="assigner-with-periodic-watermarks">Assigner with periodic watermarks</h3>
<p>周期性的生成 watermark： 系统会周期性的将 watermark 插入到流中(水位线也<br>
是一种特殊的事件!)。 默认周期是 200 毫秒。 可以使用<br>
ExecutionConfig.setAutoWatermarkInterval()方法进行设置。</p>
<pre><code class="language-java">StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
env.setStreamTimeCharacteristic(TimeCharacteristic.EventTime)
// 每隔 5 秒产生一个 watermark
env.getConfig.setAutoWatermarkInterval(5000)
</code></pre>
<p style="text-indent:2em">产生 watermark 的逻辑： 每隔 5 秒钟， Flink 会调用
AssignerWithPeriodicWatermarks 的 getCurrentWatermark()方法。 如果方法返回一个
时间戳大于之前水位的时间戳， 新的 watermark 会被插入到流中。 这个检查保证了
水位线是单调递增的。 如果方法返回的时间戳小于等于之前水位的时间戳， 则不会
产生新的 watermark。</p>
<p>例子， 自定义一个周期性的时间戳抽取：</p>
<pre><code class="language-java">public class PeriodicAssigner extends
AssignerWithPeriodicWatermarks&lt;SensorReading&gt; {
    Long bound= 60 * 1000 // 延时为 1 分钟
    Long maxTs = Long.MinValue // 观察到的最大时间戳
    public Watermark  getCurrentWatermark() {
        new Watermark(maxTs - bound)
    } 
    public void extractTimestamp(SensorReading r,Long previousTS) = {
        maxTs = maxTs.max(r.timestamp)
        r.timestamp
    }
}
</code></pre>
<p style="text-indent:2em">一种简单的特殊情况是， 如果我们事先得知数据流的时间戳是单调递增的， 也就是说没有乱序， 那我们可以使用 assignAscendingTimestamps， 这个方法会直接使
用数据的时间戳生成 watermark。</p>
<pre><code class="language-java">DataStream&lt;SensorReading&gt; stream:  = ...
DataStream&lt;SensorReading&gt; withTimestampsAndWatermarks = stream
.assignAscendingTimestamps(e =&gt; e.timestamp)
&gt;&gt; result: E(1), W(1), E(2), W(2), ...
</code></pre>
<p>而对于乱序数据流， 如果我们能大致估算出数据流中的事件的最大延迟时间，<br>
就可以使用如下代码：</p>
<pre><code class="language-java">DataStream&lt;SensorReading&gt; stream:  = ...
DataStream&lt;SensorReading&gt; withTimestampsAndWatermarks = stream.assignTimestampsAndWatermarks(
    new SensorTimeAssigner()
) 
public class SensorTimeAssigner extends
BoundedOutOfOrdernessTimestampExtractor&lt;SensorReading&gt;(Time.seconds(5)) {
    // 抽取时间戳
    public Long  extractTimestamp(SensorReading r){
        return r.timestamp
    }
}
&gt;&gt; relust: E(10), W(0), E(8), E(7), E(11), W(1), ...
</code></pre>
<h3 id="assigner-with-punctuated-watermarks">Assigner with punctuated watermarks</h3>
<p style="text-indent:2em">间断式地生成 watermark。 和周期性生成的方式不同， 这种方式不是固定时间的，而是可以根据需要对每条数据进行筛选和处理。 直接上代码来举个例子， 我们只给
sensor_1 的传感器的数据流插入 watermark：</p>
<pre><code class="language-java">public class PunctuatedAssigner extends
    AssignerWithPunctuatedWatermarks&lt;SensorReading&gt; {
    Long bound=60 * 1000
    public  Watermark checkAndGetNextWatermark(SensorReading r , Long extractedTS) {
        if (r.id == &quot;sensor_1&quot;) {
            new Watermark(extractedTS - bound)
        } else {
            null
         }
    } 
    public  Long extractTimestamp(SensorReading r, Long previousTS){
        return  r.timestamp
    }
}
</code></pre>
<h2 id="evnettime-在-window-中的使用">EvnetTime 在 window 中的使用</h2>
<h3 id="滚动窗口tumblingeventtimewindows">滚动窗口（TumblingEventTimeWindows）</h3>
<pre><code class="language-java">public class EventTimeTumblingWindowAllDemo {
    public static void main(String[] args) throws Exception {
        //  2021-03-06 21:00:00,1
        //  2021-03-06 21:00:05,2
        // 结果 ： 2&gt; 1
        //        3&gt; 2
        StreamExecutionEnvironment env = StreamExecutionEnvironment.createLocalEnvironmentWithWebUI(new Configuration());
        // 老版本必须要设置时间标准 （1.20 之前的）
        env.setStreamTimeCharacteristic(TimeCharacteristic.EventTime);
        DataStreamSource&lt;String&gt; lines = env.socketTextStream(&quot;linux01&quot;, 8888);
       // flink里面的时间都精确到毫秒
       //  时间提取器 BoundedOutOfOrdernessTimestampExtractor : 允许时间乱序，并且可以指定窗口延时
       // WaterMark （水位线，可以让窗口延迟触发的一种机制）
       // 一个窗口中的一个分区的水位线 = 当前窗口当前分区最大的 EventTime - 延迟时间     Time.seconds(0)
        SingleOutputStreamOperator&lt;String&gt; linesWithWaterMark = lines.assignTimestampsAndWatermarks(new BoundedOutOfOrdernessTimestampExtractor&lt;String&gt;(Time.seconds(0)) {

            private SimpleDateFormat dateFormat = new SimpleDateFormat(&quot;yyyy-MM-dd HH:mm:ss&quot;) ;
            @Override
            public long extractTimestamp(String s) {
                String strings = s.split(&quot;,&quot;)[0];
                long timestamp = 0;
                try {
                    Date date = dateFormat.parse(strings);
                    timestamp = date.getTime();
                } catch (ParseException e) {
                    e.printStackTrace();
                    timestamp = System.currentTimeMillis();
                }
                return timestamp;
            }
        });

        SingleOutputStreamOperator&lt;Integer&gt; nums = linesWithWaterMark.map(new MapFunction&lt;String, Integer&gt;() {
            @Override
            public Integer map(String s) throws Exception {
                int i = Integer.parseInt(s.split(&quot;,&quot;)[1]);
                return i;
            }
        });
        //  划分窗口
        AllWindowedStream&lt;Integer, TimeWindow&gt; window = nums.windowAll(TumblingProcessingTimeWindows.of(Time.seconds(5)));
        // 对 window 中的数据 进行聚合
        SingleOutputStreamOperator&lt;Integer&gt; sum = window.sum(0);

        sum.print();
        env.execute() ;


    }
}
</code></pre>
<p>结果是按照 Event Time 的时间窗口计算得出的， 而无关系统的时间（ 包括输入的快慢） 。</p>
<h3 id="滑动窗口slidingeventtimewindows">滑动窗口（SlidingEventTimeWindows）</h3>
<pre><code class="language-java">public class EventTimeSlidingWindowDemo {
    public static void main(String[] args) throws Exception {

        StreamExecutionEnvironment env = StreamExecutionEnvironment.createLocalEnvironmentWithWebUI(new Configuration());
         //  两秒 调一次方法
        env.getConfig().setAutoWatermarkInterval(1000);
        // 老版本必须要设置时间标准 （1.20 之前的）
        env.setStreamTimeCharacteristic(TimeCharacteristic.EventTime);
        DataStreamSource&lt;String&gt; lines = env.socketTextStream(&quot;linux01&quot;, 8888);
       // flink里面的时间都精确到毫秒
       //  时间提取器 BoundedOutOfOrdernessTimestampExtractor : 允许时间乱序，并且可以指定窗口延时
       // WaterMark （水位线，可以让窗口延迟触发的一种机制）
       // 一个窗口中的一个分区的水位线 = 当前窗口当前分区最大的 EventTime - 延迟时间     Time.seconds(0)
        SingleOutputStreamOperator&lt;String&gt; linesWithWaterMark = lines.assignTimestampsAndWatermarks(new BoundedOutOfOrdernessTimestampExtractor&lt;String&gt;(Time.seconds(0)) {


            @Override
            public long extractTimestamp(String s) {
                return Long.parseLong(s.split(&quot;,&quot;)[0]); // EventTime
            }
        });
       //   提取完 EventTime 后生成 WaterMark ，但数据还是原来的老样子
       // 1000,spark,1 --&gt; spark,1
        SingleOutputStreamOperator &lt;Tuple2&lt;String,Integer&gt;&gt; WordAndCount = linesWithWaterMark.map(new MapFunction&lt;String, Tuple2&lt;String,Integer&gt;&gt;() {

            @Override
            public Tuple2&lt;String,Integer&gt; map(String s) throws Exception {
                String[] split = s.split(&quot;,&quot;);
                return Tuple2.of(split[1],Integer.parseInt(split[2]));
            }
        });

        //  先 keyBy，再划分窗口
        KeyedStream&lt;Tuple2&lt;String, Integer&gt;, String&gt; keyed = WordAndCount.keyBy(t -&gt; t.f0);

        //   划分窗口
        WindowedStream&lt;Tuple2&lt;String, Integer&gt;, String, TimeWindow&gt; window = keyed.window(SlidingEventTimeWindows.of(Time.seconds(10), Time.seconds(5)));
         //  对窗口里面的数据进行 sum
        SingleOutputStreamOperator&lt;Tuple2&lt;String, Integer&gt;&gt; sum = window.sum(1);
        sum.print();
        env.execute() ;
    }
}
</code></pre>
<h3 id="会话窗口eventtimesessionwindows">会话窗口（EventTimeSessionWindows）</h3>
<p style="text-indent:2em">相邻两次数据的 EventTime 的时间差超过指定的时间间隔就会触发执行。 如果加入 Watermark， 会在符合窗口触发的情况下进行延迟。 到达延迟水位再进行窗口
触发。</p>
<pre><code class="language-java">public class EventTimeSessionWindowDemo {
    public static void main(String[] args) throws Exception {

        StreamExecutionEnvironment env = StreamExecutionEnvironment.createLocalEnvironmentWithWebUI(new Configuration());
         //  两秒 调一次方法
        env.getConfig().setAutoWatermarkInterval(1000);
        // 老版本必须要设置时间标准 （1.20 之前的）
        env.setStreamTimeCharacteristic(TimeCharacteristic.EventTime);
        DataStreamSource&lt;String&gt; lines = env.socketTextStream(&quot;linux01&quot;, 8888);
       // flink里面的时间都精确到毫秒
       //  时间提取器 BoundedOutOfOrdernessTimestampExtractor : 允许时间乱序，并且可以指定窗口延时
       // WaterMark （水位线，可以让窗口延迟触发的一种机制）
       // 一个窗口中的一个分区的水位线 = 当前窗口当前分区最大的 EventTime - 延迟时间     Time.seconds(0)
        SingleOutputStreamOperator&lt;String&gt; linesWithWaterMark = lines.assignTimestampsAndWatermarks(new BoundedOutOfOrdernessTimestampExtractor&lt;String&gt;(Time.seconds(0)) {


            @Override
            public long extractTimestamp(String s) {
                return Long.parseLong(s.split(&quot;,&quot;)[0]); // EventTime
            }
        });
       //   提取完 EventTime 后生成 WaterMark ，但数据还是原来的老样子
       // 1000,spark,1 --&gt; spark,1
        SingleOutputStreamOperator &lt;Tuple2&lt;String,Integer&gt;&gt; WordAndCount = linesWithWaterMark.map(new MapFunction&lt;String, Tuple2&lt;String,Integer&gt;&gt;() {

            @Override
            public Tuple2&lt;String,Integer&gt; map(String s) throws Exception {
                String[] split = s.split(&quot;,&quot;);
                return Tuple2.of(split[1],Integer.parseInt(split[2]));
            }
        });

        //  先 keyBy，再划分窗口
        KeyedStream&lt;Tuple2&lt;String, Integer&gt;, String&gt; keyed = WordAndCount.keyBy(t -&gt; t.f0);

        //   划分窗口
        WindowedStream&lt;Tuple2&lt;String, Integer&gt;, String, TimeWindow&gt; window = keyed.window(EventTimeSessionWindows.withGap(Time.seconds(5)));
         //  对窗口里面的数据进行 sum
        SingleOutputStreamOperator&lt;Tuple2&lt;String, Integer&gt;&gt; sum = window.sum(1);

        sum.print();
        env.execute() ;


    }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[第六章 Flink 的Window 操作]]></title>
        <id>https://tianxiawuhao.github.io/post/5uA_yv-aO/</id>
        <link href="https://tianxiawuhao.github.io/post/5uA_yv-aO/">
        </link>
        <updated>2021-04-13T02:30:05.000Z</updated>
        <content type="html"><![CDATA[<p>Window是无限数据流处理的核心，Window将一个无限的stream拆分成有限大小的”buckets”桶，我们可以在这些桶上做计算操作。本文主要聚焦于在Flink中如何进行窗口操作，以及程序员如何从window提供的功能中获得最大的收益。<br>
　　窗口化的Flink程序的一般结构如下，第一个代码段中是分组的流，而第二段是非分组的流。正如我们所见，唯一的区别是分组的stream调用<code>keyBy(…)</code>和<code>window(…)</code>，而非分组的stream中<code>window()</code>换成了<code>windowAll(…)</code>，这些也将贯穿都这一页的其他部分中。</p>
<h2 id="keyed-windows">Keyed Windows</h2>
<pre><code class="language-java">stream.keyBy(...)           &lt;-  keyed versus non-keyed windows
       .window(...)         &lt;-  required: &quot;assigner&quot;
      [.trigger(...)]       &lt;-  optional: &quot;trigger&quot; (else default trigger)
      [.evictor(...)]       &lt;-  optional: &quot;evictor&quot; (else no evictor)
      [.allowedLateness()]  &lt;-  optional, else zero
       .reduce/fold/apply() &lt;-  required: &quot;function&quot;
</code></pre>
<h2 id="non-keyed-windows">Non-Keyed Windows</h2>
<pre><code class="language-java">stream.windowAll(...)       &lt;-  required: &quot;assigner&quot;
      [.trigger(...)]       &lt;-  optional: &quot;trigger&quot; (else default trigger)
      [.evictor(...)]       &lt;-  optional: &quot;evictor&quot; (else no evictor)
      [.allowedLateness()]  &lt;-  optional, else zero
       .reduce/fold/apply() &lt;-  required: &quot;function&quot;
</code></pre>
<p>在上面的例子中，方括号[]内的命令是可选的，这表明Flink允许你根据最符合你的要求来定义自己的window逻辑。</p>
<h2 id="window-的生命周期">Window 的生命周期</h2>
<p>简单地说,当一个属于<code>window</code>的元素到达之后这个<code>window</code>就创建了,而当当前时间(事件或者处理时间)为<code>window</code>的创建时间跟用户指定的延迟时间相加时,窗口将被彻底清除。Flink 确保了只清除基于时间的<code>window</code>,其他类型的<code>window</code>不清除,例如:全局<code>window</code>。例如:对于一个每5分钟创建无覆盖的(即 翻滚窗口)窗口,允许一个1分钟的时延的窗口策略，Flink将会在12:00到12:05这段时间内第一个元素到达时创建窗口,当水印通过12:06时,移除这个窗口。<br>
　　此外,每个 <code>Window</code>都有一个<code>Trigger</code>和一个附属于 Window 的函数(例如: <code>WindowFunction</code>, <code>ReduceFunction</code> 及 <code>FoldFunction</code>)，函数里包含了应用于<code>窗口(Window)</code>内容的计算，而<code>Trigger(触发器)</code>则指定了函数在什么条件下可被应用(函数何时被触发),一个触发策略可以是 &quot;当窗口中的元素个数超过4个时&quot; 或者 &quot;当水印达到窗口的边界时&quot;。触发器还可以决定在窗口创建和删除之间的任意时刻清除窗口的内容,本例中的清除仅指清除窗口的内容而不是窗口的元数据,也就是说新的数据还是可以被添加到当前的<code>window</code>中。<br>
　　除了上面的提到之外，你还可以指定一个<code>驱逐者</code>, <code>Evictor</code>将在触发器触发之后或者在函数被应用之前或者之后，清楚窗口中的元素。<br>
　　接下来我们将更深入的去了解上述的部件，我们从上述片段的主要部分开始(如:<code>Keyed vs Non-Keyed Windows</code>, <code>Window Assigner</code>, 及 <code>Window Function</code>),然后是可选部分。</p>
<h2 id="分组和非分组windows-keyed-vs-non-keyed-windows">分组和非分组Windows (Keyed vs Non-Keyed Windows)</h2>
<p>首先，第一件事是指定你的数据流是分组的还是未分组的，这个必须在定义<code>window</code> 之前指定好。使用 <code>keyBy(...)</code>会将你的无限数据流拆分成逻辑分组的数据流，如果 <code>keyBy(...)</code> 函数不被调用的话，你的数据流将不是分组的。<br>
　　在分组数据流中,任何正在传入的事件的属性都可以被当做key,分组数据流将你的window计算通过多任务并发执行，以为每一个逻辑分组流在执行中与其他的逻辑分组流是独立地进行的。<br>
　　在非分组数据流中，你的原始数据流并不会拆分成多个逻辑流并且所有的window逻辑将在一个任务中执行，并发度为1。</p>
<h2 id="窗口分配器window-assingers">窗口分配器(Window Assingers)</h2>
<p>指定完你的数据流是分组的还是非分组的之后，接下来你需要定义一个<code>窗口分配器(window assigner)</code>，窗口分配器定义了元素如何分配到窗口中，这是通过在分组数据流中调用window(...)或者非分组数据流中调用<code>windowAll(...)</code>时你选择的窗口分配器<code>(WindowAssigner)</code>来指定的。<code>WindowAssigner</code>是负责将每一个到来的元素分配给一个或者多个<code>窗口(window)</code>,Flink 提供了一些常用的预定义窗口分配器，即:<code>滚动窗口</code>、<code>滑动窗口</code>、<code>会话窗口</code>和<code>全局窗口</code>。你也可以通过继承<code>WindowAssigner</code>类来自定义自己的窗口。所有的内置窗口分配器(除了全局窗口 <code>global window</code>)都是通过时间来分配元素到窗口中的，这个时间要么是处理的时间，要么是事件发生的时间。请看一下我们的 <code>event time (https://ci.apache.org/projects/flink/flink-docs-release-1.3/dev/event_time.html )</code>部分来了解更多处理时间和事件时间的区别及<code>时间戳(timestamp)</code>和<code>水印(watermark)</code>是如何产生的。<br>
　　接下来我们将展示Flink的预定义窗口分配器是如何工作的，以及它们在<code>DataStream</code>程序中是如何使用的。接下来我们将展示Flink的预定义窗口分配器是如何工作的，以及它们在<code>DataStream</code>程序中是如何使用的。下图中展示了每个分配器是如何工作的，紫色圆圈代表着数据流中的一个元素，这些元素是通过一些key进行分区(在本例中是 user1,user2,user3), X轴显示的是时间进度。</p>
<h3 id="滚动窗口">滚动窗口</h3>
<p>滚动窗口分配器将每个元素分配的一个指定窗口大小的窗口中，滚动窗口有一个固定的大小，并且不会出现重叠。例如:如果你指定了一个5分钟大小的滚动窗口，当前窗口将被评估并将按下图说明每5分钟创建一个新的窗口。<br>
<img src="https://tianxiawuhao.github.io/post-images/1618886583930.png" alt="" loading="lazy"><br>
下面的代码片段展示了如何使用滚动窗口。</p>
<p>Java 代码</p>
<pre><code class="language-java">DataStream&lt;T&gt; input = ...;
</code></pre>
<p>滚动事件时间窗口( tumbling event-time windows )</p>
<pre><code class="language-java">input
    .keyBy(&lt;key selector&gt;)
    .window(TumblingEventTimeWindows.of(Time.seconds(5)))
    .&lt;windowed transformation&gt;(&lt;window function&gt;); 
</code></pre>
<p>滚动处理时间窗口(tumbling processing-time windows)</p>
<pre><code class="language-java">input
    .keyBy(&lt;key selector&gt;)
    .window(TumblingProcessingTimeWindows.of(Time.seconds(5)))
    .&lt;windowed transformation&gt;(&lt;window function&gt;);
</code></pre>
<p>每日偏移8小时的滚动事件时间窗口(daily tumbling event-time windows offset by -8 hours. )</p>
<pre><code class="language-java">input
    .keyBy(&lt;key selector&gt;)
    .window(TumblingEventTimeWindows.of(Time.days(1), Time.hours(-8)))
    .&lt;windowed transformation&gt;(&lt;window function&gt;);
</code></pre>
<p>Scala 代码:</p>
<pre><code class="language-java">val input:DataStream[T] =  
</code></pre>
<p>滚动事件时间窗口(tumbling event-time windows)</p>
<pre><code class="language-java">input
    .keyBy(&lt;key selector&gt;)
    .window(TumblingEventTimeWindows.of(Time.seconds(5)))
    .&lt;windowed transformation&gt;(&lt;window function&gt;)
</code></pre>
<p>滚动处理时间窗口(tumbling processing-time windows)</p>
<pre><code class="language-java">input
    .keyBy(&lt;key selector&gt;)
    .window(TumblingProcessingTimeWindows.of(Time.seconds(5)))
    .&lt;windowed transformation&gt;(&lt;window function&gt;)
</code></pre>
<p>每日偏移8小时的滚动事件时间窗口(daily tumbling event-time windows offset by -8 hours. )</p>
<pre><code class="language-java">input
    .keyBy(&lt;key selector&gt;)
    .window(TumblingEventTimeWindows.of(Time.days(1), Time.hours(-8)))
    .&lt;windowed transformation&gt;(&lt;window function&gt;)
</code></pre>
<p>时间间隔可以通过<code>Time.milliseconds(x)</code>，<code>Time.seconds(x)</code>，<code>Time.minutes(x)</code>等其中的一个来指定。<br>
在上面最后的例子中，滚动窗口分配器还接受了一个可选的偏移参数，可以用来改变窗口的排列。例如，没有偏移的话按小时的滚动窗口将按时间纪元来对齐，也就是说你将一个如: 1:00:00.000-1:59:59.999,2:00:00.000-2:59:59.999等，如果你想改变一下，你可以指定一个偏移，如果你指定了一个15分钟的偏移，你将得到1:15:00.000-2:14:59.999,2:15:00.000-3:14:59.999等。时间偏移一个很大的用处是用来调准非0时区的窗口，例如:在中国你需要指定一个8小时的时间偏移。</p>
<h3 id="滑动窗口sliding-windows">滑动窗口(Sliding Windows)</h3>
<p>滑动窗口分配器将元素分配到固定长度的窗口中，与滚动窗口类似，窗口的大小由窗口大小参数来配置，另一个窗口滑动参数控制滑动窗口开始的频率。因此，滑动窗口如果滑动参数小于滚动参数的话，窗口是可以重叠的，在这种情况下元素会被分配到多个窗口中。<br>
　　例如，你有10分钟的窗口和5分钟的滑动，那么每个窗口中5分钟的窗口里包含着上个10分钟产生的数据，如下图所示:<br>
<img src="https://tianxiawuhao.github.io/post-images/1618886623719.png" alt="" loading="lazy"><br>
下面的代码片段中展示了如何使用滑动窗口:</p>
<p>Java 代码:</p>
<pre><code class="language-java">DataStream&lt;T&gt; input = ...;
</code></pre>
<p>滑动事件时间窗口</p>
<pre><code class="language-java">input
    .keyBy(&lt;key selector&gt;)
    .window(SlidingEventTimeWindows.of(Time.seconds(10), Time.seconds(5)))
    .&lt;windowed transformation&gt;(&lt;window function&gt;);
</code></pre>
<p>滑动处理时间窗口</p>
<pre><code class="language-java">input
    .keyBy(&lt;key selector&gt;)
    .window(SlidingProcessingTimeWindows.of(Time.seconds(10), Time.seconds(5)))
    .&lt;windowed transformation&gt;(&lt;window function&gt;);
</code></pre>
<p>偏移8小时的滑动处理时间窗口(sliding processing-time windows offset by -8 hours)</p>
<pre><code class="language-java">input
    .keyBy(&lt;key selector&gt;)
    .window(SlidingProcessingTimeWindows.of(Time.hours(12), Time.hours(1), Time.hours(-8)))
    .&lt;windowed transformation&gt;(&lt;window function&gt;);
</code></pre>
<p>Scala 代码:</p>
<pre><code class="language-java">val input: DataStream[T] = ...
</code></pre>
<p>滑动事件时间窗口(sliding event-time windows)</p>
<pre><code class="language-java">input
    .keyBy(&lt;key selector&gt;)
    .window(SlidingEventTimeWindows.of(Time.seconds(10), Time.seconds(5)))
    .&lt;windowed transformation&gt;(&lt;window function&gt;)
</code></pre>
<p>滑动处理时间窗口(sliding processing-time windows)</p>
<pre><code class="language-java">input
    .keyBy(&lt;key selector&gt;)
    .window(SlidingProcessingTimeWindows.of(Time.seconds(10), Time.seconds(5)))
    .&lt;windowed transformation&gt;(&lt;window function&gt;)
</code></pre>
<p>偏移8小时的滑动处理时间窗口(sliding processing-time windows offset by -8 hours)</p>
<pre><code class="language-java">input
    .keyBy(&lt;key selector&gt;)
    .window(SlidingProcessingTimeWindows.of(Time.hours(12), Time.hours(1), Time.hours(-8)))
    .&lt;windowed transformation&gt;(&lt;window function&gt;)
</code></pre>
<p>时间间隔可以通过Time.milliseconds(x),Time.seconds(x),Time.minutes(x)等来指定。<br>
　　正如上述例子所示，滑动窗口分配器也有一个可选的偏移参数来改变窗口的对齐。例如，没有偏移参数，按小时的窗口，有30分钟的滑动，将根据时间纪元来对齐，也就是说你将得到如下的窗口1:00:00.000-1:59:59.999,1:30:00.000-2:29:59.999等。而如果你想改变窗口的对齐，你可以给定一个偏移，如果给定一个15分钟的偏移，你将得到如下的窗口:1:15:00.000-2:14.59.999,　1:45:00.000-2:44:59.999等。时间偏移一个很大的用处是用来调准非0时区的窗口，例如:在中国你需要指定一个8小时的时间偏移。</p>
<h3 id="会话窗口session-windows">会话窗口(Session Windows)</h3>
<p>session窗口分配器通过session活动来对元素进行分组，session窗口跟滚动窗口和滑动窗口相比，不会有重叠和固定的开始时间和结束时间的情况。相反，当它在一个固定的时间周期内不再收到元素，即非活动间隔产生，那个这个窗口就会关闭。一个session窗口通过一个session间隔来配置，这个session间隔定义了非活跃周期的长度。当这个非活跃周期产生，那么当前的session将关闭并且后续的元素将被分配到新的session窗口中去。</p>
<figure data-type="image" tabindex="1"><img src="https://tianxiawuhao.github.io/post-images/1618886669102.png" alt="" loading="lazy"></figure>
<p>下面的代码片段中展示了如何使用session窗口<br>
Java代码:</p>
<pre><code class="language-java">DataStream&lt;T&gt; input = ...;
</code></pre>
<p>事件时间会话窗口(event-time session windows)</p>
<pre><code class="language-java">input
    .keyBy(&lt;key selector&gt;)
    .window(EventTimeSessionWindows.withGap(Time.minutes(10)))
    .&lt;windowed transformation&gt;(&lt;window function&gt;);
</code></pre>
<p>处理时间会话窗口(processing-time session windows)</p>
<pre><code class="language-java">input
    .keyBy(&lt;key selector&gt;)
    .window(ProcessingTimeSessionWindows.withGap(Time.minutes(10)))
    .&lt;windowed transformation&gt;(&lt;window function&gt;);
</code></pre>
<p>Scala代码:</p>
<pre><code class="language-java">val input: DataStream[T] = ...
</code></pre>
<p>事件时间会话窗口(event-time session windows)</p>
<pre><code class="language-java">input
    .keyBy(&lt;key selector&gt;)
    .window(EventTimeSessionWindows.withGap(Time.minutes(10)))
    .&lt;windowed transformation&gt;(&lt;window function&gt;)
</code></pre>
<p>处理时间会话窗口(processing-time session windows)</p>
<pre><code class="language-java">input
    .keyBy(&lt;key selector&gt;)
    .window(ProcessingTimeSessionWindows.withGap(Time.minutes(10)))
    .&lt;windowed transformation&gt;(&lt;window function&gt;)
</code></pre>
<p>时间间隔可以通过<code>Time.milliseconds(x),Time.seconds(x),Time.minutes(x)</code>等来指定。<br>
注意: 因为session看窗口没有一个固定的开始和结束，他们的评估与滑动窗口和滚动窗口不同。在内部，session操作为每一个到达的元素创建一个新的窗口，并合并间隔时间小于指定非活动间隔的窗口。为了进行合并，session窗口的操作需要指定一个<code>合并触发器(Trigger)</code>和一个<code>合并窗口函数(Window Function)</code>,如:<code>ReduceFunction</code>或者<code>WindowFunction</code>(FoldFunction不能合并)。</p>
<p>###全局窗口(Global Windows)<br>
全局窗口分配器将所有具有相同key的元素分配到同一个全局窗口中，这个窗口模式仅适用于用户还需自定义触发器的情况。否则，由于全局窗口没有一个自然的结尾，无法执行元素的聚合，将不会有计算被执行。<br>
<img src="https://tianxiawuhao.github.io/post-images/1618886845541.png" alt="" loading="lazy"><br>
下面的代码片段展示了如何使用全局窗口:<br>
Java 代码:</p>
<pre><code class="language-java">DataStream&lt;T&gt; input = ...;
input
    .keyBy(&lt;key selector&gt;)
    .window(GlobalWindows.create())
    .&lt;windowed transformation&gt;(&lt;window function&gt;);
</code></pre>
<p>Scala代码:</p>
<pre><code class="language-java">val input: DataStream[T] = ...
input
    .keyBy(&lt;key selector&gt;)
    .window(GlobalWindows.create())
    .&lt;windowed transformation&gt;(&lt;window function&gt;)
</code></pre>
<h2 id="窗口函数window-functions">窗口函数(Window Functions)</h2>
<p>定义完窗口分配器后，我们还需要为每一个窗口指定我们需要执行的计算，这是窗口的责任，当系统决定一个窗口已经准备好执行之后，这个窗口函数将被用来处理窗口中的每一个元素(可能是分组的)。请参考:<code>https://ci.apache.org/projects/flink/flink-docs-release-1.3/dev/windows.html#triggers</code> 来了解当一个窗口准备好之后，Flink是如何决定的。<br>
　　window函数可以是<code>ReduceFunction</code>, <code>FoldFunction</code> 或者 <code>WindowFunction</code> 中的一个。前面两个更高效一些(),因为在每个窗口中增量地对每一个到达的元素执行聚合操作。一个 <code>WindowFunction</code> 可以获取一个窗口中的所有元素的一个迭代以及哪个元素属于哪个窗口的额外元信息。<br>
　　有<code>WindowFunction</code>的窗口化操作会比其他的操作效率要差一些，因为Flink内部在调用函数之前会将窗口中的所有元素都缓存起来。这个可以通过<code>WindowFunction</code>和<code>ReduceFunction</code>或者<code>FoldFunction</code>结合使用来获取窗口中所有元素的增量聚合和<code>WindowFunction</code>接收的额外的窗口元数据，接下来我们将看一看每一种变体的示例。</p>
<h3 id="reducefunction">ReduceFunction</h3>
<p><code>ReduceFunction</code>指定了如何通过两个输入的参数进行合并输出一个同类型的参数的过程，Flink使用<code>ReduceFunction</code>来对窗口中的元素进行增量聚合。<br>
　　一个<code>ReduceFunction</code> 可以通过如下的方式来定义和使用:<br>
Java 代码:</p>
<pre><code class="language-java">DataStream&lt;Tuple2&lt;String, Long&gt;&gt; input = ...;
 input
    .keyBy(&lt;key selector&gt;)
    .window(&lt;window assigner&gt;)
    .reduce(new ReduceFunction&lt;Tuple2&lt;String, Long&gt;&gt; {
      public Tuple2&lt;String, Long&gt; reduce(Tuple2&lt;String, Long&gt; v1, Tuple2&lt;String, Long&gt; v2) {
        return new Tuple2&lt;&gt;(v1.f0, v1.f1 + v2.f1);
      }
    });
</code></pre>
<p>Scala 代码:</p>
<pre><code class="language-java">val input: DataStream[(String, Long)] = ...
 input
    .keyBy(&lt;key selector&gt;)
    .window(&lt;window assigner&gt;)
    .reduce { (v1, v2) =&gt; (v1._1, v1._2 + v2._2) }
</code></pre>
<p>上面的例子是将窗口所有元素中元组的第二个属性进行累加操作。</p>
<h3 id="foldfunction">FoldFunction</h3>
<p><code>FoldFunction</code> 指定了一个输入元素如何与一个输出类型的元素合并的过程，这个<code>FoldFunction</code> 会被每一个加入到窗口中的元素和当前的输出值增量地调用，第一个元素是与一个预定义的类型为输出类型的初始值合并。<br>
　　一个<code>FoldFunction</code>可以通过如下的方式定义和调用:<br>
Java 代码:</p>
<pre><code class="language-java">DataStream&lt;Tuple2&lt;String, Long&gt;&gt; input = ...;
 input
    .keyBy(&lt;key selector&gt;)
    .window(&lt;window assigner&gt;)
    .fold(&quot;&quot;, new FoldFunction&lt;Tuple2&lt;String, Long&gt;, String&gt;&gt; {
       public String fold(String acc, Tuple2&lt;String, Long&gt; value) {
         return acc + value.f1;
       }
    });
</code></pre>
<p>Scala 代码:</p>
<pre><code class="language-java"> val input: DataStream[(String, Long)] = ...
 input
    .keyBy(&lt;key selector&gt;)
    .window(&lt;window assigner&gt;)
    .fold(&quot;&quot;) { (acc, v) =&gt; acc + v._2 }
</code></pre>
<p>上面例子追加所有输入的长整型到一个空的字符串中。<br>
注意 fold()不能应用于回话窗口或者其他可合并的窗口中。</p>
<h2 id="窗口函数-一般用法windowfunction-the-generic-case">窗口函数 —— 一般用法(WindowFunction - The Generic Case)</h2>
<p>一个<code>WindowFunction</code>将获得一个包含了<code>window</code>中的所有<code>元素迭代(Iterable)</code>，并且提供所有窗口函数的最大灵活性。这些带来了性能的成本和资源的消耗，因为<code>window</code>中的元素无法进行增量迭代，而是缓存起来直到<code>window</code>被认为是可以处理时为止。<br>
<code>WindowFunction</code>的使用说明如下:<br>
Java 代码:</p>
<pre><code class="language-java">public interface WindowFunction&lt;IN, OUT, KEY, W extends Window&gt; extends Function, Serializable {
  /**
   // Evaluates the window and outputs none or several elements.
   // @param key The key for which this window is evaluated.
  // @param window The window that is being evaluated.
   // @param input The elements in the window being evaluated.
   // @param out A collector for emitting elements.
  // @throws Exception The function may throw exceptions to fail the program and trigger recovery.
  */
  void apply(KEY key, W window, Iterable&lt;IN&gt; input, Collector&lt;OUT&gt; out) throws Exception;
}
</code></pre>
<p>Scala 代码:</p>
<pre><code class="language-java">trait WindowFunction[IN, OUT, KEY, W &lt;: Window] extends Function with Serializable {
  /**
    // Evaluates the window and outputs none or several elements.
    //
    // @param key    The key for which this window is evaluated.
    // @param window The window that is being evaluated.
    // @param input  The elements in the window being evaluated.
    // @param out    A collector for emitting elements.
    // @throws Exception The function may throw exceptions to fail the program and trigger recovery.
    */
  def apply(key: KEY, window: W, input: Iterable[IN], out: Collector[OUT])
}
</code></pre>
<p>一个<code>WindowFunction</code>可以按如下方式来定义和使用:<br>
Java 代码:</p>
<pre><code class="language-java">DataStream&lt;Tuple2&lt;String, Long&gt;&gt; input = ...;
 input
    .keyBy(&lt;key selector&gt;)
    .window(&lt;window assigner&gt;)
    .apply(new MyWindowFunction());
/* ... */
public class MyWindowFunction implements WindowFunction&lt;Tuple&lt;String, Long&gt;, String, String, TimeWindow&gt; {
  void apply(String key, TimeWindow window, Iterable&lt;Tuple&lt;String, Long&gt;&gt; input, Collector&lt;String&gt; out) {
    long count = 0;
    for (Tuple&lt;String, Long&gt; in: input) {
      count++;
    }
    out.collect(&quot;Window: &quot; + window + &quot;count: &quot; + count);
  }
}
</code></pre>
<p>Scala 代码:</p>
<pre><code class="language-java">val input: DataStream[(String, Long)] = ...
input
    .keyBy(&lt;key selector&gt;)
    .window(&lt;window assigner&gt;)
    .apply(new MyWindowFunction())
/* ... */
class MyWindowFunction extends WindowFunction[(String, Long), String, String, TimeWindow] {
  def apply(key: String, window: TimeWindow, input: Iterable[(String, Long)], out: Collector[String]): () = {
    var count = 0L
    for (in &lt;- input) {
      count = count + 1
    }
    out.collect(s&quot;Window $window count: $count&quot;)
  }
}
</code></pre>
<p>上面的例子展示了统计一个<code>window</code>中元素个数的<code>WindowFunction</code>，此外，还将<code>window</code>的信息添加到输出中。<br>
注意:使用<code>WindowFunction</code>来做简单的聚合操作如计数操作，性能是相当差的。下一章节我们将展示如何将<code>ReduceFunction</code>跟<code>WindowFunction</code>结合起来，来获取增量聚合和添加到<code>WindowFunction</code>中的信息。</p>
<h3 id="processwindowfunction">ProcessWindowFunction</h3>
<p>在使用<code>WindowFunction</code>的地方你也可以用<code>ProcessWindowFunction</code>，这跟<code>WindowFunction</code>很类似，除了接口允许查询跟多关于<code>context</code>的信息，<code>context</code>是<code>window</code>评估发生的地方。<br>
下面是<code>ProcessWindowFunction</code>的接口:<br>
Java 代码:</p>
<pre><code class="language-java">public abstract class ProcessWindowFunction&lt;IN, OUT, KEY, W extends Window&gt; implements Function {
    /**
     // Evaluates the window and outputs none or several elements.
     //
     // @param key The key for which this window is evaluated.
     // @param context The context in which the window is being evaluated.
     // @param elements The elements in the window being evaluated.
     // @param out A collector for emitting elements.
     //
     // @throws Exception The function may throw exceptions to fail the program and trigger recovery.
     */
    public abstract void process(
            KEY key,
            Context context,
            Iterable&lt;IN&gt; elements,
            Collector&lt;OUT&gt; out) throws Exception;
    /**
     // The context holding window metadata
     */
    public abstract class Context {
        /**
         // @return The window that is being evaluated.
         */
        public abstract W window();
    }
}
</code></pre>
<p>Scala 代码:</p>
<pre><code class="language-java">abstract class ProcessWindowFunction[IN, OUT, KEY, W &lt;: Window] extends Function {
  /**
    // Evaluates the window and outputs none or several elements.
    //
    // @param key      The key for which this window is evaluated.
    // @param context  The context in which the window is being evaluated.
    // @param elements The elements in the window being evaluated.
    // @param out      A collector for emitting elements.
    // @throws Exception The function may throw exceptions to fail the program and trigger recovery.
    */
  @throws[Exception]
  def process(
      key: KEY,
      context: Context,
      elements: Iterable[IN],
      out: Collector[OUT])
  /**
    // The context holding window metadata
    */
  abstract class Context {
    /**
      // @return The window that is being evaluated.
      */
    def window: W
  }
}
</code></pre>
<p><code>ProcessWindowFunction</code>可以通过如下方式调用:<br>
Java 代码:</p>
<pre><code class="language-java">DataStream&lt;Tuple2&lt;String, Long&gt;&gt; input = ...;
 input
    .keyBy(&lt;key selector&gt;)
    .window(&lt;window assigner&gt;)
    .process(new MyProcessWindowFunction());`
Scala 代码:
`val input: DataStream[(String, Long)] = ...
 input
    .keyBy(&lt;key selector&gt;)
    .window(&lt;window assigner&gt;)
    .process(new MyProcessWindowFunction())
</code></pre>
<p>有增量聚合功能的<code>WindowFunction (WindowFunction with Incremental Aggregation)</code><br>
<code>WindowFunction</code>可以跟<code>ReduceFunction</code>或者<code>FoldFunction</code>结合来增量地对到达<code>window</code>中的元素进行聚合，当<code>window</code>关闭之后，<code>WindowFunction</code>就能提供聚合结果。当获取到<code>WindowFunction</code>额外的<code>window</code>元信息后就可以进行增量计算窗口了。<br>
标注:你也可以使用<code>ProcessWindowFunction</code>替换<code>WindowFunction</code>来进行增量窗口聚合。</p>
<p>使用<code>FoldFunction</code> 进行<code>增量窗口聚合(Incremental Window Aggregation with FoldFunction)</code><br>
下面的例子展示了一个增量的<code>FoldFunction</code>如何跟一个<code>WindowFunction</code>结合，来获取窗口的事件数，并同时返回窗口的key和窗口的最后时间。<br>
Java 代码:</p>
<pre><code class="language-java">DataStream&lt;SensorReading&gt; input = ...;
input
  .keyBy(&lt;key selector&gt;)
  .timeWindow(&lt;window assigner&gt;)
  .fold(new Tuple3&lt;String, Long, Integer&gt;(&quot;&quot;,0L, 0), new MyFoldFunction(), new MyWindowFunction())
// Function definitions
private static class MyFoldFunction
    implements FoldFunction&lt;SensorReading, Tuple3&lt;String, Long, Integer&gt; &gt; {
  public Tuple3&lt;String, Long, Integer&gt; fold(Tuple3&lt;String, Long, Integer&gt; acc, SensorReading s) {
      Integer cur = acc.getField(2);
      acc.setField(2, cur + 1);
      return acc;
  }
}
private static class MyWindowFunction
    implements WindowFunction&lt;Tuple3&lt;String, Long, Integer&gt;, Tuple3&lt;String, Long, Integer&gt;, String, TimeWindow&gt; {
  public void apply(String key,
                    TimeWindow window,
                    Iterable&lt;Tuple3&lt;String, Long, Integer&gt;&gt; counts,
                    Collector&lt;Tuple3&lt;String, Long, Integer&gt;&gt; out) {
    Integer count = counts.iterator().next().getField(2);
    out.collect(new Tuple3&lt;String, Long, Integer&gt;(key, window.getEnd(),count));
  }
}
</code></pre>
<p>Scala 代码:</p>
<pre><code class="language-java">val input: DataStream[SensorReading] = ...
 input
 .keyBy(&lt;key selector&gt;)
 .timeWindow(&lt;window assigner&gt;)
 .fold (
    (&quot;&quot;, 0L, 0),
    (acc: (String, Long, Int), r: SensorReading) =&gt; { (&quot;&quot;, 0L, acc._3 + 1) },
    ( key: String,
      window: TimeWindow,
      counts: Iterable[(String, Long, Int)],
      out: Collector[(String, Long, Int)] ) =&gt;
      {
        val count = counts.iterator.next()
        out.collect((key, window.getEnd, count._3))
      }
  )
</code></pre>
<p>使用<code>ReduceFunction</code>进行<code>增量窗口聚合(Incremental Window Aggregation with ReduceFunction)</code><br>
下面例子展示了一个增量额<code>ReduceFunction</code>如何跟一个<code>WindowFunction</code>结合，来获取窗口中最小的事件和窗口的开始时间。<br>
Java 代码:</p>
<pre><code class="language-java">DataStream&lt;SensorReading&gt; input = ...;
input
  .keyBy(&lt;key selector&gt;)
  .timeWindow(&lt;window assigner&gt;)
  .reduce(new MyReduceFunction(), new MyWindowFunction());
// Function definitions
private static class MyReduceFunction implements ReduceFunction&lt;SensorReading&gt; {
  public SensorReading reduce(SensorReading r1, SensorReading r2) {
      return r1.value() &gt; r2.value() ? r2 : r1;
  }
}
private static class MyWindowFunction
    implements WindowFunction&lt;SensorReading, Tuple2&lt;Long, SensorReading&gt;, String, TimeWindow&gt; {
  public void apply(String key,
                    TimeWindow window,
                    Iterable&lt;SensorReading&gt; minReadings,
                    Collector&lt;Tuple2&lt;Long, SensorReading&gt;&gt; out) {
      SensorReading min = minReadings.iterator().next();
      out.collect(new Tuple2&lt;Long, SensorReading&gt;(window.getStart(), min));
  }
}
</code></pre>
<p>Scala 代码:</p>
<pre><code class="language-java">val input: DataStream[SensorReading] = ...
 input
  .keyBy(&lt;key selector&gt;)
  .timeWindow(&lt;window assigner&gt;)
  .reduce(
    (r1: SensorReading, r2: SensorReading) =&gt; { if (r1.value &gt; r2.value) r2 else r1 },
    ( key: String,
      window: TimeWindow,
      minReadings: Iterable[SensorReading],
      out: Collector[(Long, SensorReading)] ) =&gt;
      {
        val min = minReadings.iterator.next()
        out.collect((window.getStart, min))
      }
  )
</code></pre>
<h2 id="触发器triggers">触发器(Triggers)</h2>
<p>触发器决定了一个窗口何时可以被窗口函数处理，每一个窗口分配器都有一个默认的触发器，如果默认的触发器不能满足你的需要，你可以通过调用<code>trigger(...)</code>来指定一个自定义的触发器。触发器的接口有5个方法来允许触发器处理不同的事件:<br>
　　* <code>onElement()</code>方法,每个元素被添加到窗口时调用<br>
　　* <code>onEventTime()</code>方法,当一个已注册的事件时间计时器启动时调用<br>
　　* <code>onProcessingTime()</code>方法,当一个已注册的处理时间计时器启动时调用<br>
　　* <code>onMerge()</code>方法，与状态性触发器相关，当使用会话窗口时，两个触发器对应的窗口合并时，合并两个触发器的状态。<br>
　　* 最后一个<code>clear()</code>方法执行任何需要清除的相应窗口<br>
上面的方法中有两个需要注意的地方:<br>
1)第一、三通过返回一个TriggerResult来决定如何操作调用他们的事件，这些操作可以是下面操作中的一个；<br>
<code>CONTINUE</code>:什么也不做<br>
<code>FIRE</code>:触发计算<br>
<code>PURGE</code>:清除窗口中的数据<br>
<code>FIRE_AND_PURGE</code>:触发计算并清除窗口中的数据<br>
2)这些函数可以被用来为后续的操作注册处理时间定时器或者事件时间计时器</p>
<h3 id="触发和清除fire-and-purge">触发和清除(Fire and Purge)</h3>
<p>一旦一个触发器决定一个窗口已经准备好进行处理，它将触发并返回<code>FIRE</code>或者<code>FIRE_AND_PURGE</code>。这是窗口操作发送当前窗口结果的信号，给定一个拥有一个<code>WindowFunction</code>的窗口那么所有的元素都将发送到<code>WindowFunction</code>中(可能之后还会发送到驱逐器(Evitor)中)。有<code>ReduceFunction</code>或者<code>FoldFunction</code>的<code>Window</code>仅仅发送他们的急切聚合结果。<br>
　　当一个触发器触发时，它可以是FIRE或者FIRE_AND_PURGE，如果是FIRE的话，将保持window中的内容，FIRE_AND_PURGE的话，会清除window的内容。默认情况下，预实现的触发器仅仅是FIRE，不会清除window的状态。<br>
注意:清除操作仅清除window的内容，并留下潜在的窗口元信息和完整的触发器状态。</p>
<h3 id="窗口分配器默认的触发器default-triggers-of-windowassigners">窗口分配器默认的触发器(Default Triggers of WindowAssigners)</h3>
<p>默认的触发器适用于许多种情况，例如:所有的事件时间分配器都有一个<code>EventTimeTrigger</code>作为默认的触发器，这个触发器仅在当水印通过窗口的最后时间时触发。<br>
注意:<code>GlobalWindow</code>默认的触发器是<code>NeverTrigger</code>，是永远不会触发的，因此，如果你使用的是<code>GlobalWindow</code>的话，你需要定义一个自定义触发器。<br>
注意:通过调用<code>trigger(...)</code>来指定一个触发器你就重写了<code>WindowAssigner</code>的默认触发器。例如:如果你为<code>TumblingEventTimeWindows</code>指定了一个<code>CountTrigger</code>，你就不会再通过时间来获取触发了，而是通过计数。现在，如果你想通过时间和计数来触发的话，你需要写你自己自定义的触发器。</p>
<h3 id="内置的和自定义的触发器build-in-and-custom-triggers">内置的和自定义的触发器(Build-in and Custom Triggers)</h3>
<p>Flink有一些内置的触发器:<br>
　　*EventTimeTrigger(前面提到过)触发是根据由水印衡量的事件时间的进度来的<br>
　　*ProcessingTimeTrigger 根据处理时间来触发<br>
　　*CountTrigger 一旦窗口中的元素个数超出了给定的限制就会触发<br>
　　*PurgingTrigger 作为另一个触发器的参数并将它转换成一个清除类型<br>
如果你想实现一个自定义的触发器，你需要查看一下这个抽象类Trigger(https://github.com/apache/flink/blob/master//flink-streaming-java/src/main/java/org/apache/flink/streaming/api/windowing/triggers/Trigger.java ),请注意，这个API还在优化中，后续的Flink版本可能会改变。</p>
<h2 id="驱逐器evictors">驱逐器(Evictors)</h2>
<p>Flink的窗口模型允许指定一个除了<code>WindowAssigner</code>和<code>Trigger</code>之外的可选参数<code>Evitor</code>，这个可以通过调用<code>evitor(...)</code>方法(在这篇文档的开头展示过)来实现。这个驱逐器(evitor)可以在触发器触发之前或者之后，或者窗口函数被应用之前清理窗口中的元素。为了达到这个目的，Evitor接口有两个方法:</p>
<pre><code class="language-java">/**
 // Optionally evicts elements. Called before windowing function.
 //
 // @param elements The elements currently in the pane.
 // @param size The current number of elements in the pane.
 // @param window The {@link Window}
 // @param evictorContext The context for the Evictor
 ///
void evictBefore(Iterable&lt;TimestampedValue&lt;T&gt;&gt; elements, int size, W window, EvictorContext evictorContext);
/**
 // Optionally evicts elements. Called after windowing function.
 //
 // @param elements The elements currently in the pane.
 // @param size The current number of elements in the pane.
 // @param window The {@link Window}
 // @param evictorContext The context for the Evictor
 */
void evictAfter(Iterable&lt;TimestampedValue&lt;T&gt;&gt; elements, int size, W window, EvictorContext evictorContext);
</code></pre>
<p><code>evitorBefore()</code>方法包含了在<code>window function</code>之前被应用的驱逐逻辑，而<code>evitorAfter()</code>方法包含了在<code>window function</code>之后被应用的驱逐逻辑。在<code>window function</code>应用之前被驱逐的元素将不会再被<code>window function</code>处理。<br>
Flink有三个预实现的驱逐器，他们是:<br>
　　<code>CountEvitor</code>：在窗口中保持一个用户指定数量的元素，并在窗口的开始处丢弃剩余的其他元素<br>
　　<code>DeltaEvitor</code>: 通过一个<code>DeltaFunction</code>和一个阈值，计算窗口缓存中最近的一个元素和剩余的所有元素的<code>delta</code>值，并清除<code>delta</code>值大于或者等于阈值的元素<br>
　　<code>TimeEvitor</code>:使用一个<code>interval</code>的毫秒数作为参数，对于一个给定的窗口，它会找出元素中的最大时间戳<code>max_ts</code>，并清除时间戳小于<code>max_tx - interval</code>的元素。<br>
默认情况下:所有预实现的<code>evitor</code>都是在<code>window function</code>前应用它们的逻辑<br>
注意:指定一个<code>Evitor</code>要防止预聚合，因为窗口中的所有元素必须得在计算之前传递到驱逐器中<br>
注意:Flink 并不保证窗口中的元素是有序的，所以驱逐器可能从窗口的开始处清除，元素到达的先后不是那么必要。</p>
<h3 id="允许延迟allowed-lateness">允许延迟(Allowed Lateness)</h3>
<p>当处理事件时间的<code>window</code>时，可能会出现元素到达晚了，Flink用来与事件时间联系的水印已经过了元素所属的窗口的最后时间。可以查看事件时间(event time https://ci.apache.org/projects/flink/flink-docs-release-1.3/dev/event_time.html )尤其是晚到元素(late elements https://ci.apache.org/projects/flink/flink-docs-release-1.3/dev/event_time.html#late-elements )来了解Flink如何处理事件时间的讨论。<br>
　　默认情况下，当水印已经过了窗口的最后时间时晚到的元素会被丢弃。然而，Flink允许为窗口操作指定一个最大允许时延，允许时延指定了元素可以晚到多长时间，默认情况下是0。水印已经过了窗口最后时间后才来的元素，如果还未到窗口最后时间加时延时间，那么元素任然添加到窗口中。如果依赖触发器的使用的话，晚到但是未丢弃的元素可能会导致窗口再次被触发。<br>
　　为了达到这个目的，Flink将保持窗口的状态直到允许时延的发生，一旦发生，Flink将清除<code>Window</code>，删除<code>window</code>的状态，如<code>Window</code> 生命周期章节中所描述的那样。<br>
默认情况下，允许时延为0，也就是说水印之后到达的元素将被丢弃。<br>
你可以按如下方式来指定一个允许时延：<br>
Java 代码:</p>
<pre><code class="language-java"> DataStream&lt;T&gt; input = ...;
 input
    .keyBy(&lt;key selector&gt;)
    .window(&lt;window assigner&gt;)
    .allowedLateness(&lt;time&gt;)
    .&lt;windowed transformation&gt;(&lt;window function&gt;);
</code></pre>
<p>Scala 代码:</p>
<pre><code class="language-java"> val input: DataStream[T] = ...
 input
    .keyBy(&lt;key selector&gt;)
    .window(&lt;window assigner&gt;)
    .allowedLateness(&lt;time&gt;)
    .&lt;windowed transformation&gt;(&lt;window function&gt;)
</code></pre>
<p>注意:当使用<code>GlobalWindows</code>分配器时，没有数据会被认为是延迟的，因为<code>Global Window</code>的最后时间是<code>Long.MAX_VALUE</code>。</p>
<p>###以侧输出来获取延迟数据(Getting Late Data as a Site Output)<br>
使用Flink的侧输出(https://ci.apache.org/projects/flink/flink-docs-release-1.3/dev/stream/side_output.html )特性，你可以获得一个已经被丢弃的延迟数据流。<br>
　　首先你需要在窗口化的数据流中调用<code>sideOutputLateData(OutputTag)</code>指定你需要获取延迟数据，然后，你就可以在<code>window</code> 操作的结果中获取到侧输出流了。<br>
代码如下：<br>
Java 代码：</p>
<pre><code class="language-java">final OutputTag&lt;T&gt; lateOutputTag = new OutputTag&lt;T&gt;(&quot;late-data&quot;){};
DataStream&lt;T&gt; input = ...;
DataStream&lt;T&gt; result = input
    .keyBy(&lt;key selector&gt;)
    .window(&lt;window assigner&gt;)
    .allowedLateness(&lt;time&gt;)
    .sideOutputLateData(lateOutputTag)
    .&lt;windowed transformation&gt;(&lt;window function&gt;);
DataStream&lt;T&gt; lateStream = result.getSideOutput(lateOutputTag);
</code></pre>
<p>Scala代码：</p>
<pre><code class="language-java">val lateOutputTag = OutputTag[T](&quot;late-data&quot;)
val input: DataStream[T] = ...
val result = input
    .keyBy(&lt;key selector&gt;)
    .window(&lt;window assigner&gt;)
    .allowedLateness(&lt;time&gt;)
    .sideOutputLateData(lateOutputTag)
    .&lt;windowed transformation&gt;(&lt;window function&gt;)
val lateStream = result.getSideOutput(lateOutputTag)
</code></pre>
<h3 id="延迟元素考虑late-elements-considerations">延迟元素考虑(Late elements considerations)</h3>
<p>当指定一个允许延迟大于0时，<code>window</code>以及<code>window</code>中的内容将会继续保持即使水印已经达到了<code>window</code>的最后时间。在这种情况下，当一个延迟事件到来而未丢弃时，它可能会触发<code>window</code>中的其他触发器。这些触发叫做<code>late firings</code>，因为它们是由延迟事件触发的，并相对于<code>window</code>中第一个触发即主触发而言。对于<code>session window</code>而言，<code>late firing</code>还会进一步导致<code>window</code>的合并，因为它们桥接了两个之前存在差距，而未合并的<code>window</code>。</p>
<h3 id="有用状态大小的考虑useful-state-size-considerations">有用状态大小的考虑(Useful state size considerations)</h3>
<p><code>window</code> 可以定义一个很长的周期(例如：一天、一周或者一月)，因此积累了相当大的状态。这里有些规则，当估计你的窗口计算的存储要求时，需要记住。<br>
　　1. Flink会在每个窗口中为每个属于它的元素创建一份备份，鉴于此，滚动窗口保存了每个元素的一个备份，与此相反，滑动窗口会为每个元素创建几个备份，如<code>Window Assigner</code>章节所述。因此，一个窗口大小为1天，滑动大小为1秒的滑动窗口可能就不是个好的策略了。<br>
　　2. <code>FoldFunction</code>和<code>ReduceFunction</code>可以制定reduce的存储需求，因为它们预聚合元素并且每个窗口只保存一个值。相反，只有<code>WindowFunction</code>需要累积所有的元素。<br>
　　3. 使用<code>Evitor</code>需要避免任何预聚合操作，因为窗口中的所有元素都需要在应用于计算之前传递到<code>evitor</code>中</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[第六章 Flink 中的 Window]]></title>
        <id>https://tianxiawuhao.github.io/post/FnbZ5kiHQ/</id>
        <link href="https://tianxiawuhao.github.io/post/FnbZ5kiHQ/">
        </link>
        <updated>2021-04-12T07:14:19.000Z</updated>
        <content type="html"><![CDATA[<h2 id="window">Window</h2>
<h3 id="window-概述">Window 概述</h3>
<p style="text-indent:2em">streaming 流式计算是一种被设计用于处理无限数据集的数据处理引擎， 而无限数据集是指一种不断增长的本质上无限的数据集， 而 window 是一种切割无限数据
为有限块进行处理的手段。</p>
<p style="text-indent:2em">Window 是无限数据流处理的核心， Window 将一个无限的 stream 拆分成有限大小的” buckets” 桶， 我们可以在这些桶上做计算操作。</p>
<h3 id="window-类型">Window 类型</h3>
<p>Window 可以分成两类：</p>
<ol>
<li>CountWindow： 按照指定的数据条数生成一个 Window， 与时间无关。</li>
<li>TimeWindow： 按照时间生成 Window。<br>
对于 TimeWindow， 可以根据窗口实现原理的不同分成三类： <code>滚动窗口（ TumblingWindow）</code> 、 <code>滑动窗口（ Sliding Window）</code>、 <code>和会话窗口（ Session Window）</code> 。</li>
</ol>
<h4 id="滚动窗口tumbling-windows">滚动窗口（Tumbling Windows）</h4>
<p>将数据依据固定的窗口长度对数据进行切片。<br>
特点： <code>时间对齐</code>， <code>窗口长度固定</code>， <code>没有重叠</code>。</p>
<p style="text-indent:2em">滚动窗口分配器将每个元素分配到一个指定窗口大小的窗口中， 滚动窗口有一个固定的大小， 并且不会出现重叠。 例如： 如果你指定了一个 5 分钟大小的滚动窗
口， 窗口的创建如下图所示：</p>
<p><img src="https://tianxiawuhao.github.io/post-images/1618471104246.png" alt="" loading="lazy"><br>
图 滚动窗口<br>
适用场景： 适合做 BI 统计等（ 做每个时间段的聚合计算） 。</p>
<h4 id="滑动窗口sliding-windows">滑动窗口（Sliding Windows）</h4>
<p>滑动窗口是固定窗口的更广义的一种形式， 滑动窗口由固定的窗口长度和滑动间隔组成。<br>
特点：<code>时间对齐</code>， <code>窗口长度固定</code>， <code>可以有重叠</code>。</p>
<p style="text-indent:2em">滑动窗口分配器将元素分配到固定长度的窗口中， 与滚动窗口类似， 窗口的大小由窗口大小参数来配置， 另一个窗口滑动参数控制滑动窗口开始的频率。 因此，
滑动窗口如果滑动参数小于窗口大小的话， 窗口是可以重叠的， 在这种情况下元素会被分配到多个窗口中。</p>
<p style="text-indent:2em">例如， 你有 10 分钟的窗口和 5 分钟的滑动， 那么每个窗口中 5 分钟的窗口里包含着上个 10 分钟产生的数据， 如下图所示：</p>
<p><img src="https://tianxiawuhao.github.io/post-images/1618471120950.png" alt="" loading="lazy"><br>
图 滑动窗口<br>
适用场景： 对最近一个时间段内的统计（ 求某接口最近 5min 的失败率来决定是<br>
否要报警） 。</p>
<h4 id="会话窗口session-windows">会话窗口（Session Windows）</h4>
<p style="text-indent:2em">由一系列事件组合一个指定时间长度的 timeout 间隙组成， 类似于 web 应用的session， 也就是一段时间没有接收到新数据就会生成新的窗口。
特点： 时间无对齐。</p>
<p style="text-indent:2em">session 窗口分配器通过 session 活动来对元素进行分组， session 窗口跟滚动窗口和滑动窗口相比， 不会有重叠和固定的开始时间和结束时间的情况， 相反， 当它在一个固定的时间周期内不再收到元素， 即非活动间隔产生， 那个这个窗口就会关
闭。 一个 session 窗口通过一个 session 间隔来配置， 这个 session 间隔定义了非活跃
周期的长度， 当这个非活跃周期产生， 那么当前的 session 将关闭并且后续的元素将
被分配到新的 session 窗口中去。</p>
<p><img src="https://tianxiawuhao.github.io/post-images/1618471406934.png" alt="" loading="lazy"><br>
图 会话窗口</p>
<h2 id="window-api">Window API</h2>
<h3 id="timewindow">TimeWindow</h3>
<p>TimeWindow 是将指定时间范围内的所有数据组成一个 window， 一次对一个<br>
window 里面的所有数据进行计算。</p>
<ol>
<li>滚动窗口<br>
Flink 默认的时间窗口根据 Processing Time 进行窗口的划分， 将 Flink 获取到的<br>
数据根据进入 Flink 的时间划分到不同的窗口中。<br>
时间间隔可以通过 Time.milliseconds(x)， Time.seconds(x)， Time.minutes(x)等其<br>
中的一个来指定。</li>
</ol>
<pre><code class="language-java">import com.tan.flink.bean.SensorReading;
import com.tan.flink.source.SourceFromCustom;
import org.apache.flink.streaming.api.datastream.DataStreamSource;
import org.apache.flink.streaming.api.datastream.SingleOutputStreamOperator;
import org.apache.flink.streaming.api.environment.StreamExecutionEnvironment;
import org.apache.flink.streaming.api.windowing.time.Time;

public class TimeWindow_Tumbling {
    public static void main(String[] args) throws Exception {
        StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
        DataStreamSource&lt;SensorReading&gt; inputDataStream = env.addSource(new SourceFromCustom.CustomSource());
        SingleOutputStreamOperator&lt;SensorReading&gt; resultDataStream = inputDataStream.keyBy(&quot;id&quot;)
                .timeWindow(Time.seconds(5L)) // 滚动时间窗口大小
                .maxBy(&quot;temperature&quot;);
        resultDataStream.print();
        env.execute();
    }
}
</code></pre>
<ol start="2">
<li>滑动窗口（SlidingEventTimeWindows）<br>
滑动窗口和滚动窗口的函数名是完全一致的， 只是在传参数时需要传入两个参<br>
数， 一个是 window_size， 一个是 sliding_size。下面代码中的 sliding_size 设置为了 5s， 也就是说， 每 5s 就计算输出结果一次，每一次计算的 window 范围是 15s 内的所有元素。<br>
时间间隔可以通过 Time.milliseconds(x)， Time.seconds(x)， Time.minutes(x)等其<br>
中的一个来指定。</li>
</ol>
<pre><code class="language-java">import com.tan.flink.bean.SensorReading;
import com.tan.flink.source.SourceFromCustom;
import org.apache.flink.streaming.api.datastream.DataStreamSource;
import org.apache.flink.streaming.api.datastream.SingleOutputStreamOperator;
import org.apache.flink.streaming.api.environment.StreamExecutionEnvironment;
import org.apache.flink.streaming.api.windowing.time.Time;

public class TimeWindow_Sliding {
    public static void main(String[] args) throws Exception {
        StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
        DataStreamSource&lt;SensorReading&gt; inputDataStream = env.addSource(new SourceFromCustom.CustomSource());
        SingleOutputStreamOperator&lt;SensorReading&gt; resultDataStream = inputDataStream.keyBy(&quot;id&quot;)
                .timeWindow(Time.seconds(15L), Time.seconds(5)) // 窗口大小15秒 滑动大小5秒
                .minBy(&quot;temperature&quot;);
        resultDataStream.print();
        env.execute();
    }
}
</code></pre>
<h3 id="countwindow">CountWindow</h3>
<p style="text-indent:2em">CountWindow 根据窗口中相同 key 元素的数量来触发执行， 执行时只计算元素数量达到窗口大小的 key 对应的结果。</p>
注意： CountWindow 的 window_size 指的是相同 Key 的元素的个数， 不是输入
的所有元素的总数。
<p>1 滚动窗口</p>
<p style="text-indent:2em">默认的 CountWindow 是一个滚动窗口， 只需要指定窗口大小即可， 当元素数量达到窗口大小时， 就会触发窗口的执行。</p>
<pre><code class="language-java">import com.tan.flink.bean.SensorReading;
import com.tan.flink.source.SourceFromCustom;
import org.apache.flink.streaming.api.datastream.DataStreamSource;
import org.apache.flink.streaming.api.datastream.SingleOutputStreamOperator;
import org.apache.flink.streaming.api.environment.StreamExecutionEnvironment;

public class CountWindow_Tumbling {
    public static void main(String[] args) throws Exception {
        StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
        DataStreamSource&lt;SensorReading&gt; inputDataStream = env.addSource(new SourceFromCustom.CustomSource());
        SingleOutputStreamOperator&lt;SensorReading&gt; resultDataStream = inputDataStream.keyBy(&quot;id&quot;)
                .countWindow(3)
                .maxBy(&quot;temperature&quot;);
        resultDataStream.print();
        env.execute();
    }
}
</code></pre>
<p>2 滑动窗口</p>
<p style="text-indent:2em">滑动窗口和滚动窗口的函数名是完全一致的， 只是在传参数时需要传入两个参数， 一个是 window_size， 一个是 sliding_size。</p>
<p style="text-indent:2em">下面代码中的 sliding_size 设置为了 2， 也就是说， 每收到两个相同 key 的数据就计算一次， 每一次计算的 window 范围是 10 个元素。</p>
<pre><code class="language-java">import com.tan.flink.bean.SensorReading;
import com.tan.flink.source.SourceFromCustom;
import org.apache.flink.streaming.api.datastream.DataStreamSource;
import org.apache.flink.streaming.api.datastream.SingleOutputStreamOperator;
import org.apache.flink.streaming.api.environment.StreamExecutionEnvironment;

public class CountWindow_Sliding {
    public static void main(String[] args) throws Exception {
        StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
        DataStreamSource&lt;SensorReading&gt; inputDataStream = env.addSource(new SourceFromCustom.CustomSource());
        SingleOutputStreamOperator&lt;SensorReading&gt; resultDataStream = inputDataStream.keyBy(&quot;id&quot;)
                .countWindow(10, 2)
                .minBy(&quot;temperature&quot;);
        resultDataStream.print();
        env.execute();
    }
}
</code></pre>
<h3 id="window-function">window function</h3>
<p>window function 定义了要对窗口中收集的数据做的计算操作， 主要可以分为两<br>
类：</p>
<ol>
<li>增量聚合函数（ incremental aggregation functions）<br>
每条数据到来就进行计算， 保持一个简单的状态。 典型的增量聚合函数有<br>
ReduceFunction, AggregateFunction。</li>
<li>全窗口函数（ full window functions）<br>
先把窗口所有数据收集起来， 等到计算的时候会遍历所有数据。<br>
ProcessWindowFunction 就是一个全窗口函数。</li>
</ol>
<h3 id="其它可选-api">其它可选 API</h3>
<ol>
<li>trigger() —— 触发器<br>
定义 window 什么时候关闭， 触发计算并输出结果</li>
<li>evitor() —— 移除器,定义移除某些数据的逻辑</li>
<li>allowedLateness() —— 允许处理迟到的数据</li>
<li>sideOutputLateData() —— 将迟到的数据放入侧输出流</li>
<li>getSideOutput() —— 获取侧输出</li>
</ol>
<figure data-type="image" tabindex="1"><img src="https://tianxiawuhao.github.io/post-images/1618472074436.png" alt="" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[第五章 Flink 流处理 API]]></title>
        <id>https://tianxiawuhao.github.io/post/_JBiQuJHd/</id>
        <link href="https://tianxiawuhao.github.io/post/_JBiQuJHd/">
        </link>
        <updated>2021-04-11T01:28:06.000Z</updated>
        <content type="html"><![CDATA[<h2 id="environment">Environment</h2>
<h3 id="getexecutionenvironment">getExecutionEnvironment</h3>
<p style="text-indent:2em">创建一个执行环境， 表示当前执行程序的上下文。 如果程序是独立调用的， 则
此方法返回本地执行环境； 如果从命令行客户端调用程序以提交到集群， 则此方法
返回此集群的执行环境， 也就是说， getExecutionEnvironment 会根据查询运行的方
式决定返回什么样的运行环境， 是最常用的一种创建执行环境的方式。</p>
<pre><code class="language-java">// 批处理环境
ExecutionEnvironment env = ExecutionEnvironment.getExecutionEnvironment()
// 流式数据处理环境
StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment()
</code></pre>
<p>如果没有设置并行度， 会以 flink-conf.yaml 中的配置为准， 默认是 1。<br>
<img src="https://tianxiawuhao.github.io/post-images/1618450455593.png" alt="" loading="lazy"></p>
<h3 id="createlocalenvironment">createLocalEnvironment</h3>
<p>返回本地执行环境， 需要在调用时指定默认的并行度。</p>
<pre><code class="language-java"> LocalStreamEnvironment localEnvironment = StreamExecutionEnvironment.createLocalEnvironment(1);
</code></pre>
<h3 id="createremoteenvironment">createRemoteEnvironment</h3>
<p style="text-indent:2em">返回集群执行环境， 将 Jar 提交到远程服务器。 需要在调用时指定 JobManager的 IP 和端口号， 并指定要在集群中运行的 Jar 包。</p>
<pre><code class="language-java"> final ExecutionEnvironment env = ExecutionEnvironment.createRemoteEnvironment(
            cluster.getHostname(),
            cluster.getPort(),
            config
    );
</code></pre>
<p>scala</p>
<pre><code class="language-java">val env = ExecutionEnvironment.createRemoteEnvironment(&quot;jobmanage-hostname&quot;,
6123,&quot;YOURPATH//wordcount.jar&quot;)
</code></pre>
<h3 id="setparallelism">setParallelism</h3>
<pre><code class="language-java">// 为了打印到控制台的结果不乱序，我们配置全局的并发为1，改变并发对结果正确性没有影响
env.setParallelism(1);
</code></pre>
<h2 id="source">Source</h2>
<h3 id="从集合读取数据">从集合读取数据</h3>
<pre><code class="language-java">package myflink;

import lombok.*;

//传感器温度读数的数据类型
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder
@ToString
public class SensorReading {
    //属性 id,时间戳,温度值
    private String id;
    private Long timestamp;
    private Double temperature;

}

public class SourceReading_Collection {
    public static void main(String[] args) throws Exception {
        //创建执行环境
        StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
        //设置并行调度
        //env.setParallelism(1);
        //从集合中读取数据
        //属性 id,时间戳,温度值
        DataStream&lt;SensorReading&gt; dataStream = env.fromCollection(Arrays.asList(new SensorReading(&quot;sensor_1&quot;, 1537718199L, 35.8),
                new SensorReading(&quot;sensor_6&quot;, 1547718201L, 15.4),
                new SensorReading(&quot;sensor_7&quot;, 1547718202L, 6.7),
                new SensorReading(&quot;sensor_10&quot;, 1547718205L, 38.1)));
 
        DataStream&lt;Integer&gt; integerDataStream = env.fromElements(1, 2, 4, 67, 189);
        //打印输出
        dataStream.print(&quot;data&quot;);
        integerDataStream.print(&quot;int&quot;);
        //执行 Flink的jobName
        env.execute(&quot;SensorReading&quot;);
    }
}
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://tianxiawuhao.github.io/post-images/1618820709627.png" alt="" loading="lazy"></figure>
<h3 id="从文件读取数据">从文件读取数据</h3>
<pre><code class="language-java">public class SourceReading_File {
    public static void main(String[] args) throws Exception {
        //创建执行环境
        StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
        //读取文件
       DataStream&lt;String&gt; dataStream = env.readTextFile(filePath,charsetName);
       //打印输出
        dataStream.print(&quot;data&quot;);
        //执行 Flink的jobName
        env.execute(&quot;SensorReading&quot;);
    }
}
</code></pre>
<h3 id="以-kafka-消息队列的数据作为来源">以 kafka 消息队列的数据作为来源</h3>
<p>需要引入 kafka 连接器的依赖：<br>
<code>pom.xml</code></p>
<pre><code class="language-java">&lt;!--
https://mvnrepository.com/artifact/org.apache.flink/flink-connector-kafka-0.11
--&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.apache.flink&lt;/groupId&gt;
    &lt;artifactId&gt;flink-connector-kafka-0.11_2.12&lt;/artifactId&gt;
    &lt;version&gt;1.10.1&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<pre><code class="language-sh">//1. 创建一个Topic名为“test20201217”的主题
kafka-topics.bat --create --zookeeper localhost:2181 --replication-factor 1 --partitions 1 --topic test20201217
//2. 创建producer(生产者)，生产主题的消息
kafka-console-producer.bat --broker-list localhost:9092 --topic test20201217
//3. 创建consumer(消费者)，消费主题消息
kafka-console-consumer.bat --bootstrap-server localhost:9092 --topic test20201217
</code></pre>
<p>具体代码如下：</p>
<pre><code class="language-java">/**
 * kafkaSource
 *
 *    從指定的offset出消费kafka
 */
public class StreamingKafkaSource {

    public static void main(String[] args) throws Exception {
         // 创建流处理的执行环境
        StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
 
        // 配置KafKa
        //配置KafKa和Zookeeper的ip和端口
        Properties properties = new Properties();
        properties.setProperty(&quot;bootstrap.servers&quot;, &quot;localhost:9092&quot;);
        properties.setProperty(&quot;zookeeper.connect&quot;, &quot;localhost:2181&quot;);
        properties.setProperty(&quot;group.id&quot;, &quot;consumer-group&quot;);
        //将kafka和zookeeper配置信息加载到Flink的执行环境当中StreamExecutionEnvironment
        FlinkKafkaConsumer011&lt;String&gt; myConsumer = new FlinkKafkaConsumer011&lt;String&gt;(&quot;test20201217&quot;, new SimpleStringSchema(),
                properties);
 
        //添加数据源，此处选用数据流的方式，将KafKa中的数据转换成Flink的DataStream类型
        DataStream&lt;String&gt; stream = env.addSource(myConsumer);
 
 
        //打印输出
        stream.print();
        //执行Job，Flink执行环境必须要有job的执行步骤，而以上的整个过程就是一个Job
        env.execute(&quot;kafka sink test&quot;);
    }
}
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://tianxiawuhao.github.io/post-images/1618821668432.png" alt="" loading="lazy"></figure>
<h3 id="自定义-source">自定义 Source</h3>
<p style="text-indent:2em">除了以上的 source 数据来源， 我们还可以自定义 source。 需要做的， 只是传入一个 SourceFunction 就可以。 具体调用如下：</p>
<pre><code class="language-java"> env.addSource( new MySensorSource() )
</code></pre>
<p>我们希望可以随机生成传感器数据， MySensorSource 具体的代码实现如下：</p>
<pre><code class="language-java">import org.apache.flink.api.common.functions.MapFunction;
import org.apache.flink.api.java.tuple.Tuple;
import org.apache.flink.api.java.tuple.Tuple2;
import org.apache.flink.streaming.api.datastream.DataStreamSource;
import org.apache.flink.streaming.api.datastream.SingleOutputStreamOperator;
import org.apache.flink.streaming.api.datastream.WindowedStream;
import org.apache.flink.streaming.api.environment.StreamExecutionEnvironment;
import org.apache.flink.streaming.api.functions.source.SourceFunction;
import org.apache.flink.streaming.api.functions.windowing.WindowFunction;
import org.apache.flink.streaming.api.windowing.time.Time;
import org.apache.flink.streaming.api.windowing.windows.TimeWindow;
import org.apache.flink.util.Collector;
import org.apache.log4j.Level;
import org.apache.log4j.Logger;
import org.apache.lucene.analysis.CachingTokenFilter;
 
import java.util.Random;
 
public class MySelfSourceTest01 {
    public static void main(String[] args) {
        Logger.getLogger(&quot;org&quot;).setLevel(Level.OFF);
        StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
        DataStreamSource&lt;String&gt; dataStreamSource = env.addSource(new SourceFunction&lt;String&gt;() {
            @Override
            public void run(SourceContext&lt;String&gt; ctx) throws Exception {
                Random random = new Random();
                // 循环可以不停的读取静态数据
                while (true) {
                    int nextInt = random.nextInt(100);
                    ctx.collect(&quot;random : &quot; + nextInt);
                    Thread.sleep(1000);
                }
            }
 
            @Override
            public void cancel() {
 
            }
        });
        WindowedStream&lt;Tuple2&lt;String, Integer&gt;, Tuple, TimeWindow&gt; window = dataStreamSource.map(new MapFunction&lt;String, Tuple2&lt;String, Integer&gt;&gt;() {
            @Override
            public Tuple2&lt;String, Integer&gt; map(String value) throws Exception {
                String[] sps = value.split(&quot;:&quot;);
                return new Tuple2&lt;&gt;(value, Integer.parseInt(sps[1].trim()));
            }
        }).keyBy(0).timeWindow(Time.seconds(5));
 
        SingleOutputStreamOperator&lt;String&gt; apply = window.apply(new WindowFunction&lt;Tuple2&lt;String, Integer&gt;, String, Tuple, TimeWindow&gt;() {
            @Override
            public void apply(Tuple tuple, TimeWindow window, Iterable&lt;Tuple2&lt;String, Integer&gt;&gt; input, Collector&lt;String&gt; out) throws Exception {
                input.forEach(x -&gt; {
                    System.out.println(&quot;apply function -&gt; &quot; + x.f0);
                    out.collect(x.f0);
                });
            }
        });
 
        apply.print();
 
        try {
            env.execute(&quot;myself_source_test01&quot;);
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}
</code></pre>
<h2 id="transform">Transform</h2>
<p>转换算子</p>
<h3 id="map">map</h3>
<figure data-type="image" tabindex="3"><img src="https://tianxiawuhao.github.io/post-images/1618450860405.png" alt="" loading="lazy"></figure>
<pre><code class="language-java">stream.map { x =&gt; x * 2 }
// 1.map.把string转化成长度输出
//参数T  R
//T就是传的数据，什么类型都行
//R就是返回的类型
DataStream&lt;Integer&gt; mapStream = stringDataStream.map(new MapFunction&lt;String, Integer&gt;() {
    @Override
    public Integer map(String value) throws Exception {
        return value.length();
    }
});

</code></pre>
<h3 id="flatmap">flatMap</h3>
<p>flatMap 的函数签名： <code>def flatMap[A,B](as: List[A])(f: A ⇒ List[B]): List[B]</code><br>
例如: flatMap(List(1,2,3))(i ⇒ List(i,i))<br>
结果是 List(1,1,2,2,3,3),<br>
而 List(&quot;a b&quot;, &quot;c d&quot;).flatMap(line ⇒ line.split(&quot; &quot;))<br>
结果是 List(a, b, c, d)。</p>
<pre><code class="language-java">stream.flatMap{
    x =&gt; x.split(&quot; &quot;)
}
//2、flatmap,按照逗号分隔
DataStream&lt;String&gt; flatMapStream = stringDataStream.flatMap(new FlatMapFunction&lt;String, String&gt;() {
    @Override
    public void flatMap(String value, Collector&lt;String&gt; out) throws Exception {
        String[] fields = value.split(&quot;,&quot;);
        for(String field:fields)
            out.collect(field);
    }
});

</code></pre>
<h3 id="filter">Filter</h3>
<figure data-type="image" tabindex="4"><img src="https://tianxiawuhao.github.io/post-images/1618450868563.png" alt="" loading="lazy"></figure>
<pre><code class="language-java">stream.filter{
    x =&gt; x == 1
}
 //3.filter,筛选sensor_1开头的的id对应的数据
DataStream&lt;String&gt; filterStream = stringDataStream.filter(new FilterFunction&lt;String&gt;() {
    @Override
    public boolean filter(String value) throws Exception {
        return value.startsWith(&quot;sensor_1&quot;);
    }
});
</code></pre>
<h3 id="keyby">KeyBy</h3>
<figure data-type="image" tabindex="5"><img src="https://tianxiawuhao.github.io/post-images/1618450909734.png" alt="" loading="lazy"></figure>
<p><code>DataStream → KeyedStream</code>： 逻辑地将一个流拆分成不相交的分区， 每个分区包含具有相同 key 的元素， 在内部以 hash 的形式实现的。</p>
<pre><code class="language-java"> DataStream&lt;Tuple2&lt;String, Integer&gt;&gt; windowCounts = text
                .flatMap(new FlatMapFunction&lt;String, Tuple2&lt;String, Integer&gt;&gt;() {
                    @Override
                    public void flatMap(String value, Collector&lt;Tuple2&lt;String, Integer&gt;&gt; out) {
                        for (String word : value.split(&quot;\\s&quot;)) {
                            out.collect(Tuple2.of(word, 1));
                        }
                    }
                })
                //按Tuple2的第一个属性进行分区
                .keyBy(0)

//或者根据对象属性进行分区
 KeyedStream&lt;SensorReading, String&gt; tempkeyedStream = mapDataStream.keyBy(mpdata -&gt; mpdata.getId());
</code></pre>
<h3 id="滚动聚合算子rolling-aggregation">滚动聚合算子（Rolling Aggregation）</h3>
<p>这些算子可以针对 KeyedStream 的每一个支流做聚合。</p>
<ol>
<li>sum()</li>
<li>min()</li>
<li>max()</li>
<li>minBy()</li>
<li>maxBy()</li>
</ol>
<h3 id="reduce">Reduce</h3>
<p><code>KeyedStream → DataStream</code>： 一个分组数据流的聚合操作， 合并当前的元素和上次聚合的结果， 产生一个新的值， 返回的流中包含每一次聚合的结果， 而不是只返回最后一次聚合的最终结果。</p>
<pre><code class="language-java">DataStream&lt;String&gt; stream2 = env.readTextFile(&quot;YOUR_PATH\\sensor.txt&quot;)
    .map( data =&gt; {
        String[] dataArray = data.split(&quot;,&quot;)
        SensorReading(dataArray(0).trim, dataArray(1).trim.toLong,
        dataArray(2).trim.toDouble)
    })
    .keyBy(&quot;id&quot;)
    .reduce( (x, y) =&gt; SensorReading(x.id, x.timestamp + 1, y.temperature) )
</code></pre>
<h3 id="split-和-select">Split 和 Select</h3>
<p>Split<br>
<img src="https://tianxiawuhao.github.io/post-images/1618450986439.png" alt="" loading="lazy"><br>
图 Split<br>
<code>DataStream → SplitStream</code>： 根据某些特征把一个 DataStream 拆分成两个或者<br>
多个 DataStream。<br>
Select<br>
<img src="https://tianxiawuhao.github.io/post-images/1618450993631.png" alt="" loading="lazy"><br>
图 Select<br>
<code>SplitStream→ DataStream</code>： 从一个 SplitStream 中获取一个或者多个<br>
DataStream。<br>
需求： 传感器数据按照温度高低（ 以 30 度为界） ， 拆分成两个流。</p>
<pre><code class="language-java">public static void main(String[] args) throws Exception {
        final StreamExecutionEnvironment env=StreamExecutionEnvironment.getExecutionEnvironment();
        DataStream&lt;Long&gt; input=env.generateSequence(0,10);
        SplitStream&lt;Long&gt; splitStream = input.split(new OutputSelector&lt;Long&gt;(){
            @Override
            public Iterable&lt;String&gt; select(Long value) {
                List&lt;String&gt; output = new ArrayList&lt;String&gt;();
                if (value % 2 == 0) {
                    output.add(&quot;even&quot;);
                }else {
                    output.add(&quot;odd&quot;);
                }
                return output;
            }
        });
        //splitStream.print();
        DataStream&lt;Long&gt; even = splitStream.select(&quot;even&quot;);
        DataStream&lt;Long&gt; odd = splitStream.select(&quot;odd&quot;);
        DataStream&lt;Long&gt; all = splitStream.select(&quot;even&quot;,&quot;odd&quot;);
        //even.print();
        odd.print();
        //all.print();
        env.execute();
    }
</code></pre>
<h3 id="connect-和-comap">Connect 和 CoMap</h3>
<p><img src="https://tianxiawuhao.github.io/post-images/1618451022910.png" alt="" loading="lazy"><br>
图 Connect 算子<br>
<code>DataStream,DataStream → ConnectedStreams</code>： 连接两个保持他们类型的数<br>
据流， 两个数据流被 Connect 之后， 只是被放在了一个同一个流中， 内部依然保持<br>
各自的数据和形式不发生任何变化， 两个流相互独立。</p>
<p><code>CoMap</code>,<code>CoFlatMap</code><br>
<img src="https://tianxiawuhao.github.io/post-images/1618451082833.png" alt="" loading="lazy"><br>
图 CoMap/CoFlatMap</p>
<p><code>ConnectedStreams → DataStream</code>： 作用于 ConnectedStreams 上， 功能与 map<br>
和 flatMap 一样， 对 ConnectedStreams 中的每一个 Stream 分别进行 map 和 flatMap<br>
处理。</p>
<pre><code class="language-java">DataStream  warning = high.map( sensorData =&gt; (sensorData.id,
sensorData.temperature) )
ConnectedStreams connected = warning.connect(low)
DataStream coMap = connected.map(
    warningData =&gt; (warningData._1, warningData._2, &quot;warning&quot;),
    lowData =&gt; (lowData.id, &quot;healthy&quot;)
)
</code></pre>
<h3 id="union">Union</h3>
<p><img src="https://tianxiawuhao.github.io/post-images/1618451109858.png" alt="" loading="lazy"><br>
图 Union<br>
<code>DataStream → DataStream</code>： 对两个或者两个以上的 DataStream 进行 union 操<br>
作， 产生一个包含所有 DataStream 元素的新 DataStream。</p>
<pre><code class="language-java">//合并以后打印
 DataStream&lt;StartUpLog&gt; unionStream = appStoreStream.union(otherStream)
unionStream.print(&quot;union:::&quot;)
</code></pre>
<p><code>Connect</code> 与 <code>Union</code> 区别：</p>
<blockquote>
<ol>
<li>Union 之前两个流的类型必须是一样， Connect 可以不一样， 在之后的 coMap<br>
中再去调整成为一样的。</li>
<li>Connect 只能操作两个流， Union 可以操作多个。</li>
</ol>
</blockquote>
<h2 id="支持的数据类型">支持的数据类型</h2>
<p style="text-indent:2em">Flink 流应用程序处理的是以数据对象表示的事件流。 所以在 Flink 内部， 我们
需要能够处理这些对象。 它们需要被序列化和反序列化， 以便通过网络传送它们；
或者从状态后端、 检查点和保存点读取它们。 为了有效地做到这一点， Flink 需要明
确知道应用程序所处理的数据类型。 Flink 使用类型信息的概念来表示数据类型， 并
为每个数据类型生成特定的序列化器、 反序列化器和比较器。
Flink 还具有一个类型提取系统， 该系统分析函数的输入和返回类型， 以自动获
取类型信息， 从而获得序列化器和反序列化器。 但是， 在某些情况下， 例如 lambda
函数或泛型类型， 需要显式地提供类型信息， 才能使应用程序正常工作或提高其性
能。</p>
<p>Flink 支持 Java 和 Scala 中所有常见数据类型。 使用最广泛的类型有以下几种。</p>
<h3 id="基础数据类型">基础数据类型</h3>
<p>Flink 支持所有的 Java 和 Scala 基础数据类型， Int, Double, Long, String, …​</p>
<pre><code class="language-java">DataStream&lt;Long&gt; numbers = env.fromElements(1L, 2L, 3L, 4L)
numbers.map( n =&gt; n + 1 )
</code></pre>
<h3 id="java-和-scala-元组tuples">Java 和 Scala 元组（Tuples）</h3>
<pre><code class="language-java">DataStream&lt;String, Integer&gt; persons= env.fromElements(
    (&quot;Adam&quot;, 17),
    (&quot;Sarah&quot;, 23) 
)
persons.filter(p =&gt; p._2 &gt; 18)
</code></pre>
<h3 id="scala-样例类case-classes">Scala 样例类（case classes）</h3>
<pre><code class="language-java">case class Person(name: String, age: Int)
val persons: DataStream[Person] = env.fromElements(
Person(&quot;Adam&quot;, 17),
Person(&quot;Sarah&quot;, 23) )
persons.filter(p =&gt; p.age &gt; 18)
</code></pre>
<h3 id="java-简单对象pojos">Java 简单对象（POJOs）</h3>
<pre><code class="language-java">public class Person {
    public String name;
    public int age;
    public Person() {}
    public Person(String name, int age) {
        this.name = name;
        this.age = age;
    }
}
 DataStream&lt;Person&gt; persons = env.fromElements(
new Person(&quot;Alex&quot;, 42),
new Person(&quot;Wendy&quot;, 23));
</code></pre>
<h3 id="其它arrays-lists-maps-enums-等等">其它（Arrays, Lists, Maps, Enums, 等等）</h3>
<p>Flink 对 Java 和 Scala 中的一些特殊目的的类型也都是支持的， 比如 Java 的<br>
ArrayList， HashMap， Enum 等等。</p>
<h2 id="实现-udf-函数更细粒度的控制流">实现 UDF 函数——更细粒度的控制流</h2>
<h3 id="函数类function-classes">函数类（Function Classes）</h3>
<p>Flink 暴露了所有 udf 函数的接口(实现方式为接口或者抽象类)。 例如<br>
MapFunction, FilterFunction, ProcessFunction 等等。<br>
下面例子实现了 FilterFunction 接口：</p>
<pre><code class="language-java">public class CustomFilterFunction implements FilterFunction&lt;SensorReading&gt; {
    @Override
    public boolean filter(SensorReading sensorReading) throws Exception {
        return sensorReading.temperature&gt;30.0;
    }
}
public static void main(String[] args) throws Exception {
 // 创建流处理的执行环境
        StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
 
        // 从文件中读取数据
        String inputPath  = &quot;F:\\Projects\\BigData\\Flink\\FlinkTutorial\\src\\main\\resources\\sensor.txt&quot;;
        // 获取数据
        DataStreamSource&lt;String&gt; dataStream  = env.readTextFile(inputPath);
 
        // 1、先转换成SensorReading类型（简单转换操作）
        DataStream&lt;SensorReading&gt; stream =  dataStream.map(new MapFunction&lt;String, SensorReading&gt;() {
            @Override
            public SensorReading map(String data) throws Exception {
                String[] arr = data.split(&quot;,&quot;);
                return new SensorReading(arr[0], arr[1], Double.valueOf(arr[2].toString()));
            }
        });
 
       // 调用自定义CustomFilterFunction类的，实现过滤
       DataStream&lt;SensorReading&gt; dataStreamFilter = stream.filter(new CustomFilterFunction());
 
       dataStreamFilter .print(&quot;CustomFilterFunction&quot;);
      
       env.execute(&quot;Function test&quot;);
}
</code></pre>
<p>还可以将函数实现成匿名类</p>
<pre><code class="language-java"> DataStream&lt;SensorReading&gt; dataStreamFilter = stream.filter(new FilterFunction&lt;SensorReading&gt;() {
    @Override
    public boolean filter(SensorReading sensorReading) throws Exception {
        return sensorReading.temperature&gt;30.0;
    }
);
</code></pre>
<p>我们 filter 的字符串&quot;flink&quot;还可以当作参数传进去。</p>
<pre><code class="language-java">DataStream&lt;String&gt; tweets = ...
DataStream&lt;String&gt; flinkTweets = tweets.filter(new KeywordFilter(&quot;flink&quot;))

public class KeywordFilter(String keyWord) implements FilterFunction&lt;String&gt; {
    @Override
    public boolean filter(String value) throws Exception {
        return value.contains(keyWord)
    }
}
</code></pre>
<h3 id="匿名函数lambda-functions">匿名函数（Lambda Functions）</h3>
<pre><code class="language-java">DataStream&lt;String&gt; tweets = ...
DataStream&lt;String&gt; flinkTweets = tweets.filter((value) -&gt;value.contains(&quot;flink&quot;))
</code></pre>
<h3 id="富函数rich-functions">富函数（Rich Functions）</h3>
<p>“ 富函数” 是 DataStream API 提供的一个函数类的接口， 所有 Flink 函数类都<br>
有其 Rich 版本。 它与常规函数的不同在于， 可以获取运行环境的上下文， 并拥有一<br>
些生命周期方法， 所以可以实现更复杂的功能。</p>
<ol>
<li>RichMapFunction</li>
<li>RichFlatMapFunction</li>
<li>RichFilterFunction<br>
…​</li>
</ol>
<p>Rich Function 有一个生命周期的概念。 典型的生命周期方法有：</p>
<ol>
<li>open()方法是 rich function 的初始化方法， 当一个算子例如 map 或者 filter<br>
被调用之前 open()会被调用。</li>
<li>close()方法是生命周期中的最后一个调用的方法， 做一些清理工作。</li>
<li>getRuntimeContext()方法提供了函数的 RuntimeContext 的一些信息， 例如函<br>
数执行的并行度， 任务的名字， 以及 state 状态</li>
</ol>
<pre><code class="language-java">    public class MyFlatMap extends RichFlatMapFunction&lt;Tuple2&lt;Integer, Integer&gt;, Tuple2&lt;Integer, Integer&gt;&gt; {
    Integer subTaskIndex = 0
    StreamingRuntimeContext context = (StreamingRuntimeContext) getRuntimeContext()
     public void open(Configuration parameters) throws Exception {
         subTaskIndex = context.getIndexOfThisSubtask
        // 以下可以做一些初始化工作， 例如建立一个和 HDFS 的连接
    }
    @Override
    public void flatMap(Tuple2&lt;Integer, Integer&gt; integerIntegerTuple2, Collector&lt;Tuple2&lt;Integer, Integer&gt;&gt; collector) throws Exception {
            if (in % 2 == subTaskIndex) {
                out.collect((subTaskIndex, in))
            }
        }
    public void close() throws Exception {
        // 以下做一些清理工作， 例如断开和 HDFS 的连接。
    }
}
</code></pre>
<h2 id="sink">Sink</h2>
<p style="text-indent:2em">Flink 没有类似于 spark 中 foreach 方法， 让用户进行迭代的操作。 虽有对外的
输出操作都要利用 Sink 完成。 最后通过类似如下方式完成整个任务最终输出操作。
stream.addSink(new MySink(xxxx))
官方提供了一部分的框架的 sink。 除此以外， 需要用户自定义实现 sink。</p>
<figure data-type="image" tabindex="6"><img src="https://tianxiawuhao.github.io/post-images/1618451579569.png" alt="" loading="lazy"></figure>
<h3 id="kafka">Kafka</h3>
<pre><code class="language-java">pom.xml
&lt;!--
https://mvnrepository.com/artifact/org.apache.flink/flink-connector-kafka-0.11
--&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.apache.flink&lt;/groupId&gt;
    &lt;artifactId&gt;flink-connector-kafka-0.11_2.12&lt;/artifactId&gt;
    &lt;version&gt;1.10.1&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<p>主函数中添加 sink：</p>
<pre><code class="language-java">DataStream&lt;String&gt; union = high.union(low).map(item-&gt;item.temperature.toString)
union.addSink(new FlinkKafkaProducer&lt;String&gt;(&quot;localhost:9092&quot;,
&quot;test&quot;, new SimpleStringSchema()))
</code></pre>
<h3 id="redis">Redis</h3>
<p>pom.xml</p>
<pre><code class="language-java">&lt;!-- https://mvnrepository.com/artifact/org.apache.bahir/flink-connector-redis
--&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.apache.bahir&lt;/groupId&gt;
    &lt;artifactId&gt;flink-connector-redis_2.11&lt;/artifactId&gt;
    &lt;version&gt;1.0&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<p>定义一个 redis 的 mapper 类， 用于定义保存到 redis 时调用的命令：</p>
<pre><code class="language-java">public class MyRedisMapper extends RedisMapper&lt;SensorReading&gt;{
    public RedisCommandDescription getCommandDescription{
        new RedisCommandDescription(RedisCommand.HSET, &quot;sensor_temperature&quot;)
    } 
    public String  getValueFromData(SensorReading t){
        t.temperature.toString
        String getKeyFromData(SensorReading t) {
            return t.id
        }
    }
}
</code></pre>
<p>在主函数中调用：</p>
<pre><code class="language-java">dataStream.addSink( new RedisSink&lt;SensorReading&gt;( new
FlinkJedisPoolConfig.Builder().setHost(&quot;localhost&quot;).setPort(6379).build(), new MyRedisMapper) )
</code></pre>
<h3 id="elasticsearch">Elasticsearch</h3>
<p>pom.xml</p>
<pre><code class="language-java">&lt;dependency&gt;
    &lt;groupId&gt;org.apache.flink&lt;/groupId&gt;
    &lt;artifactId&gt;flink-connector-elasticsearch6_2.12&lt;/artifactId&gt;
    &lt;version&gt;1.10.1&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<p>在主函数中调用：</p>
<pre><code class="language-java">List httpHosts = new ArrayList&lt;HttpHost&gt;()
httpHosts.add(new HttpHost(&quot;localhost&quot;, 9200))
DataStream esSinkBuilder = new ElasticsearchSink.Builder&lt;SensorReading&gt;( httpHosts,new ElasticsearchSinkFunction&lt;SensorReading&gt; {
    public  Unit process(SensorReading t, RuntimeContext runtimeContext,
    RequestIndexer requestIndexer ) {
        println(&quot;saving data: &quot; + t)
        map json = new util.HashMap&lt;String, String&gt;()
        json.put(&quot;data&quot;, t.toString)
        IndexRequest  indexRequest =
        Requests.indexRequest().index(&quot;sensor&quot;).`type`(&quot;readingData&quot;).source(json)
        requestIndexer.add(indexRequest)
        println(&quot;saved successfully&quot;)
    }
} )
dataStream.addSink( esSinkBuilder.build() )
</code></pre>
<h3 id="jdbc-自定义-sink">JDBC 自定义 sink</h3>
<pre><code class="language-java">&lt;!-- https://mvnrepository.com/artifact/mysql/mysql-connector-java --&gt;
&lt;dependency&gt;
    &lt;groupId&gt;mysql&lt;/groupId&gt;
    &lt;artifactId&gt;mysql-connector-java&lt;/artifactId&gt;
    &lt;version&gt;5.1.44&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<p>添加 MyJdbcSink</p>
<pre><code class="language-java">public class MyJdbcSink() extends RichSinkFunction&lt;SensorReading&gt;{
    Connection conn;
    PreparedStatement insertStmt;
    PreparedStatement updateStmt;
    // open 主要是创建连接
    public Unit open(Configuration parameters)  = {
        super.open(parameters)
        conn = DriverManager.getConnection(&quot;jdbc:mysql://localhost:3306/test&quot;,
        &quot;root&quot;, &quot;123456&quot;)
        insertStmt = conn.prepareStatement(&quot;INSERT INTO temperatures (sensor,
        temp) VALUES (?, ?)&quot;)
        updateStmt = conn.prepareStatement(&quot;UPDATE temperatures SET temp = ? WHERE
        sensor = ?&quot;)
    } 
    //调用连接， 执行 sql
    public Unit invoke(SensorReading value , SinkFunction.Context[] context）{
        updateStmt.setDouble(1, value.temperature)
        updateStmt.setString(2, value.id)
        updateStmt.execute()
        if (updateStmt.getUpdateCount == 0) {
            insertStmt.setString(1, value.id)
            insertStmt.setDouble(2, value.temperature)
            insertStmt.execute()
        }
    } 
    public Unit close() {
        insertStmt.close()
        updateStmt.close()
        conn.close()
    }
}
</code></pre>
<p>在 main 方法中增加， 把明细保存到 mysql 中</p>
<pre><code class="language-java">dataStream.addSink(new MyJdbcSink())
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[第四章 Flink 运行架构]]></title>
        <id>https://tianxiawuhao.github.io/post/4_EhGqVjY/</id>
        <link href="https://tianxiawuhao.github.io/post/4_EhGqVjY/">
        </link>
        <updated>2021-04-10T09:17:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="flink-运行时的组件">Flink 运行时的组件</h2>
<p style="text-indent:2em">Flink 运行时架构主要包括四个不同的组件， 它们会在运行流处理应用程序时协同工作：
作业管理器（JobManager） 、 资源管理器（ResourceManager） 、 任务管理器（TaskManager），以及分发器（Dispatcher） 。 因为 Flink 是用 Java 和 Scala 实现的， 所以所有组件都会运行在Java 虚拟机上。 每个组件的职责如下：</p>
1. 作业管理器（JobManager）
<p style="text-indent:2em">控制一个应用程序执行的主进程， 也就是说， 每个应用程序都会被一个不同的
JobManager 所控制执行。 JobManager 会先接收到要执行的应用程序， 这个应用程序会包括：
作业图（JobGraph） 、 逻辑数据流图（logical dataflow graph） 和打包了所有的类、 库和其它
资源的 JAR 包。 JobManager 会把 JobGraph 转换成一个物理层面的数据流图， 这个图被叫做
“执行图” （ExecutionGraph） ， 包含了所有可以并发执行的任务。 JobManager 会向资源管
理器（ResourceManager） 请求执行任务必要的资源， 也就是任务管理器（TaskManager） 上
的插槽（ slot） 。 一旦它获取到了足够的资源， 就会将执行图分发到真正运行它们的TaskManager 上。 而在运行过程中， JobManager 会负责所有需要中央协调的操作， 比如说检
查点（checkpoints） 的协调。</p>
2. 资源管理器（ResourceManager）
<p style="text-indent:2em">主要负责管理任务管理器（TaskManager） 的插槽（slot） ， TaskManger 插槽是 Flink 中
定义的处理资源单元。 Flink 为不同的环境和资源管理工具提供了不同资源管理器， 比如
YARN、 Mesos、 K8s， 以及 standalone 部署。 当 JobManager 申请插槽资源时， ResourceManager会将有空闲插槽的 TaskManager 分配给 JobManager。 如果 ResourceManager 没有足够的插槽来满足 JobManager 的请求， 它还可以向资源提供平台发起会话， 以提供启动 TaskManager进程的容器。 另外， ResourceManager 还负责终止空闲的TaskManager， 释放计算资源。</p>
3. 任务管理器（TaskManager）
<p style="text-indent:2em">Flink 中的工作进程。 通常在 Flink 中会有多个 TaskManager 运行， 每一个 TaskManager
都包含了一定数量的插槽（slots） 。 插槽的数量限制了 TaskManager 能够执行的任务数量。
启动之后， TaskManager 会向资源管理器注册它的插槽； 收到资源管理器的指令后，TaskManager 就会将一个或者多个插槽提供给 JobManager 调用。 JobManager 就可以向插槽分配任务（tasks） 来执行了。 在执行过程中， 一个 TaskManager 可以跟其它运行同一应用程序的 TaskManager 交换数据。</p>
4. 分发器（Dispatcher）
<p style="text-indent:2em">可以跨作业运行， 它为应用提交提供了 REST 接口。 当一个应用被提交执行时， 分发器
就会启动并将应用移交给一个 JobManager。 由于是 REST 接口， 所以 Dispatcher 可以作为集
群的一个 HTTP 接入点， 这样就能够不受防火墙阻挡。 Dispatcher 也会启动一个 Web UI， 用
来方便地展示和监控作业执行的信息。 Dispatcher 在架构中可能并不是必需的， 这取决于应
用提交运行的方式。</p>
<h2 id="任务提交流程">任务提交流程</h2>
<p>我们来看看当一个应用提交执行时， Flink 的各个组件是如何交互协作的：</p>
<figure data-type="image" tabindex="1"><img src="https://tianxiawuhao.github.io/post-images/1618219646371.png" alt="" loading="lazy"></figure>
<p>图 任务提交和组件交互流程</p>
<p style="text-indent:2em">上图是从一个较为高层级的视角， 来看应用中各组件的交互协作。 如果部署的集群环境
不同（例如 YARN， Mesos， Kubernetes， standalone 等） ， 其中一些步骤可以被省略， 或是
有些组件会运行在同一个 JVM 进程中。</p>
<p style="text-indent:2em">具体地， 如果我们将 Flink 集群部署到 YARN 上， 那么就会有如下的提交流程：</p>
<figure data-type="image" tabindex="2"><img src="https://tianxiawuhao.github.io/post-images/1618219672649.png" alt="" loading="lazy"></figure>
<p>图 Yarn 模式任务提交流程</p>
<p style="text-indent:2em">Flink 任 务 提 交 后 ， Client 向 HDFS 上 传 Flink 的 Jar 包 和 配 置 ， 之 后 向 YarnResourceManager 提 交 任 务 ， ResourceManager 分 配 Container 资 源 并 通 知 对 应 的NodeManager 启动 ApplicationMaster， ApplicationMaster 启动后加载 Flink 的 Jar 包和配置构建环境， 然后启动 JobManager， 之后 ApplicationMaster 向 ResourceManager申 请 资 源 启 动 TaskManager ， ResourceManager 分 配 Container 资 源 后 ， 由ApplicationMaster 通 知 资 源 所 在 节 点 的 NodeManager 启 动 TaskManager ，NodeManager 加载 Flink 的 Jar 包和配置构建环境并启动 TaskManager， TaskManager启动后向 JobManager 发送心跳包， 并等待 JobManager 向其分配任务。</p>
<h2 id="任务调度原理">任务调度原理</h2>
<p><img src="https://tianxiawuhao.github.io/post-images/1618219735041.png" alt="" loading="lazy"><br>
图 任务调度原理</p>
<p style="text-indent:2em">客 户 端 不 是 运 行 时 和 程 序 执 行 的 一 部 分 ， 但 它 用 于 准 备 并 发 送
dataflow(JobGraph)给 Master(JobManager)， 然后， 客户端断开连接或者维持连接以
等待接收计算结果。</p>
<p style="text-indent:2em">当 Flink 集 群 启 动 后 ， 首 先 会 启 动 一 个 JobManger 和 一 个 或 多 个 的TaskManager。 由 Client 提交任务给 JobManager， JobManager 再调度任务到各个TaskManager 去执行， 然后 TaskManager 将心跳和统计信息汇报给 JobManager。TaskManager 之间以流的形式进行数据的传输。 上述三者均为独立的 JVM 进程。</p>
<p style="text-indent:2em">Client 为提交 Job 的客户端， 可以是运行在任何机器上（ 与 JobManager 环境连通即可） 。 提交 Job 后， Client 可以结束进程（ Streaming 的任务） ， 也可以不结束并等待结果返回。</p>
<p style="text-indent:2em">JobManager`主 要 负 责 调 度 Job 并 协 调 Task 做 checkpoint ， 职 责 上 很 像Storm 的 Nimbus。 从 Client 处接收到 Job 和 JAR 包等资源后， 会生成优化后的执行计划， 并以 Task 的单元调度到各个 TaskManager 去执行。</p>
<p style="text-indent:2em">TaskManager`在启动的时候就设置好了槽位数（ Slot） ， 每个 slot 能启动一个Task， Task 为线程。 从 JobManager 处接收需要部署的 Task， 部署启动后， 与自己的上游建立 Netty 连接， 接收数据并处理。</p>
<h3 id="taskmanger-与-slots">TaskManger 与 Slots</h3>
<p style="text-indent:2em">Flink 中每一个 worker(TaskManager)都是一个 `JVM 进程`， 它可能会在`独立的线
程`上执行一个或多个 subtask。 为了控制一个 worker 能接收多少个 task， worker 通
过 task slot 来进行控制（ 一个 worker 至少有一个 task slot） 。</p>
<p style="text-indent:2em">每个 task slot 表示 TaskManager 拥有资源的`一个固定大小的子集` 。 假如一个
TaskManager 有三个 slot， 那么它会将其管理的内存分成三份给各个 slot。 资源 slot
化意味着一个 subtask 将不需要跟来自其他 job 的 subtask 竞争被管理的内存， 取而
代之的是它将拥有一定数量的内存储备。 需要注意的是， 这里不会涉及到 CPU 的隔
离， slot 目前仅仅用来隔离 task 的受管理的内存。</p>
<p style="text-indent:2em">通过调整 task slot 的数量， 允许用户定义 subtask 之间如何互相隔离。 如果一个
TaskManager 一个 slot， 那将意味着每个 task group 运行在独立的 JVM 中（ 该 JVM
可能是通过一个特定的容器启动的） ， 而一个 TaskManager 多个 slot 意味着更多的
subtask 可以共享同一个 JVM。 而在同一个 JVM 进程中的 task 将共享 TCP 连接（ 基
于多路复用） 和心跳消息。 它们也可能共享数据集和数据结构， 因此这减少了每个
task 的负载。</p>
<p><img src="https://tianxiawuhao.github.io/post-images/1618219822454.png" alt="" loading="lazy"><br>
图 TaskManager 与 Slot<br>
图 子任务共享</p>
<p style="text-indent:2em">默认情况下， Flink 允许子任务共享 slot， 即使它们是不同任务的子任务（ 前提是它们来自同一个 job） 。 这样的结果是， 一个 slot 可以保存作业的整个管道。</p>
<p style="text-indent:2em">Task Slot 是静态的概念， 是指 TaskManager 具有的并发执行能力， 可以通过参数 taskmanager.numberOfTaskSlots 进行配置； 而并行度 parallelism 是动态概念，即 TaskManager 运行程序时实际使用的并发能力， 可以通过参数 parallelism.default
进行配置。</p>
<p style="text-indent:2em">也就是说， 假设一共有 3 个 TaskManager， 每一个 TaskManager 中的分配 3 个TaskSlot， 也就是每个 TaskManager 可以接收 3 个 task， 一共 9 个 TaskSlot， 如果我们设置 parallelism.default=1， 即运行程序默认的并行度为 1， 9 个 TaskSlot 只用了 1个， 有 8 个空闲， 因此， 设置合适的并行度才能提高效率。</p>
<figure data-type="image" tabindex="3"><img src="https://tianxiawuhao.github.io/post-images/1618219838522.png" alt="" loading="lazy"></figure>
<h3 id="程序与数据流dataflow">程序与数据流（DataFlow）</h3>
<figure data-type="image" tabindex="4"><img src="https://tianxiawuhao.github.io/post-images/1618219930935.png" alt="" loading="lazy"></figure>
<p style="text-indent:2em">所有的 Flink 程序都是由三部分组成的： `Source` 、 `Transformation` 和 `Sink`。</p>
<p style="text-indent:2em">Source 负责读取数据源， Transformation 利用各种算子进行处理加工， Sink 负责输出。</p>
<p style="text-indent:2em">在运行时， Flink 上运行的程序会被映射成“ 逻辑数据流” （ dataflows） ， 它包含了这三部分。 每一个 dataflow 以一个或多个 sources 开始以一个或多个 sinks 结束。 dataflow 类似于任意的有向无环图（ DAG） 。 在大部分情况下， 程序中的转换
运算（ transformations） 跟 dataflow 中的算子（ operator） 是一一对应的关系， 但有
时候， 一个 transformation 可能对应多个 operator。</p>
<p><img src="https://tianxiawuhao.github.io/post-images/1618219939601.png" alt="" loading="lazy"><br>
图 程序与数据流</p>
<h3 id="执行图executiongraph">执行图（ExecutionGraph）</h3>
<p style="text-indent:2em">由 Flink 程序直接映射成的数据流图是 StreamGraph， 也被称为逻辑流图， 因为它们表示的是计算逻辑的高级视图。 为了执行一个流处理程序， Flink 需要将逻辑流
图转换为物理数据流图（ 也叫执行图） ， 详细说明程序的执行方式。
Flink 中的执行图可以分成四层： StreamGraph -> JobGraph -> ExecutionGraph ->
物理执行图。</p>
<p><code>StreamGraph</code>： 是根据用户通过 Stream API 编写的代码生成的最初的图。 用来表示程序的拓扑结构。<br>
<code>JobGraph</code>： StreamGraph 经过优化后生成了 JobGraph， 提交给 JobManager 的数据结构。 主要的优化为， 将多个符合条件的节点 chain 在一起作为一个节点， 这样可以减少数据在节点之间流动所需要的序列化/反序列化/传输消耗。<br>
<code>ExecutionGraph</code> ： JobManager 根 据 JobGraph 生 成 ExecutionGraph 。<code>ExecutionGraph</code> 是 JobGraph 的并行化版本， 是调度层最核心的数据结构。<br>
<code>物理执行图</code>： JobManager 根据 ExecutionGraph 对 Job 进行调度后， 在各个TaskManager 上部署 Task 后形成的“ 图” ， 并不是一个具体的数据结构。</p>
<figure data-type="image" tabindex="5"><img src="https://tianxiawuhao.github.io/post-images/1618219998246.png" alt="" loading="lazy"></figure>
<h3 id="并行度parallelism">并行度（Parallelism）</h3>
<p style="text-indent:2em">Flink 程序的执行具有并行、 分布式的特性。</p>
<p style="text-indent:2em">在执行过程中， 一个流（ stream） 包含一个或多个分区（ stream partition） ， 而
每一个算子（ operator） 可以包含一个或多个子任务（ operator subtask） ， 这些子任
务在不同的线程、 不同的物理机或不同的容器中彼此互不依赖地执行。</p>
<p style="text-indent:2em">一个特定算子的子任务（ subtask） 的个数被称之为其并行度（ parallelism） 。
一般情况下， 一个流程序的并行度， 可以认为就是其所有算子中最大的并行度。 一
个程序中， 不同的算子可能具有不同的并行度。</p>
<p><img src="https://tianxiawuhao.github.io/post-images/1618220010684.png" alt="" loading="lazy"><br>
图 并行数据流</p>
<p style="text-indent:2em">Stream 在算子之间传输数据的形式可以是 one-to-one(forwarding)的模式也可以是 redistributing 的模式， 具体是哪一种形式， 取决于算子的种类。</p>
<p><code>One-to-one</code>： stream(比如在 source 和 map operator 之间)维护着分区以及元素的<br>
顺序。 那意味着 map 算子的子任务看到的元素的个数以及顺序跟 source 算子的子<br>
任务生产的元素的个数、 顺序相同， map、 fliter、 flatMap 等算子都是 one-to-one 的<br>
对应关系。</p>
<ol>
<li>类似于 spark 中的窄依赖</li>
</ol>
<p><code>Redistributing</code>： stream(map()跟 keyBy/window 之间或者 keyBy/window 跟 sink之间)的分区会发生改变。 每一个算子的子任务依据所选择的 transformation 发送数据到不同的目标任务。 例如， keyBy() 基于 hashCode 重分区、 broadcast 和 rebalance会随机重新分区， 这些算子都会引起 redistribute 过程， 而 redistribute 过程就类似于Spark 中的 shuffle 过程。</p></p>
<ol>
<li>类似于 spark 中的宽依赖</li>
</ol>
<h3 id="任务链operator-chains">任务链（Operator Chains）</h3>
<p style="text-indent:2em">相同并行度的 one to one 操作， Flink 这样相连的算子链接在一起形成一个 task，原来的算子成为里面的一部分。 将算子链接成 task 是非常有效的优化： 它能减少线程之间的切换和基于缓存区的数据交换， 在减少时延的同时提升吞吐量。 链接的行
为可以在编程 API 中进行指定。</p>
<p><img src="https://tianxiawuhao.github.io/post-images/1618220048787.png" alt="" loading="lazy"><br>
图 task 与 operator chains</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[第三章 Flink 部署]]></title>
        <id>https://tianxiawuhao.github.io/post/aPsJFkgVZ/</id>
        <link href="https://tianxiawuhao.github.io/post/aPsJFkgVZ/">
        </link>
        <updated>2021-04-09T07:20:19.000Z</updated>
        <content type="html"><![CDATA[<h2 id="standalone-模式">Standalone 模式</h2>
<h3 id="安装">安装</h3>
<p>解压缩 flink-1.10.1-bin-scala_2.12.tgz， 进入 conf 目录中。</p>
<ol>
<li>修改 flink/conf/flink-conf.yaml 文件：<br>
<img src="https://tianxiawuhao.github.io/post-images/1617953255377.png" alt="" loading="lazy"></li>
<li>修改 /conf/slaves 文件：<br>
<img src="https://tianxiawuhao.github.io/post-images/1617953264929.png" alt="" loading="lazy"></li>
<li>分发给另外两台机子：<br>
<img src="https://tianxiawuhao.github.io/post-images/1617953282098.png" alt="" loading="lazy"></li>
<li>启动<br>
<img src="https://tianxiawuhao.github.io/post-images/1617953292985.png" alt="" loading="lazy"><br>
访问 http://localhost:8081 可以对 flink 集群和任务进行监控管理。<br>
<img src="https://tianxiawuhao.github.io/post-images/1617953298218.png" alt="" loading="lazy"></li>
</ol>
<h3 id="提交任务">提交任务</h3>
<ol>
<li>准备数据文件（ 如果需要）<br>
<img src="https://tianxiawuhao.github.io/post-images/1617953344217.png" alt="" loading="lazy"></li>
<li>把含数据文件的文件夹， 分发到 taskmanage 机器中<br>
<img src="https://tianxiawuhao.github.io/post-images/1617953351280.png" alt="" loading="lazy"></li>
</ol>
<p style="text-indent:2em">如 果 从 文 件 中 读 取 数 据 ， 由 于 是 从 本 地 磁 盘 读 取 ， 实 际 任 务 会 被 分 发 到taskmanage 的机器中， 所以要把目标文件分发。</p>
<ol start="3">
<li>执行程序</li>
</ol>
<pre><code class="language-sh">./flink run -c com.atguigu.wc.StreamWordCount –p 2
FlinkTutorial-1.0-SNAPSHOT-jar-with-dependencies.jar --host lcoalhost –port 7777
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://tianxiawuhao.github.io/post-images/1617953534756.png" alt="" loading="lazy"></figure>
<ol start="4">
<li>到目标文件夹中查看计算结果<br>
注 意 ： 如 果 计 算 结 果 输 出 到 文 件 ， 会 保 存 到 taskmanage 的 机 器 下 ， 不 会 在jobmanage 下。<br>
<img src="https://tianxiawuhao.github.io/post-images/1617953549746.png" alt="" loading="lazy"></li>
<li>在 webui 控制台查看计算过程<br>
<img src="https://tianxiawuhao.github.io/post-images/1617953465087.png" alt="" loading="lazy"></li>
</ol>
<h2 id="yarn-模式">Yarn 模式</h2>
<p style="text-indent:2em">以 Yarn 模式部署 Flink 任务时， 要求 Flink 是有 Hadoop 支持的版本， Hadoop环境需要保证版本在 2.2 以上， 并且集群中安装有 HDFS 服务</p>
<h3 id="flink-on-yarn">Flink on Yarn</h3>
<p style="text-indent:2em">Flink 提供了两种在 yarn 上运行的模式， 分别为 Session-Cluster 和 Per-Job-Cluster模式。</p>
<ol>
<li>
<p>Session-cluster 模式：</p>
<figure data-type="image" tabindex="2"><img src="https://tianxiawuhao.github.io/post-images/1617953616812.png" alt="" loading="lazy"></figure>
</li>
</ol>
<p style="text-indent:2em">Session-Cluster 模式需要先启动集群， 然后再提交作业， 接着会向 yarn 申请一块空间后， 资源永远保持不变。 如果资源满了， 下一个作业就无法提交， 只能等到
yarn 中的其中一个作业执行完成后， 释放了资源， 下个作业才会正常提交。 所有作
业共享 Dispatcher 和 ResourceManager； 共享资源； 适合规模小执行时间短的作业。
<p style="text-indent:2em">在 yarn 中初始化一个 flink 集群， 开辟指定的资源， 以后提交任务都向这里提交。 这个 flink 集群会常驻在 yarn 集群中， 除非手工停止。</p>
<ol>
<li>Per-Job-Cluster 模式：</li>
</ol>
<figure data-type="image" tabindex="3"><img src="https://tianxiawuhao.github.io/post-images/1617953630802.png" alt="" loading="lazy"></figure>
<p style="text-indent:2em">一个 Job 会对应一个集群， 每提交一个作业会根据自身的情况， 都会单独向 yarn申请资源， 直到作业执行完成， 一个作业的失败与否并不会影响下一个作业的正常
提交和运行。 独享 Dispatcher 和 ResourceManager， 按需接受资源申请； 适合规模大
长时间运行的作业。
<p style="text-indent:2em">每次提交都会创建一个新的 flink 集群， 任务之间互相独立， 互不影响， 方便管理。 任务执行完成之后创建的集群也会消失。</p>
<h3 id="session-cluster">Session Cluster</h3>
<ol>
<li>启动 hadoop 集群（ 略）</li>
<li>启动 yarn-session</li>
</ol>
<pre><code class="language-sh">./yarn-session.sh -n 2 -s 2 -jm 1024 -tm 1024 -nm test -d
</code></pre>
<p>其中：</p>
<blockquote>
<p>-n(--container)： TaskManager 的数量。<br>
-s(--slots)： 每个 TaskManager 的 slot 数量， 默认一个 slot 一个 core， 默认每个<br>
taskmanager 的 slot 的个数为 1， 有时可以多一些 taskmanager， 做冗余。<br>
-jm： JobManager 的内存（ 单位 MB)。<br>
-tm： 每个 taskmanager 的内存（ 单位 MB)。<br>
-nm： yarn 的 appName(现在 yarn 的 ui 上的名字)。<br>
-d： 后台执行。</p>
</blockquote>
<figure data-type="image" tabindex="4"><img src="https://tianxiawuhao.github.io/post-images/1617953810515.png" alt="" loading="lazy"></figure>
<ol start="3">
<li>执行任务</li>
</ol>
<pre><code class="language-sh">./flink run -c com.atguigu.wc.StreamWordCount
FlinkTutorial-1.0-SNAPSHOT-jar-with-dependencies.jar --host lcoalhost –port 7777
</code></pre>
<ol start="4">
<li>去 yarn 控制台查看任务状态</li>
</ol>
<figure data-type="image" tabindex="5"><img src="https://tianxiawuhao.github.io/post-images/1617953816721.png" alt="" loading="lazy"></figure>
<ol start="5">
<li>取消 yarn-session</li>
</ol>
<pre><code class="language-sh">yarn application --kill application_1577588252906_0001
3.2.2 Per Job Cluster
</code></pre>
<ol start="6">
<li>启动 hadoop 集群（ 略）</li>
<li>不启动 yarn-session， 直接执行 job</li>
</ol>
<pre><code class="language-sh">./flink run –m yarn-cluster -c com.atguigu.wc.StreamWordCount
FlinkTutorial-1.0-SNAPSHOT-jar-with-dependencies.jar --host lcoalhost –port
7777
</code></pre>
<h2 id="kubernetes-部署">Kubernetes 部署</h2>
<p style="text-indent:2em">容器化部署时目前业界很流行的一项技术， 基于 Docker 镜像运行能够让用户更加 方 便 地 对 应 用 进 行 管 理 和 运 维 。 容 器 管 理 工 具 中 最 为 流 行 的 就 是 Kubernetes（ k8s） ， 而 Flink 也在最近的版本中支持了 k8s 部署模式。</p>
<ol>
<li>搭建 Kubernetes 集群（ 略）</li>
<li>配置各组件的 yaml 文件</li>
</ol>
<p style="text-indent:2em">在 k8s 上构建 Flink Session Cluster， 需要将 Flink 集群的组件对应的 docker 镜像分别在 k8s 上启动， 包括 JobManager、 TaskManager、 JobManagerService 三个镜像服务。 每个镜像服务都可以从中央镜像仓库中获取。</p>
<ol start="3">
<li>启动 Flink Session Cluster</li>
</ol>
<pre><code class="language-sh">// 启动 jobmanager-service 服务
kubectl create -f jobmanager-service.yaml
// 启动 jobmanager-deployment 服务
kubectl create -f jobmanager-deployment.yaml
// 启动 taskmanager-deployment 服务
kubectl create -f taskmanager-deployment.yaml
</code></pre>
<ol start="4">
<li>访问 Flink UI 页面</li>
</ol>
<p style="text-indent:2em">集群启动后， 就可以通过 JobManagerServicers 中配置的 WebUI 端口， 用浏览器输入以下 url 来访问 Flink UI 页面了：</p>
<pre><code>http://{JobManagerHost:Port}/api/v1/namespaces/default/services/flink-jobmanager:ui/proxy
</code></pre>
]]></content>
    </entry>
</feed>